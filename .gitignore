__pycache__/
*.pyc
.pytest_cache/

# Real local Foundry runs contain generated workspaces, immutable artifacts,
# package registries, and SDK session state. They are audit data, not source.
.agent-world-live/

# Normal staged semantic-prefix runs are durable downstream-test inputs, but
# are still local generated state rather than source.
.agent-world-staged/

# Same for run state written under this root: generated workspaces, telemetry,
# control records and registries. Audit data, not source.
.agent-world-runs/

# Explicitly opt-in local diagnostics; never stage raw debug transcripts.
observability/**/_debug/

# Runtime pipeline outputs can include generated projects, traces, SDK state,
# local logs, and agent workspaces. Keep the committed seed sample visible.
outputs/*
!outputs/seed_scenario.jsonl

# Local research paper PDFs are useful evidence but should not be swept into
# normal source commits.
research/papers/*.pdf

# Optional local research-agent clones. Keep adapters in agent_world/research/adapters/.
external/research/*/
!external/research/README.md

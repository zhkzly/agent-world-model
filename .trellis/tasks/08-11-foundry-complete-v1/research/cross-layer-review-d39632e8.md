# Research: cross-layer review d39632e8

- Query: Fresh independent parent C3 review of the Direct trusted-wheel
  ingestion correction across all complete-v1 children.
- Scope: internal
- Date: 2026-08-11
- Reviewer: independent read-only `trellis-research`, model `gpt-5.6-terra`

## Decision

Decision: allow

- Plan digest: `d39632e88ff13a1b447e490beb379540fe22dcb839690cfdbf6138f114d1efe5`.
- Embedded Direct digest: `dec00ffe10140fb81258182347f658a0370dfdb5155f8344ed8fbc0b8751e372`.
- Plan revision: C3 revision 1; pre-execution installer-contradiction
  correction.
- Scope classification: local Direct installer boundary with verified
  compatibility for Repair, Expand, Consumer, Registry/package and Observe.
- Trigger: static plan contradiction only; no real terminal, Observe scene or
  Diagnosis Record applies.

The reviewer independently recomputed all sixteen raw hashes and the declared
aggregate, then independently reproduced the embedded Direct digest.

## Product Target And Impact Chain

The target remains natural-language need -> evidence-grounded executable
environment -> isolated Integration and independent Judge -> immutable Registry
package -> safe Observe. Expand remains a separate evidence/exact-parent entry
into the same trusted generation path. Consumer remains a downstream exact-
package Episode surface without environment, reward or release authority.

```text
trusted verified wheel store
  -> framework run-local flat directory
  -> fixed uv --no-index --find-links
  -> isolated Integration
  -> unchanged Judge / ReleaseKernel / Registry closure
```

## Owners And Consumer Compatibility

- Builder framework remains the sole dependency-admission owner: it parses
  metadata, validates lock hash/size, copies admitted wheels, supplies fixed
  argv and fails closed. Candidate code, network and ambient configuration gain
  no authority.
- Repair consumes immutable Work/Finding provenance and never owns or relaxes
  installation.
- Expand admits exact current Registry parents and reuses the existing Design
  and Candidate graphs; parent source access remains read-only and is not an
  installer authority.
- Consumer cold-reads exact released packages and privately materializes
  Episodes; it receives no dependency-selection role.
- Package/Registry still bind passed Integration, source closure, Judge
  evidence and receipt. Observe remains a safe read-only projection.

The run-local wheel directory and empty uv config are bounded inputs to the
existing framework transaction, not a downloader, package-index client,
generic configuration system, cache mutation, graph node or control plane.

## Smallest Allowed Implementation And Proof

1. Implement only the fixed `uv 0.11.29` argv/environment and trusted-wheel
   verification/copy boundary.
2. Prove exact argv, hash/size agreement, absent ambient/index configuration
   and fail-before-execution rejection of hostile dependency sources/builds.
3. Prove one valid locked-wheel install in a fresh external venv without
   installing or mutating the candidate root.
4. Continue the existing Direct true-boundary sequence and read Observe after
   every terminal. Later children retain their separately reviewed proofs.

## Non-Claims And Next Gate

This allow proves only the written correction. It does not prove installation,
CandidateBuild, Direct release, Repair, Expand/multi-parent, Consumer/SFT/RL,
Registry publication, provider availability or product completion.

Next permitted gate: add this record to parent and Direct implementation/check
contexts and dispatch implementation. Any digest, installer trust-boundary or
new real-scene change expires it.

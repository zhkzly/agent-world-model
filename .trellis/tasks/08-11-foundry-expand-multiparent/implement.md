# Foundry Expand and multi-parent evolution — implementation plan

1. Record exact Direct and optional Repair commits plus shared contract
   digests; verify the clean worktree and legacy-reference firewall.
2. Curate manifests with parent/source-of-truth contracts and close exact
   request/release/profile/source/parent snapshot, split outcome,
   current package-use admission, operator/lineage serialization and Node cards
   against real code.
3. Dispatch a fresh read-only Campaign/Release critic using explicit
   `--provider codex --model gpt-5.6-terra`; implement only after exact-digest
   `allow`.
4. Dispatch implementation with explicit
   `--provider codex --model gpt-5.6-terra`, then implement frozen campaign
   persistence, real ExpansionSource adapters,
   bounded Researcher synthesis, `directed@1`, framework admission,
   `ToolSurface` and `Composite`.
5. Reuse existing DesignGraph/CandidateGraph as ordinary typed components.
   Add no third generic DAG or second release path.
6. Implement exact parent re-resolution and Builder-only read-only source roots,
   child self-containment checks, semantic/implementation lineage and Campaign
   Observe.
7. Run deterministic tests for immutable snapshot/Policy input across mutable
   profile/catalog/feedback changes, post-freeze quarantine/supersession
   blocking before Design/Build, split execution/hard-gate/release outcomes,
   mandatory infrastructure evidence, policy replay, no-feedback behavior,
   plural parents, source secrecy and lineage. Prove each child reuses the
   Direct difficulty compiler/validator, commits a complete schema and rejects
   parent-union or candidate-defined levels.
8. Run a real documentation-grounded single-parent Campaign and read Observe.
9. Run a real useful multi-parent Campaign with two exact released parents and
   prove both parent capabilities plus an integrated unknown-seed task.
10. Dispatch check with explicit
    `--provider codex --model gpt-5.6-terra`; freeze exact released
    child package/commit refs for Consumer.

Stop and revise the plan if useful composition requires a new runtime family,
automatic framework source merge, inherited verdict or shared graph contract
change.

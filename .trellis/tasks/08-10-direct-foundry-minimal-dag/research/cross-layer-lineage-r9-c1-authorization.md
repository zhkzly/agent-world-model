# R9 closure lineage C1 — human authorization

- Date: 2026-08-11
- Status: planning authorized; product implementation not yet authorized
- Prior terminal review:
  `cross-layer-review-r9-5eb43ca9-needs-human.md`
- New lineage revision: 1
- Semantic plan digest:
  `afad182665188ec1e7e9e5c1f2192851ddaa5a903d8f5be3d5411e0a54198016`
  over `prd.md`, `design.md`, `node-contracts.md`, `implement.md` and the
  derived execution map in that order

The user explicitly authorized a new planning lineage after the prior R9
lineage exhausted its two-review limit. The authorized scope is limited to:

1. close the undefined `FieldDeclarationDraft` and verifier argument-strategy
   subtypes and name every current framework owner, including one
   Controller-owned Package/ReleaseKernel decision;
2. specify an executable frozen/offline/wheel-only candidate installation
   contract and hostile-dependency regressions;
3. add a real Codex Runtime-Skill discovery proof while retaining the existing
   thin `openai-codex` Python SDK adapter;
4. remove the stale review-request digest and submit one exact fresh semantic
   digest to an independent read-only critic.

The user additionally required that Agent implementation not grow a custom
permission-management system. The allowed Agent boundary is the existing
three-field route, one ephemeral `AsyncCodex` session, constant full access,
one temporary `CODEX_HOME`, one mounted Runtime Skill and existing invocation
result/error contracts. Profile/capability/permission matrices, configurable
sandbox layers, hooks/MCP inheritance, SDK worker protocols and plugin/profile
DSLs are outside this lineage.

C1 does not reopen the two-graph architecture, Judge/Router split, Finding-only
current slice, Expand/Observe/Training seams or child-task boundaries. A fresh
independent `allow` on the exact new digest and a later explicit user approval
of the final planning summary are still required before `task.py start` or any
product-code edit.

# Real proof — sparse WorldArchitecture SourceDraft

## Result

- Run: `run_b934bc068ebc4dd99c711fed6df6f75c`
- Node: `design/world_architecture`
- Execution: `direct_llm`
- Route/model: configured primary `gpt-5.6-luna`
- Work status: `passed`
- Invocation count: 1
- Correction: none
- Usage: 688 input, 2,352 output, 3,040 total tokens
- Output: 4 entities, 5 tools, 26 entity fields

The compiled Artifact contains the existing one-based tool `actor_indexes` and
contains no `actor_names`. Its WorkRecord has one passed attempt, one real Luna
operation record, no Finding, and one committed `design.world_architecture`
output. Observe reports no findings and `release=not_published`.

## Alignment and non-claims

This proves the repaired sparse SourceDraft crossed the real Luna boundary,
compiled, persisted and became the existing downstream WorldArchitecture
contract without a correction. It supports the product path from natural
language to an executable, independently verified, publishable environment,
but proves only this node. The partial proof run intentionally remains
`running`; it does not claim Research, remaining Design nodes, Agent SDK,
Candidate, Integration, Judge, Registry, Expand, Consumer or E2E success.

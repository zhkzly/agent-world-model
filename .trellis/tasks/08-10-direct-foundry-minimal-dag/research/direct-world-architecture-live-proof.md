# Real proof — WorldArchitecture Direct node

- Date: 2026-08-11
- Run: `run_037cc42d277a45e5acd6bb80aabf1e32`
- Model: `gpt-5.6-luna`
- Node: DesignGraph `world_architecture` (`direct_llm`)

## Result

The frozen natural-language need and constructed evidence input produced one
framework-compiled `design.world_architecture` Artifact with two tools. The
WorkRecord is `passed`, binds the exact request/evidence dependencies, and
retains canonical input/output/total usage evidence. Observe reports no
Finding and `release=not_published`.

This proves the current Direct backend, exact model-facing contract,
framework compiler, Artifact commit and WorkRecord closure for this one node.
It does not prove later Design nodes, a complete DesignContract, any Agent,
Candidate, Integration, Judge, Registry package, Repair, Expand, Consumer, or
E2E completion.

## Next proof

Run the already-planned real Codex Agent SDK preflight with one isolated
workspace and exactly one mounted Runtime Skill. Do not reuse this partial run
as an E2E run or release input.

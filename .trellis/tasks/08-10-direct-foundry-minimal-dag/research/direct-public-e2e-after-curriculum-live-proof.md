# Live proof — fresh public Direct E2E after Curriculum repair

- Date: 2026-08-12
- Status: failed safely
- Public route: `agent-world generate`
- Need: a fresh natural-language community equipment-lending workflow
- Direct primary/fallback: `gpt-5.6-luna` / `gpt-5.3-codex-spark`
- Agent primary/fallback: `gpt-5.6-luna` / `gpt-5.3-codex-spark`

## Falsifiable claim

The frozen Curriculum predecessor now commits under the current cleanroom
implementation. A fresh natural-language request must traverse the real public
DesignGraph and CandidateGraph and either publish one independently judged
Registry `EnvironmentPackage`, or stop honestly at the first new terminal with
no downstream release. Research tools, Direct LLM calls, single-Skill Codex
Agent calls, candidate processes, independent framework Judge and Registry
authority must retain their declared boundaries.

Passing establishes one real Direct E2E example only. It does not prove
bounded Repair, Expand/multi-parent evolution, Consumer/SFT/RL, or universal
generation success.

## Result

Public run `run_804e6cc894674e69b7ea72d0714c8daa` stopped safely at
`design/tool_semantics[manage_equipment]` with
`direct_response_not_json`; Registry remained `not_published`.

Before that terminal it committed ResearchPlan, ResearchAcquisition,
EvidenceGraph, WorldArchitecture, SharedToolSemantics and
`tool_semantics[register_member]`. The failing shard made two Luna calls. Both
finished with measured usage but were safely classified as `outer_content`:
one JSON answer had non-JSON leading/trailing content or extra JSON data. The
second logical call received the original task, its immediately preceding
ephemeral answer and the existing next-user format Feedback, but failed the
same condition. Framework correctly denied a third format call, committed a
failed WorkRecord and blocking Finding, and did not enter WorldRules,
Curriculum, Candidate, Judge or Registry.

This disproves the fresh Direct E2E claim. It does not invalidate the prior
Curriculum leaf; it exposes the next causal boundary in the format Feedback
action for another ToolSemantics shard.

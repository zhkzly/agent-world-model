# Product Alignment Checkpoints

## PAC-01 — Direct work graph / future Campaign boundary (entry)

1. **Checkpoint and boundary:** Entry checkpoint for the Direct work-graph
   refactor and the future Evolve Campaign seam.
2. **North-star link:** The changed control representation must continue to
   move a natural-language EnvironmentRequest through evidence, design,
   candidate code, isolated execution, independent Judge and Registry into a
   publishable EnvironmentPackage; Observe only exposes safe durable facts.
3. **Trust owner and Artifact effect:** Controller owns Direct run/work events;
   Designer owns research/design Artifacts; Builder owns candidate and
   Integration Artifacts; Judge owns JudgeReport; Registry owns package and
   receipt. The task adds no new model release or gate authority.
4. **No-hybrid assertion:** The new normal path begins from the cleanroom
   branch and must have no legacy graph/control/expansion import or call. This
   is not yet proven; no Direct/Evolve completion is claimed at entry.
5. **Evidence:** Read-only audit of `foundry-direct-rewrite@9562c05`,
   `docs/agent-world-environment-generation.zh.md`,
   `docs/direct-rewrite-execution-map.zh.md`,
   `docs/plans/stategraph-rewrite.md`, and
   `docs/observe-expand-handoff.zh.md`. The prior Direct E2E is baseline
   evidence only, not evidence for this branch.
6. **Non-claims and remaining risk:** No refactor, Direct graph E2E, work-aware
   Observe proof, Evolve Campaign, repair/resume or Campaign Observe exists yet.
7. **Bad-case effect:** Targets historical duplicate-control-plane and
   node-type conflation cases; rejects the shortcut “Expand is merely a seed
   node.”
8. **Next permitted gate:** Independent cross-layer review of the final plan.

## PAC-02 — Build/Judge work-boundary correction (planning)

1. **Checkpoint and boundary:** Planning correction for Builder and Judge
   boundaries in the explicit Direct work graph.
2. **North-star link:** Build must turn frozen Design and advisory input into a
   candidate that Integration and an independent Judge can really consume on
   the path to a Registry EnvironmentPackage.
3. **Trust owner and Artifact effect:** Builder owns `build` and the
   framework-created CandidateManifest; CandidateBuild is its single Agent
   proposal operation. Judge owns `judge` and JudgeReport.
4. **No-hybrid assertion:** This corrects planning only; no code/import/runtime
   path has changed and no graph/E2E claim is made.
5. **Evidence:** User review caught that treating CandidateBuild as the
   graph-level Build work obscures the Builder validation boundary.
6. **Non-claims and remaining risk:** No implementation/proof exists; the
   separate Evolve Campaign remains deferred.
7. **Bad-case effect:** Prevents an Agent turn appearing to own manifest, gate
   or release facts and preserves Judge as independent framework work.
8. **Next permitted gate:** Cross-layer review after the scope decision.

## PAC-03 — documentation-grounded environment evolution correction (planning)

1. **Checkpoint and boundary:** The user corrected the intended meaning of
   Expand/Evolve before implementation.
2. **North-star link:** The product must grow a diverse set of independently
   verified Agent environments. Technical documents expand the reachable tool
   and workflow universe; released environments provide proven semantic anchors
   from which new complete environments can be evolved.
3. **Trust owner and Artifact effect:** ExpansionSource may create only real
   evidence-backed clues; Policy may select only; Operator may propose a typed
   SemanticDelta; Designer reconstructs complete contracts; Builder/Judge/
   Registry remain the sole route to a released child. Parent release and raw
   docs never become child release facts.
4. **No-hybrid assertion:** No Campaign code, source patching, parent inheritance
   or generic graph platform is introduced by this planning correction.
5. **Evidence:** The source document sections 2.1, 2.2 and 15.2 explicitly
   define the frozen Pool/Inbox -> Source -> Policy -> Operator -> SemanticDelta
   -> shared trusted path. The user clarified that this library-evolution goal,
   not a one-shot retrieval repair, is the intended product meaning.
6. **Non-claims and remaining risk:** The cleanroom currently has only Direct
   and lineage seams; it has not proved a technical-document source, a policy,
   diversity, or an Evolve Campaign E2E.
7. **Bad-case effect:** Prevents both “one fixed environment is enough” and
   “reuse parent source / inherited pass equals evolution.” It keeps the
   current task minimal by deferring the first real Campaign to a separately
   verifiable scope unless the user explicitly expands this task.
8. **Next permitted gate:** Resolve the current-task versus next-child Campaign
   scope, then re-run the independent cross-layer critic on the final plan.

## PAC-04 — reusable candidate-core correction (planning)

1. **Checkpoint and boundary:** The Direct graph was refined after inspecting
   the verified cleanroom implementation and the user asked how it enables
   Evolve.
2. **North-star link:** Direct and Evolve must both reach the same independently
   verified Build -> Integration -> Judge -> Registry path, while retaining
   their different evidence-preparation responsibilities.
3. **Trust owner and Artifact effect:** Direct preparation produces a compiled
   Design from a request and research. Future Evolve preparation produces a
   compiled Design from frozen parents, document evidence and a SemanticDelta.
   The shared candidate core retains Builder, Judge and Registry authority;
   neither preparation path receives release authority.
4. **No-hybrid assertion:** The planned work map is a static causal description
   plus fixed direct code. It does not add a generic executor, scheduler,
   callback system, public Evolve API, or source-code mutation path.
5. **Evidence:** At `foundry-direct-rewrite@9562c05`, `DirectFoundry.generate`
   currently performs one linear flow and the package manifest already has
   `origin=direct` plus `parent_package_refs=[]`. The remaining question is
   whether the package re-read validates this public lineage shape explicitly;
   it is not a reason to rewrite the proven Direct foundation.
6. **Non-claims and remaining risk:** No candidate-core extraction, Direct
   work-map proof or Evolve Campaign exists yet. A static graph record alone
   will not prove Evolve.
7. **Bad-case effect:** Prevents a future Campaign from duplicating or bypassing
   Build/Judge/Registry, and prevents the inverse over-design of a generic graph
   platform merely to express two known flows.
8. **Next permitted gate:** Independent cross-layer critic review of this final
   selected Direct-graph plan before product-code work.

## PAC-06 — baseline-lineage evidence correction (planning)

1. **Checkpoint and boundary:** The main planner rechecked the actual cleanroom
   manifest after identifying a contradiction in the prior planning claim.
2. **North-star link:** Product progress depends on preserving verified Direct
   behavior and using actual evidence, rather than manufacturing work from a
   mistaken plan premise.
3. **Trust owner and Artifact effect:** `DirectFoundry._release` already writes
   framework-owned `origin=direct` and `parent_package_refs=[]`. No Agent,
   candidate, Judge, Registry or Observe authority changes from this correction.
4. **No-hybrid assertion:** This is a planning-fact correction only. It does not
   alter the cleanroom or reintroduce legacy code.
5. **Evidence:** `git show 9562c05:agent_world/foundry.py`, lines 678–685,
   confirms the two fields. The earlier task draft incorrectly described the
   parent list as absent and has been corrected before implementation.
6. **Non-claims and remaining risk:** The current package re-read compares the
   stored manifest with the framework-built manifest; the independent audit must
   decide whether a small explicit shape regression/validation is needed for
   future parent selection. No Expand claim follows.
7. **Bad-case effect:** Prevents redundant code changes and preserves the rule
   that a plan never overrides inspected product evidence.
8. **Next permitted gate:** Resume the corrected independent audit; only its
   final decision may authorize a final implementation plan.

## PAC-05 — Evolve start/input and Agent-boundary clarification (planning)

1. **Checkpoint and boundary:** The user asked for the exact Evolve input/output
   and whether its start is an Agent node.
2. **North-star link:** A growing environment library requires diverse,
   documentation-grounded proposals without letting an Agent choose unbounded
   parents, evidence, budget, verification or release.
3. **Trust owner and Artifact effect:** Framework owns CampaignRequest,
   CampaignSnapshot, SourceRequest, SourceResult validation, policy checkpoint,
   admission, Operator, formal SemanticDelta and CandidateOutcome. The existing
   Researcher Agent owns only bounded source-clue synthesis; Designer owns only
   semantic source proposals; Builder/Judge/Registry retain their prior roles.
4. **No-hybrid assertion:** This creates no new Agent role, public CLI, policy
   framework or current Campaign runtime. It documents the smallest first-
   Campaign contract for the later child task.
5. **Evidence:** Source-document section 11.1 fixes the frozen inputs -> Source
   -> clue snapshot -> Policy.ask -> MutationIntent -> Operator -> SemanticDelta
   -> Designer -> shared path sequence; section 11.3 fixes CandidateOutcome as
   a multi-objective vector and parent eligibility as released exact manifests.
6. **Non-claims and remaining risk:** No source request, policy, operator,
   campaign snapshot or CandidateOutcome type has been implemented in the
   cleanroom. Exact first-Campaign source/provider and operator selection remain
   user-visible scope choices for its child-task plan.
7. **Bad-case effect:** Prevents treating a free-form Agent prompt as the
   evolution control plane, direct document-to-code generation, self-scored
   release, or inherited parent validity.
8. **Next permitted gate:** User confirms this boundary; then the current
   Direct-graph plan can undergo independent cross-layer review.

## PAC-07 — cleanroom fail-close and persistence correction (planning)

1. **Checkpoint and boundary:** Read-only recheck of the cleanroom Direct
   composition before graph/refactor implementation.
2. **North-star link:** A natural-language request advances toward a publishable
   EnvironmentPackage only while each framework-owned boundary has passed;
   Integration therefore cannot be a merely informational precursor to Judge.
3. **Trust owner and Artifact effect:** Builder owns the framework Integration
   report and its failure transition; Judge owns only an invocation after that
   report passed; Registry stays unreachable on an Integration failure. Direct
   work records are framework facts, while raw acquired documents remain
   ephemeral Agent input.
4. **No-hybrid assertion:** The repair applies only in
   `foundry-direct-graph`; it does not import, adapt, copy or execute the old
   StateGraph/Campaign control plane.
5. **Evidence:** Baseline `DirectFoundry.generate()` records Integration at
   lines 168–170 and immediately calls `_judge()` at line 172 without a
   `passed` guard. Baseline `_release()` already writes `origin=direct` and
   `parent_package_refs=[]` at lines 678–681, and `_verify_package()` re-reads
   the manifest at lines 772–783. This corrected the previous false premise
   that parent refs were absent.
6. **Non-claims and remaining risk:** No product code, graph proof, real E2E,
   Campaign, nonempty lineage, policy or Expand proof exists yet. A static map
   must not be claimed as a working Evolve engine.
7. **Bad-case effect:** Prevents a failed candidate from consuming Judge or
   accidentally reaching any downstream publish path; prevents redundant
   lineage code caused by a planning error; replaces ambiguous stage events
   with one inspectable Direct work view.
8. **Next permitted gate:** Fresh independent read-only cross-layer critic on
   plan revision 5, then final user review before product-code work.

## PAC-08 — Integration-to-release closure correction (planning)

1. **Checkpoint and boundary:** Independent critic rejected plan revision 5 at
   the Builder Integration -> Registry package/release handoff.
2. **North-star link:** A natural-language request can become a publishable
   EnvironmentPackage only when the package binds the actual passed Integration
   evidence for the exact generated Design and Candidate, not merely because
   linear control flow once called Integration.
3. **Trust owner and Artifact effect:** Builder creates the framework-owned
   Integration Artifact bound to Design/Candidate. Registry/release re-reads it
   and binds its digest in the dossier and physical manifest. Judge remains an
   independent later owner; Observe only reads the resulting safe work facts.
4. **No-hybrid assertion:** The correction remains inside the cleanroom's
   fixed Direct composition. It does not import old graph/control/Campaign
   code, introduce a second Registry/package route, or create public Evolve
   types.
5. **Evidence:** The independent read-only critic `trellis-research-r5`
   inspected the pinned cleanroom and returned `block`: current `_release()`
   binds Design/Candidate/Judge but not Integration. Its required smallest
   correction is Integration Artifact identity binding plus release re-read and
   manifest/dossier digest closure.
6. **Non-claims and remaining risk:** No code or live proof exists; this does
   not prove Evolve, Campaigns, repair/resume, Consumer/training or a generic
   graph engine.
7. **Bad-case effect:** Prevents a future refactor from satisfying only the
   local `if integration.passed` control-flow condition while letting a release
   package omit the preceding hard-gate evidence; resolves double ownership of
   `research_acquire` by assigning it to Designer.
8. **Next permitted gate:** Fresh independent read-only cross-layer critic on
   plan revision 6, then final user review before product-code work.

## PAC-09 — revision-6 cross-layer allow (planning)

1. **Checkpoint and boundary:** Fresh independent review of the complete Direct
   work-map, Integration and release-package closure plan.
2. **North-star link:** The allowed slice preserves the full Direct chain from
   a natural-language need through evidence, candidate code, isolated
   Integration/Judge and a Registry EnvironmentPackage, while Observe exposes
   safe facts only.
3. **Trust owner and Artifact effect:** Designer owns research acquisition and
   design; Builder owns CandidateManifest and exact Integration evidence; Judge
   owns independent evidence; Registry re-reads/binds passed Integration before
   publish; Observe reads `works`. No Agent gains a Gate, manifest or release
   authority.
4. **No-hybrid assertion:** The reviewer found the proposed cleanroom slice can
   remain free of legacy control/graph/Campaign imports and does not require a
   generic executor, public lineage API or second release path.
5. **Evidence:** Independent read-only `trellis-research-r6` returned `allow`
   for plan revision 6 after inspecting the pinned cleanroom, canonical product
   document, execution map, current persistence/Observe/release code and prior
   revision-5 `block`. The full decision is in
   `cross-layer-review-r6-9382cd24-48dfd0b1-7c8561ff.md`.
6. **Non-claims and remaining risk:** No implementation or real proof exists
   yet. The allow does not prove Direct E2E, Evolve, Campaign behavior,
   repair/resume, Consumer/training or universal model reliability.
7. **Bad-case effect:** Rejects both the local-only `if integration.passed`
   patch and the opposite overreaction of importing a legacy graph platform;
   it requires physical package evidence closure without expanding the product
   slice.
8. **Next permitted gate:** User reviews and explicitly approves the final
   plan; only then may `task.py start` and implementation begin.

## PAC-10 — system-boundary re-anchor (planning)

1. **Checkpoint and boundary:** Entry checkpoint for the user-requested system
   re-anchor: Direct seed generation, Expand library evolution, and Observe
   explanation are one product architecture; whether either path uses a graph
   representation is an internal implementation decision.
2. **North-star link:** Direct advances `EnvironmentRequest -> evidence/design
   -> executable candidate -> independent Judge -> Registry EnvironmentPackage`.
   Expand must subsequently use released semantic anchors plus permitted
   technical evidence to create another complete Design and traverse that same
   trusted path; Observe exposes only safe durable facts for both paths.
3. **Trust owner and Artifact effect:** Controller/framework owns Direct/Campaign
   entry snapshots, budgets, gates and release routing; Designer owns bounded
   research/design proposals; Builder/Judge/Registry retain their existing
   candidate, independent-verdict and publication authority. This planning
   correction creates no product Artifact or Campaign type. It preserves the
   compiled Design/candidate-core seam as the only required future reuse point.
4. **No-hybrid assertion:** The intended new normal remains cleanroom-only and
   imports no legacy StateGraph, ExpansionCampaignRunner, scheduler, replay or
   compatibility authority. No dormant Expand implementation is added merely to
   name the architecture.
5. **Evidence:** Canonical sections 1–2 define Direct, independent Discovery,
   Evolve and Registry/Consumer roles; the execution map limits the current
   cleanroom to Direct plus minimal Observe while naming compiled Design as the
   future reuse boundary. The task design now makes this division explicit.
6. **Non-claims and remaining risk:** No product code changed. There is no real
   Expand Campaign, campaign snapshot, policy/operator, `observe campaign`, or
   proof that evolving environments improves coverage/diversity. Direct E2E,
   if later passed, remains only the seed proof.
7. **Bad-case effect:** Prevents two opposite failures: treating a static Direct
   graph as the whole product, and importing the old oversized graph/Campaign
   implementation merely because Expand needs a loop.
8. **Next permitted gate:** Fresh independent cross-layer review of plan
   revision 7, then user approval before `task.py start` or any product-code
   change.

## PAC-11 — downstream SFT/RL consumption boundary (planning)

1. **Checkpoint and boundary:** Entry checkpoint for the user clarification
   that released environments will later supply SFT data and online RL episodes.
2. **North-star link:** The generated or evolved result must remain a real,
   parameterized, independently verified `EnvironmentPackage`; training consumes
   exact releases only after Registry publication and cannot become a hidden
   prerequisite for Generation or Evolve.
3. **Trust owner and Artifact effect:** Registry owns exact released package
   identity; a future framework Consumer owns Suite snapshots, task
   materialization, isolated Episode lifecycle and trusted reward/termination.
   SFT/RL adapters consume only the public Episode surface. No product Artifact
   or Runtime code changes at this planning checkpoint.
4. **No-hybrid assertion:** The proposed seam does not restore legacy rollout,
   replay, fixed-task or training code into the cleanroom Foundry path. Training
   models, optimizers and framework-specific adapters stay downstream.
5. **Evidence:** Canonical sections 2.2, 3.4, 7, 8 and 12 define exact package
   snapshots, parameterized materialization, isolated Runtime, trusted evaluator
   and public-only RPC; sections 15.3 and 16 make Consumption optional and forbid
   rewriting training frameworks into Foundry.
6. **Non-claims and remaining risk:** No SuiteSnapshot, Consumer/RPC, SFT
   exporter, RL adapter, real training Episode or training-utility proof exists
   in the current cleanroom slice. The current Direct implementation plan still
   proves only generation/release/Observe.
7. **Bad-case effect:** Prevents fixed-trajectory environments, evaluator/sealed
   leakage into training data, candidate-defined reward, mutable `latest`
   package selection, shared Episode state, and feedback becoming self-validating
   release evidence.
8. **Next permitted gate:** User chooses whether the smallest Consumption proof
   is an immediate child parallel to Expand or is deferred; then revise the full
   plan and obtain a fresh cross-layer review before implementation.

## PAC-12 — multi-parent code and decoupled training correction (planning)

1. **Checkpoint and boundary:** Entry checkpoint for plan revision 8 after the
   user clarified that “multiple parents” means reusable code from multiple
   exact released environments, not multiple policy implementations, and that
   training must be a separately removable downstream subsystem.
2. **North-star link:** Direct still turns one natural-language need into an
   independently verified package. Expand may use several verified packages as
   semantic and implementation anchors, but it must produce one complete new
   Design and one fresh self-contained candidate that traverses Integration,
   Judge and Registry again. Training consumes only those exact releases.
3. **Trust owner and Artifact effect:** Policy selects exact parent refs and a
   bounded semantic intent; Designer/compiler owns the complete child Design
   and authoritative SemanticDelta. Only Builder, after Design commit, may
   mount independently verified parent source closures read-only for its one
   CandidateBuild Agent. Builder rescans the final source closure and records
   separate `ImplementationLineage`; Registry owns the released child package.
   A future Consumer, not Foundry or candidate Runtime, owns Suite snapshots,
   Episode lifecycle and reward/termination.
4. **No-hybrid assertion:** No legacy merge engine, source-patching success
   route, parent runtime dependency, training framework, old replay ABI or
   generic graph executor enters the cleanroom. The child packages every byte
   it needs and cannot import a mutable parent at runtime.
5. **Evidence:** Canonical sections 7–8 require a separate Task Materializer and
   five-operation Runtime; sections 11.1–11.3 define plural-parent policy and
   semantic operators; sections 12.1–12.4 separate semantic/implementation
   lineage, immutable packages and framework Consumer; sections 15.2–16 prove
   Evolve without training and forbid embedding a trainer in Foundry.
6. **Non-claims and remaining risk:** No multi-parent Campaign, parent-source
   mount, Consumer, SFT export or RL episode has been implemented. The first
   live Expand proof remains one-parent to bound risk; plural-parent contract
   and read-only source-mount tests do not prove useful multi-parent synthesis.
7. **Bad-case effect:** Prevents Policy from becoming a code-merger, code reuse
   from masquerading as semantic evolution, inherited parent verdicts, mutable
   runtime parent imports, evaluator leakage into training, and a Foundry that
   stops working when training dependencies are removed.
8. **Next permitted gate:** Rewrite design/implementation revision 8 around the
   canonical Direct package contract, multi-parent Builder boundary and
   separate Consumption child; then obtain a fresh independent cross-layer
   `allow` before implementation.

## PAC-13 — difficulty producer/consumer closure (planning)

1. **Checkpoint and boundary:** C2 planning revision after the fresh Direct
   critic found that downstream Materializer/Judge/package/Consumer expected a
   closed difficulty mapping but no node produced finite levels.
2. **North-star link:** A natural-language need can become a genuinely
   parameterized executable EnvironmentPackage only when task difficulty is
   part of the independently validated task contract, not a candidate label or
   fixed test fixture.
3. **Trust owner and Artifact effect:** Curriculum Direct LLM proposes bounded
   dimension/level meanings; Designer framework compiles one per-family
   `DifficultySchema`; TaskRequirement, CandidateBuild contract, Integration,
   Judge, package and Consumer consume it. Candidate code only exact-echoes an
   admitted selection.
4. **No-hybrid assertion:** This adds no node, graph, schema service, router,
   scheduler or compatibility path. The dirty legacy root remains excluded.
5. **Evidence:** Canonical Task Materialization v3 requires difficulty
   dimensions and levels and exact matching. The first fresh child review
   `cross-layer-review-baddd746.md` traced the missing producer through every
   immediate and future consumer. A separate Terra baseline audit found no
   other deterministic plan/code conflict.
6. **Non-claims and remaining risk:** Only planning documents changed. No
   compiler, candidate, Direct proof, Expand child or Consumer Episode exists
   yet; graph/test progress is not product completion.
7. **Bad-case effect:** Rejects free-form dicts, fixed example difficulties,
   candidate-defined levels, partial/defaulted selections and a separate
   Consumer schema.
8. **Next permitted gate:** Recompute the 16-file parent and five-file Direct
   digests, obtain fresh independent Terra `allow` records for both, then and
   only then dispatch implementation.

## PAC-14 — R9-C2 Direct graph implementation (entry)

1. **Checkpoint and boundary:** Entry to the reviewed DesignGraph and CandidateGraph implementation.
2. **North-star link:** This work makes the Direct path explicit from a natural-language request through evidence/design, an isolated Materializer and Runtime, independent Judge evidence, Registry publication, and safe Observe.
3. **Trust owner and Artifact effect:** Designer commits DesignGraph artifacts; Builder owns BuildPlan, Candidate and Integration; Judge owns evidence-only JudgeReport/Findings; Controller is the only Package/ReleaseKernel owner; Registry only cold-reads and publishes. Work records and envelopes preserve exact producer and dependency facts.
4. **No-hybrid assertion:** The cleanroom path will use only its two static graphs and does not import legacy StateGraph, Campaign, scheduler, replay, or compatibility authority.
5. **Evidence:** Parent allow `734a274a6b3092f0b526530fd264d105dd65bf068b1aee74fe67984219d7f117` and Direct allow `ca1c588d4e9e970431aa002832207709b55f35a085b3ed76ebf045816dcbe6db` were re-read before editing. Deterministic checks remain pending.
6. **Non-claims and remaining risk:** No live model, research, Agent SDK preflight, isolated candidate E2E, Judge, Registry release, Expand, repair, or Consumer proof has occurred.
7. **Bad-case effect:** The implementation must stop a failed Integration before Judge/Package/Registry and prevent verifier material from reaching CandidateBuild.
8. **Next permitted gate:** Implement the two fixed graph families and then run deterministic graph, runtime, package, and firewall checks.

## PAC-15 — R9-C2 DesignGraph/CandidateGraph boundaries (exit)

1. **Checkpoint and boundary:** Exit checkpoint for the deterministic Direct DesignGraph and CandidateGraph implementation.
2. **North-star link:** The cleanroom now records the reviewed Direct handoffs from request/evidence and compiled Design through Candidate, isolated Integration, Judge, framework package, Registry, and safe Observe. This advances the natural-language-to-publishable-package path but does not itself produce one.
3. **Trust owner and Artifact effect:** `NodeSpec`/`EdgeSpec` declare the closed Design and Candidate owners/kinds; framework envelopes and WorkRecords bind producer and direct inputs. Builder Integration is a candidate-process boundary and records a failed WorkRecord before the controller terminates. Judge remains downstream of passed Integration plus verifier intent; Controller alone packages and Registry cold-reads/publishes.
4. **No-hybrid assertion:** `agent_world` has no import of retired StateGraph, Campaign, scheduler, replay, old ABI, or compatibility authority. The legacy firewall passes with the new lightweight `graph.py` explicitly permitted.
5. **Evidence:** Deterministic: `uv run pytest` passed 37 tests; `ruff format --check`, `ruff check`, `mypy agent_world`, `compileall`, and the standalone legacy firewall all passed. The failure regression confirms Integration leaves verifier/Judge/package/Registry unreachable and Observe projects the failed candidate work. No live E2E evidence was created.
6. **Non-claims and remaining risk:** No real Direct LLM, Researcher/search/fetch/extract, Codex SDK singleton-Skill preflight, CandidateBuild, offline `uv` installation, independent Judge, Registry release, or cold-directory package proof has run. Expand, repair, Consumer/SFT/RL, multi-parent lineage, and real package diversity remain unimplemented downstream work.
7. **Bad-case effect:** Detects and honestly stops failed Integration before Judge/release; removes verifier files from CandidateBuild input; rejects invalid ordered difficulty selections, four-operation Runtime responses, untrusted dependency sources, and retired two-file package shape.
8. **Next permitted gate:** Main session obtains the separate check review and then runs the smallest true-boundary proof sequence from the reviewed plan, reading Observe after each real terminal.

## PAC-16 — R9-C2 implementation checkpoint correction

1. **Checkpoint and boundary:** Terra's implementation checkpoint after the
   first deterministic pass, before final check or any live proof.
2. **North-star link:** The changes advance the path from natural-language need
   toward an executable, independently verified, publishable EnvironmentPackage,
   but graph declarations currently wrap a concentrated orchestration rather
   than owning every node transaction.
3. **Trust owner and Artifact effect:** The retained graph, Runtime, Skill,
   difficulty and supply-chain primitives are framework groundwork. The actual
   Design-node proposals, committed handoff refs, passed Integration closure,
   package/Registry cold-read and final WorkRecord chain remain incomplete.
4. **No-hybrid assertion:** No live success or old compatibility path is being
   claimed; the partial implementation must be completed in the cleanroom
   worktree before downstream children are considered.
5. **Evidence:** Terra reported 37 deterministic tests and static checks green,
   while explicitly reporting no live proof and identifying the remaining
   monolith and closure gaps. The independent checkpoint audit records the same
   findings.
6. **Non-claims and remaining risk:** Direct E2E, Agent Skill preflight,
   CandidateBuild, offline install, independent Judge, Registry cold-read,
   release and all Repair/Expand/Consumer behavior remain unproven.
7. **Next permitted gate:** Complete only the approved Direct follow-up slices,
   then dispatch a fresh check and run true-boundary proofs. A green graph/test
   checkpoint is not child completion or product completion.

## PAC-17 — trusted-wheel ingestion plan correction

1. **Checkpoint and boundary:** C3 planning entry before changing the
   framework-owned candidate installer.
2. **North-star link:** A generated environment is executable and publishable
   only if its locked dependencies can be installed without network, build
   hooks or candidate-controlled sources; rejecting every dependency would not
   satisfy arbitrary environment generation.
3. **Trust owner and Artifact effect:** Framework remains the sole dependency
   admission owner. It verifies lock hashes/sizes, copies admitted wheels into
   one run-local flat directory, and passes that directory to fixed uv argv.
   CandidateBuild, candidate Runtime, Judge, Registry and future children gain
   no dependency-selection authority.
4. **No-hybrid assertion:** No legacy installer, package downloader, generic
   configuration layer or direct uv-cache manipulation enters the cleanroom.
5. **Evidence:** Official uv CLI documentation defines `--find-links` for a
   local directory of distributions and `--no-index` for excluding registries;
   uv cache documentation says direct cache modification is unsafe. The Terra
   implementation worker independently found the frozen C2 argv could not
   consume the promised trusted store and made no code change.
6. **Non-claims and remaining risk:** This is a plan correction only. No wheel
   has installed, no candidate has executed, and no Direct/Expand/Consumer
   outcome is proven.
7. **Next permitted gate:** Obtain fresh independent Terra `allow` records for
   Direct digest `dec00ffe...` and parent digest `d39632e8...`, add them to
   implementation/check context, then implement and prove the valid-wheel and
   hostile-input cases.

## PAC-18 — tested uv installation contract correction

1. **Checkpoint and boundary:** C4 planning entry after the first real local
   valid-wheel deterministic case exercised the exact C3 argv.
2. **North-star link:** Arbitrary generated environments require trusted
   third-party dependencies to install from admitted bytes; an argv that can
   only reject them cannot produce the executable EnvironmentPackage target.
3. **Trust owner and Artifact effect:** Framework still canonical-parses the
   candidate lock, selects the complete transitive closure, verifies wheel
   hash/size, and compiles a temporary exact-version/hash requirements file.
   Fixed `uv venv` and `uv pip sync` consume only that file and the verified
   flat directory. CandidateBuild supplies no installer input after preflight.
4. **No-hybrid assertion:** No downloader, index client, resolver service,
   cache manipulation, candidate project install, legacy path or configuration
   framework is introduced.
5. **Evidence:** Tested `uv 0.11.29` rejected `--frozen --no-sources`; removing
   only `--no-sources` still followed the lock's remote artifact URL. A local
   `uv pip sync --require-hashes --offline --no-index --find-links --no-build`
   probe installed the same verified wheel into a fresh venv without network.
   Official uv documentation defines `--no-sources` as resolution behavior and
   `--require-hashes` as exact pinned hash-checking mode.
6. **Non-claims:** This proves installer command feasibility only. Candidate
   execution, Judge, Registry, Direct E2E and later children remain unproven.
7. **Next permitted gate:** Recompute C4 Direct/parent digests, obtain fresh
   scope-complete Terra allows, then resume all Direct implementation slices.

## PAC-19 — finite install closure and project isolation

1. **Checkpoint and boundary:** C5 plan revision after the scope-complete C4
   critic blocked the exact installer transaction.
2. **North-star link:** Both stdlib-only and trusted-wheel environments must
   reach isolated execution without exposing candidate project/config inputs or
   weakening exact dependency evidence.
3. **Trust owner and Artifact effect:** Framework commits one finite
   `AdmittedLockClosure`, compiles normalized pinned/hash requirements, and
   requires exact installed `(name, version)` set equality. Ambiguous markers,
   extras, forks or multiple versions fail before uv rather than invoking a new
   framework resolver.
4. **No-hybrid assertion:** Both uv commands run outside candidate source;
   `--no-project`, empty framework config and scrubbed environment exclude
   candidate discovery. No candidate-specific skip, downloader, cache editing,
   resolver service or legacy path is added.
5. **Evidence:** Local `uv 0.11.29` help requires
   `--allow-empty-requirements` for an empty sync and exposes
   `uv venv --no-project`. Real-wheel and empty-closure probes both pass with
   the revised command shape. C4 block record lists the exact corrected facts.
6. **Non-claims:** Planning/probe evidence does not prove CandidateBuild,
   Integration, Judge, Registry, Direct E2E or later children.
7. **Next permitted gate:** Obtain the final fresh full-scope Terra allow for
   Direct `37988f70...` and parent `3c9098e3...`, then resume implementation.

## PAC-20 — DesignGraph exit and CandidateGraph integration entry

1. **Checkpoint and boundary:** Exit from the independently implemented
   DesignGraph/graph transaction, C5 installer/Runtime and Codex adapter slices;
   entry to the single CandidateGraph/Package/Registry integration slice.
2. **North-star link:** The canonical goal is still an arbitrary natural-
   language need becoming an evidence-grounded executable environment,
   independently verified in real isolation and atomically published as an
   immutable EnvironmentPackage with safe Observe—not merely passing graph
   declarations or focused tests.
3. **Trust owner and Artifact effect:** GraphRunner now owns proposal,
   framework compilation and immutable Work/Artifact commit; Builder owns
   candidate scan and Integration; Judge must consume passed Integration plus
   the sibling VerifierBundle; Controller alone packages; Registry alone
   cold-reads/publishes. The integration agent must connect these exact owners
   without restoring `graph.commit` or a monolithic bypass.
4. **No-hybrid assertion:** Focused slices contain no legacy StateGraph,
   replay, old ABI, compatibility route or generic scheduler. CandidateGraph
   integration must delete the fixed `_compile_design` test seam and stale
   node-local patches rather than preserve them beside the new path.
5. **Evidence:** Agent adapter tests: 17 passed; graph/Design tests: 3 passed;
   supply-chain/Runtime tests: 23 passed. Repository Ruff and compileall pass.
   Full mypy now reports only the expected stale CandidateGraph calls to the
   removed `GraphRunner.commit`; full pytest reaches only the retired Direct
   release monkeypatch test after 24 passes.
6. **Non-claims and remaining risk:** No VerifierBundle has been executed by
   Judge, no exact package closure has passed Registry cold-read, and no live
   LLM/Agent/Candidate/E2E proof exists. Repair, Expand and Consumer remain
   later children.
7. **Next permitted gate:** One Terra integration worker replaces Candidate,
   Judge, Package/Registry, Foundry composition and Observe/test seams, then a
   separate Terra check reviews the whole Direct diff before live proofs.

## PAC-21 — Direct deterministic implementation exit and proof entry

1. **Checkpoint and boundary:** Exit from the complete deterministic
   DesignGraph/CandidateGraph implementation and entry to independent Direct
   review, followed by the ordered real-execution proofs.
2. **North-star link:** The target remains an arbitrary natural-language need
   becoming an evidence-grounded executable environment, independently
   exercised and judged, atomically published as an immutable
   `EnvironmentPackageRef`, and safely inspectable through Observe.
3. **Trust owner and Artifact effect:** Direct LLM and singleton-Skill Agent
   nodes only propose bounded drafts; framework compilers and `GraphRunner`
   commit immutable Artifacts/WorkRecords; candidate processes only execute;
   Judge records evidence without routing; Controller alone packages and
   Registry alone cold-reads and publishes. The released ref binds exact
   Design, Candidate, passed Integration/Judge, receipt and separate semantic
   and implementation lineage.
4. **No-hybrid assertion:** The implemented slice contains exactly the two
   fixed domain graphs and no legacy `awm`, StateGraph, replay, ABI-v1,
   compatibility path, generic scheduler/plugin runtime, or second release
   owner. The dead fixed-design seam was deleted rather than preserved.
5. **Evidence:** The main session reproduced `56 passed`; Ruff format/check,
   mypy for `agent_world`, compileall and `git diff --check` all pass. Focused
   tests cover singleton Skills, graph transactions, C5 offline installation,
   five-operation Runtime, verifier separation, fail-stop, Registry cold-read,
   released-ref lineage and safe failed/released Observe scenes.
6. **Non-claims and remaining risk:** No real Direct LLM, Codex SDK Agent,
   generated candidate, isolated Integration/Judge or fresh request-to-Registry
   E2E has run. Passing tests and graph completion are not product completion;
   Repair, Expand/multi-parent and Consumer/SFT/RL remain unimplemented.
7. **Next permitted gate:** Dispatch an independent Terra `trellis-check` over
   the whole Direct diff. Only after it passes may the four ordered real proofs
   begin; every real terminal is followed by Observe, and any failure enters
   the required diagnosis/critic flow before a semantic repair.

## PAC-22 — independent Direct block and C6 plan entry

1. **Checkpoint and boundary:** The first independent whole-Direct check
   returned `block`; no real provider proof started. This checkpoint enters the
   R9-C6 plan/critic boundary rather than patching the green test suite.
2. **North-star link:** Natural language must become a genuinely executable,
   independently challenged and cold-read publishable EnvironmentPackage.
   Declared edges, a code generator missing its process protocol, repeated
   public Judge cases, hard-coded telemetry or an incomplete package cannot
   establish that product result.
3. **Trust owner and Artifact effect:** C6 keeps framework authority over port
   validation, correction packets, sealed-case compilation, operation evidence,
   dependency admission, package metadata and Registry verification. Agents
   still only propose semantic/build drafts and candidate processes still own
   no commit, Judge, route or release fact.
4. **No-hybrid assertion:** The correction plan edits the two existing graph
   executors and their contracts in place. It adds no legacy module, third
   graph, scheduler, generic verifier language, permission/profile framework,
   alternate Registry or later-child success path.
5. **Evidence:** The independent Terra reviewer reproduced both C5 plan
   digests, `56 passed`, the focused hostile/tamper checks and all static gates,
   then demonstrated that `candidate_build` could commit without its declared
   BuildPlan edge and identified six other trust-boundary mismatches. The main
   session separately found the hard-coded telemetry contradiction.
6. **Non-claims:** C6 is only a revised plan. No corrected graph binding,
   private verifier execution, real telemetry, complete SBOM/package closure,
   candidate isolation or live Direct result is yet proven. Repair, Expand and
   Consumer remain later children; graph/test progress remains non-completion.
7. **Next permitted gate:** Recompute exact Direct/parent C6 identities and
   obtain fresh independent Terra cross-layer `allow` records. Only then may
   bounded implementation resume, followed by a new whole-diff check and the
   ordered live proofs.

## PAC-23 — C6 deterministic closure exit and independent-check entry

1. **Checkpoint and boundary:** The allowed C6 implementation slices are
   complete and this checkpoint exits deterministic implementation into a
   fresh, independent whole-Direct check. No live provider proof has started.
2. **North-star link:** The goal remains an arbitrary natural-language need
   becoming an evidence-grounded executable environment, independently
   exercised and judged, atomically published as an immutable
   `EnvironmentPackageRef`, and safely cold-read through Observe.
3. **Trust owner and Artifact effect:** Named graph ports now bind exact
   upstream Artifacts; framework validation owns the one bounded correction,
   executable private verifier cases, dependency admission, evidence-derived
   telemetry, SBOM and package metadata; candidate processes cannot commit;
   Judge cannot route or release; Registry alone cold-reads and publishes; and
   Observe independently rechecks the released closure.
4. **Minimality assertion:** C6 modified the two existing graphs and release
   boundary only. It introduced no third graph, generic workflow engine,
   compatibility route, permission/configuration framework, automatic repair,
   Expand or training path. The independent check must explicitly identify
   removable duplication or future-only abstraction before live proof.
5. **Evidence:** The main session reproduced `70 passed`; Ruff format/check,
   mypy for `agent_world`, compileall and `git diff --check` all pass. Focused
   release/Observe tests are `16 passed`; the implementation worker reports
   `32 passed` across its package, Observe and supply-chain slice. Tamper tests
   cover package inventory, canonical metadata, verifier commitment refs,
   lineage, admitted dependency closure and Registry/Observe cold reads.
6. **Non-claims and remaining risk:** These deterministic results do not prove
   provider reachability, real Direct output quality, the Codex SDK Skill
   boundary, generated-candidate installability, isolated Integration/Judge or
   a fresh need-to-Registry E2E. Repair, Expand/multi-parent and Consumer/SFT/RL
   remain later children; graph/test completion is not product completion.
7. **Next permitted gate:** Dispatch one fresh read-only Terra `trellis-check`
   over the complete C6 diff with explicit cross-layer and code-size review.
   Only an allowing result permits the ordered real proofs; any semantic block
   returns to a bounded plan revision, not an ad-hoc patch.

## PAC-24 — C6 check block and minimal C7 plan entry

1. **Checkpoint and boundary:** The fresh C6 whole-diff check returned `block`
   before any provider call. C7 re-enters planning for only exact model-output
   correction delivery and the five existing Runtime response envelopes.
2. **North-star link:** A natural-language need cannot honestly become an
   executable, independently verified package if Builder receives an open ABI
   or candidate responses can smuggle undeclared fields. The correction packet
   exists only to make one pre-commit semantic retry causal; it is not Repair.
3. **Trust owner and Artifact effect:** Framework compilers alone construct an
   exact safe packet and Runtime validators alone accept closed candidate
   responses. Models still only replace the same proposal, candidate processes
   still cannot commit, and Judge/Registry authority is unchanged.
4. **Minimality assertion:** C7 reuses `CorrectionPacket`, `GraphRunner`, the
   existing Agent adapter and five Runtime operations. It forbids issue
   aggregation, a schema engine, retry manager, new graph/node, permissions,
   compatibility, Repair, Expand and Consumer code.
5. **Evidence:** The independent Terra check reproduced both C6 digests and all
   `70 passed` plus Ruff, mypy, compileall, diff and legacy-firewall gates. It
   found no future-only abstraction or duplicate release authority; its two
   blocking findings are recorded in `direct-c6-whole-diff-check-block.md`.
6. **Non-claims:** C7 is a plan only. Exact packets and response rejection are
   not implemented, and no live Direct, Agent, Candidate, Integration, Judge,
   Registry or E2E proof has run. Later children remain unimplemented.
7. **Next permitted gate:** Compute fresh Direct/parent C7 plan identities and
   obtain one independent Terra cross-layer `allow`. Only then may the bounded
   C7 implementation and a fresh whole-diff check run.

## PAC-25 — C7 deterministic exit and final Direct-check entry

1. **Checkpoint and boundary:** The exact-digest C7 implementation is complete
   and exits deterministic work into a fresh whole-Direct check. No real
   provider, candidate or E2E proof has started.
2. **North-star link:** The arbitrary need-to-executable-package path now gives
   Builder the same closed five-response Runtime ABI that Integration/Judge
   enforce, and one pre-commit model correction receives a causal exact issue.
   These are prerequisites, not proof of an actual generated environment.
3. **Trust owner and Artifact effect:** Framework compilers alone construct a
   safe packet; `GraphRunner` alone permits one second invocation and records
   attempts. Runtime supervisors alone validate untrusted envelopes. Candidate,
   Judge, Controller and Registry authorities remain unchanged.
4. **Minimality assertion:** C7 reused existing helpers and four modules, added
   no module, graph, node, schema/feedback/retry system or later-child path. An
   unexpected compiler exception without a safe exact issue is terminal rather
   than converted into generic model feedback.
5. **Evidence:** The main session reproduced `88 passed`; Ruff format/check,
   mypy for `agent_world`, compileall and `git diff --check` pass. Regressions
   cover causal Design/BuildPlan/VerifierIntent/CandidateCompletion correction,
   non-correctable framework defects, exact five-operation responses and a
   conforming Integration/Judge path.
6. **Non-claims:** No real Direct LLM, Codex Skill, CandidateBuild, offline
   installation, Integration/Judge, Registry or fresh request E2E has run.
   Repair, Expand/multi-parent and Consumer/SFT/RL remain later children.
7. **Next permitted gate:** One fresh independent Terra `trellis-check` audits
   the whole diff against the C6 block, C7 allow, minimality and downstream
   contracts. Only `allow` permits the ordered real proofs.

## PAC-26 — C7 check block and minimal C8 provenance plan entry

1. **Checkpoint and boundary:** The fresh C7 whole-diff check returned `block`
   before any real provider or candidate execution. C8 re-enters planning only
   for exact graph source-port and direct Artifact-input provenance.
2. **North-star link:** A need-to-package run is not independently auditable if
   a WorkRecord can omit an Artifact used by its model projection, compiler,
   Package or Registry publication. Closing that causal chain is required, but
   is not a generated-environment proof.
3. **Trust owner and Artifact effect:** Framework alone commits output-port
   declarations, validates fixed Edges and records exact input refs. Model,
   Agent, candidate, Judge, Controller and Registry authorities do not change.
4. **No-hybrid and minimality assertion:** The affected path remains entirely
   in the cleanroom `agent_world/` slice and imports no legacy control
   authority. C8 adds one tuple field to the existing envelope, corrects fixed
   declarations/bindings and adds focused regressions. It forbids `PortRef`,
   split outputs, another graph/node, scheduler, compatibility, Repair, Expand
   and Consumer implementation.
5. **Evidence:** The independent Terra check reproduced `88 passed`, Ruff,
   mypy, compileall, diff and legacy-firewall success, then identified the
   exact source-port and five executor-binding omissions in
   `direct-c7-final-whole-diff-check-block.md`.
6. **Non-claims:** C8 is a plan only. No corrected provenance code, provider,
   Agent, CandidateBuild, Integration/Judge, Registry or E2E result is yet
   proved; all later children remain unimplemented.
7. **Bad-case effect:** C8 is designed to reject a right-node/wrong-port
   substitution and to prevent Package or Registry from publishing from a
   WorkRecord that omitted a directly consumed dossier, telemetry, lineage or
   package Artifact. Those bad cases are not closed until implementation and
   the independent check pass.
8. **Next permitted gate:** Compute fresh Direct/parent C8 plan identities and
   obtain one independent read-only Terra cross-layer `allow`. Only then may
   implementation begin.

## PAC-27 — C8 deterministic provenance exit and whole-diff check entry

1. **Checkpoint and boundary:** The exact-digest C8 implementation exits the
   five affected Direct provenance boundaries into a fresh independent
   whole-diff check. No real provider, Agent or candidate execution has run.
2. **North-star link:** Direct WorkRecords can now support an auditable
   `EnvironmentRequest -> Design -> Candidate -> Judge -> Package -> Registry`
   chain by recording each declared source port and every Artifact directly
   consumed by Package/Registry. This remains prerequisite evidence, not a real
   generated-environment result.
3. **Trust owner and Artifact effect:** Framework commits immutable envelope
   output ports and exact input/dependency refs. Designer, Controller and
   Registry retain their existing nodes; model/Agent/candidate/Judge authority
   and release ownership are unchanged.
4. **No-hybrid and minimality assertion:** The change stays inside the cleanroom
   two-graph path, adds no legacy import, module, node, graph, PortRef, split
   output, scheduler or media framework, and does not implement Repair, Expand
   or Consumer.
5. **Evidence:** The main session reproduced `91 passed`; Ruff format/check,
   mypy over 13 source files, compileall, `git diff --check`, and the two-test
   legacy firewall all pass. Focused regressions cover hostile source ports,
   one-envelope/two-port fan-out, JSON/zip cold reads and complete
   Package/Registry WorkRecord inputs.
6. **Non-claims:** Deterministic checks do not prove the configured Direct LLM,
   singleton Codex Skill surface, generated CandidateBuild, isolated
   Integration/Judge, Registry publication, safe Observe or fresh E2E. Later
   children remain unimplemented.
7. **Bad-case effect:** A right-node/wrong-port envelope now fails closed, and
   Package/Registry can no longer omit their directly used verifier, lineage,
   dossier, telemetry, physical package or telemetry WorkRecords from causal
   input records. Independent review must still verify the whole diff.
8. **Next permitted gate:** Run one fresh read-only Terra whole-diff check
   against the C8 allow and all prior blocks. Only `allow` permits the ordered
   real Direct proofs.

## PAC-28 — C8 independent allow and real-proof entry

1. **Checkpoint and boundary:** A fresh independent Terra whole-diff review has
   allowed C8 and releases the existing ordered Direct proof gate. The first
   proof is the real `world_architecture` Direct LLM node only, using one
   committed request plus one realistically shaped synthetic evidence envelope;
   it does not claim live upstream Research.
2. **North-star link:** Deterministic provenance is now sufficient to attempt,
   but not assume, the real sequence from model-authored semantics through
   generated candidate, independent Judge, Registry package and Observe.
3. **Trust owner and Artifact effect:** No owner changes. Each proof must use
   the production adapter/boundary and persist only its existing secret-safe
   evidence; a model or Agent response remains a proposal until framework
   validation commits it.
4. **No-hybrid and minimality assertion:** Proofs run only the cleanroom public
   or exact node entrypoints. They add no mock/template/replay success path,
   compatibility code, retry framework or diagnostic node.
5. **Evidence:** Independent review reproduced Direct and parent digests,
   `91 passed`, Ruff, mypy, compileall, legacy firewall and diff checks, and
   found every C8 and prior deterministic closure intact.
6. **Non-claims:** No provider route, Codex Skill surface, CandidateBuild,
   isolated Integration/Judge, Registry release, Observe scene or full Direct
   E2E is yet proved. Repair, Expand and Consumer remain later children.
7. **Bad-case effect:** Each real proof must terminate honestly and be followed
   by Observe. A failure cannot be converted directly into a prompt, Skill,
   retry or code patch.
8. **Next permitted gate:** Use the configured primary
   `deepseek-v4-flash` route (and only the adapter's existing retryable transport
   fallback) to run that exact node, then read its terminal WorkRecord through
   Observe before selecting the next proof.

## PAC-29 — first real Direct proof diagnosis and route-repair entry

1. **Checkpoint and boundary:** The first real `world_architecture` proof
   stopped before model execution with provider HTTP 403; Observe committed
   `direct_http_failure`, no output and `release=not_published`.
2. **North-star link:** Natural-language need to publishable package still
   requires a real semantic model result. The C8 causal provenance chain is
   intact, but real Direct execution is not yet proved.
3. **Trust owner and evidence:** Framework and Observe behaved correctly. The
   first plan incorrectly attributed 403 to `/zen/go`; independent review
   blocked it because both Zen and Go endpoints are official. Identical controls
   then returned 403 on both OpenCode products and 200 on both user-authorized
   localhost models.
4. **Minimality assertion:** Revised plan R1 selects the user-confirmed
   Spark-primary/Luna-fallback localhost routes in the existing
   config/documentation and adds one regression. It
   forbids adapter, graph, Prompt, compiler, fallback-policy, retry,
   provider-profile and later-child changes.
5. **Non-claims:** No Direct model output, Agent Skill, Candidate, Integration,
   Judge, Registry release or E2E is proved. Tests and C8 closure remain
   prerequisites only.
6. **Next permitted gate:** A fresh independent Terra critic must allow exact
   plan R1 before implementation. After deterministic checks, run the exact
   frozen node proof and read Observe.

## PAC-30 — real Spark result reached; usage-evidence repair entry

1. **Checkpoint and boundary:** Route R1 passed independent deterministic
   check. The exact real `world_architecture` proof then reached Spark and
   parsed a response, but failed while committing operation evidence; Observe
   shows the new run still `running`, with zero WorkRecords and no release.
2. **North-star link:** A model result cannot contribute to need-to-package
   generation until its invocation evidence and validated output commit
   atomically. No model success or E2E success is claimed.
3. **Trust owner and evidence:** The first deviation is the framework contract
   mismatch between allowed provider usage aliases and Artifact Prompt-field
   safety. Offline reproduction rejects `prompt_tokens` and commits equivalent
   canonical input/output token keys.
4. **Minimality assertion:** Revised plan R1 normalizes two names, removes two
   unpersistable aliases and adds focused coverage. It leaves the stale run
   unchanged rather than manufacturing a historical terminal fact, and forbids
   reconciliation, safety exceptions, graph/Prompt/compiler/route changes and
   all later-child work.
5. **Non-claims:** The returned JSON did not reach compiler validation. Agent,
   Candidate, Integration, Judge, Registry, Expand and Consumer remain
   unproved.
6. **Next permitted gate:** Obtain a fresh independent Terra critic `allow` for
   exact usage plan R1 before any code or fresh proof. The stale run is not
   cleaned up or adopted.

## PAC-31 — canonical evidence passed; Spark semantic rejection

1. **Checkpoint and boundary:** Canonical usage repair passed independent check.
   A fresh Spark proof then committed two operation-evidence refs and an honest
   failed WorkRecord after one exact correction did not converge.
2. **North-star link:** The invocation/provenance boundary now works, but no
   WorldArchitecture output exists, so need-to-package generation remains
   blocked and no later-stage success is claimed.
3. **Trust owner and evidence:** Observe attributes the failure to Designer
   model-output compatibility: attempt 1 violated `$.name`; attempt 2 failed the
   closed tool contract. Framework, C8 provenance and release blocking behaved
   correctly.
4. **Minimality assertion:** The next plan uses the user's conditional model
   selection and swaps two existing Direct model literals plus synchronized
   descriptions/test. It forbids Prompt/schema normalization, retries, graph or
   later-child changes.
5. **Non-claims:** Luna has only transport reachability evidence. No Luna
   semantic result, Agent, Candidate, Judge, Registry or E2E is proved.
6. **Next permitted gate:** A fresh Terra critic must allow the exact Luna
   selection plan before implementation or another model call.

## PAC-32 — Luna reproduces rejection; terminal feedback gap

1. **Checkpoint and boundary:** Luna reproduced Spark's exact two-stage failure:
   `$.name` correction, then terminal tool-contract rejection. Both runs are
   rejected with complete WorkRecords and no release.
2. **North-star link:** The need-to-package chain cannot be repaired safely from
   a coarse terminal code when the exact safe compiler condition was discarded.
   No Design output or E2E progress is claimed.
3. **Trust owner and evidence:** Model-only attribution is falsified. Runner
   retains retry authority but currently omits the terminal
   `CorrectionPacket` from the existing failure evidence when retry is
   exhausted.
4. **Minimality assertion:** The plan changes one existing failure-value choice
   and adds one regression. It adds no schema, retry, Prompt/compiler rule,
   Observe field, graph or later-child path.
5. **Non-claims:** The exact terminal tool path remains unknown until a fresh
   run records it. Agent, Candidate, Judge, Registry and E2E remain unproved.
6. **Next permitted gate:** Obtain a fresh Terra critic `allow` before the
   one-line persistence change or another model call.

## PAC-33 — exact WorldArchitecture contract mismatch identified

1. **Checkpoint and boundary:** After the terminal-feedback repair passed its
   independent check, fresh Luna run `run_66a0ba4ecc274c20a112e4ed8cf268be`
   rejected at `$.tools[0].name` after the one earlier `$.name` correction.
2. **North-star link:** The natural-language-need-to-package chain is still at
   its first semantic Direct node. A usable architecture was not committed, so
   no downstream or E2E success is claimed.
3. **Trust owner and evidence:** Observe and the referenced failure Artifact
   prove the model-facing shape omitted identifier rules that the Designer's
   framework compiler enforces. The compiler, retry bound, provenance and
   release block remain correct.
4. **Minimality assertion:** The plan changes one existing node-local shape
   string and one focused test. It retains the same compiled output and forbids
   normalization, relaxed validation, more retries, generic schema machinery,
   graph changes and later-child work.
5. **Non-claims:** No WorldArchitecture output, later Design node, Agent,
   Candidate, Integration, Judge, Registry package, Repair, Expand, Consumer,
   or E2E is proved.
6. **Next permitted gate:** A fresh independent Terra critic must allow the
   exact plan before implementation; then deterministic checks precede one
   fresh Luna node proof and Observe read.

## PAC-34 — first real Direct semantic node passed

1. **Checkpoint and boundary:** Fresh Luna run
   `run_037cc42d277a45e5acd6bb80aabf1e32` committed one passed
   `world_architecture` WorkRecord and one compiled architecture Artifact with
   two tools. Observe reports no Finding and no release.
2. **North-star link:** The first semantic step from natural-language need
   toward an executable environment now has real evidence; the remaining
   Design/Build/Judge/Registry chain is still required.
3. **Trust owner and evidence:** Luna proposed through the Direct backend;
   Designer/framework retained validation and commit authority. The WorkRecord
   binds exact request/evidence dependencies and canonical operation usage.
4. **Minimality assertion:** Success came from disclosing the existing
   contract in one string. No compiler relaxation, normalization, retry,
   helper/schema framework, graph, route, or downstream change was added.
5. **Non-claims:** The partial run remains `running` and
   `release=not_published`. No later Design node, Agent, Candidate,
   Integration, Judge, Registry package, Repair, Expand, Consumer, or E2E is
   proved.
6. **Next permitted gate:** Run the planned exact-one-Skill Codex Agent SDK
   preflight. A failure must begin from its safe observable evidence before any
   code change.

## PAC-35 — Codex Agent singleton-Skill proof entry

1. **Checkpoint and boundary:** Entering the approved real SDK preflight for
   the product Agent backend, outside either runtime graph.
2. **North-star link:** Research and build Agent nodes must execute with their
   exact product Runtime Skill before the need-to-package chain can be trusted.
3. **Trust owner and evidence target:** The framework adapter must mount one
   temporary bundle, invoke the real SDK, verify its closure digest, close the
   session and delete the temporary `CODEX_HOME`; the model may only return the
   singleton Skill name and a public bundle-only marker.
4. **Minimality assertion:** This is one temporary proof fixture through the
   existing adapter. It adds no product Skill, profile, permission manager,
   callback system, runtime node, retry or code change.
5. **Non-claims:** A successful preflight would not prove Research semantics,
   CandidateBuild, Integration, Judge, Registry, Repair, Expand, Consumer, or
   E2E.
6. **Next permitted gate:** Run exactly one real SDK turn; validate the returned
   closed JSON and local cleanup facts. On failure, stop and diagnose before
   changing code.

## PAC-36 — Agent preflight failed on SDK-bundled surfaces

1. **Checkpoint and boundary:** The real Agent SDK turn completed and closed,
   but the post-turn singleton check rejected SDK-created `.system` Skills.
   Both temporary homes and the proof fixture were deleted.
2. **North-star link:** Agent-backed Research/Build cannot be trusted while the
   model-visible surface exceeds its selected product Skill; no downstream
   progress is claimed.
3. **Trust owner and evidence:** Bundle digests stayed identical. Safe snapshots
   prove the pinned runtime added `.system` and plugin startup state. The
   adapter correctly retained rejection authority.
4. **Minimality assertion:** The plan adds only two fixed existing SDK config
   overrides and updates one exact tuple assertion. It does not add runtime
   configuration, profiles, permission logic, callbacks or a new node.
5. **Non-claims:** No semantic Agent node, Candidate, Integration, Judge,
   Registry package, Repair, Expand, Consumer or E2E is proved. This preflight
   is outside the product graphs and has no Observe run.
6. **Next permitted gate:** Obtain a fresh independent Terra critic `allow`
   before changing the adapter; then deterministic checks precede one final
   real preflight.

## PAC-37 — Agent preflight exposed the ambient cross-agent Skill root

1. **Checkpoint and boundary:** The post-override real Luna preflight passed
   physical bundle, bundled/plugin, close and cleanup checks, but failed the
   model-visible singleton because the pinned runtime also discovered 24
   Skills from `/home/kelong/.agents/skills`.
2. **North-star link:** Research and Build Agents cannot contribute trustworthy
   evidence or code while unrelated user Skills enter their authority surface;
   no downstream progress is claimed.
3. **Trust owner and evidence:** The adapter retained rejection authority. A
   typed no-model `skills/list` probe attributed the leak to the child
   process's ambient user home; an empty extra-roots RPC was ineffective, while
   an isolated child `HOME` produced exactly the selected Runtime Skill.
4. **Minimality assertion:** The proposed repair adds one fixed child
   environment entry and one exact regression assertion. It adds no SDK RPC,
   config field, profile, permission layer, callback, prompt, retry, graph, or
   downstream behavior.
5. **Non-claims:** No semantic Agent node, Candidate, Integration, Judge,
   Registry package, Repair, Expand, Consumer or E2E is proved. This preflight
   remains outside both product graphs and has no Observe run.
6. **Next permitted gate:** A fresh independent Terra critic must allow the
   exact plan before implementation; deterministic checks then precede one
   final real preflight.

## PAC-38 — final singleton-Skill proof entry

1. **Checkpoint and boundary:** The exact `HOME=CODEX_HOME` implementation has
   fresh critic and independent-check `allow`; focused `20 passed`, full
   `101 passed`, Ruff, mypy, compileall and diff checks are green. Entering one
   fresh real Luna SDK preflight outside both product graphs.
2. **North-star link:** A trustworthy Research/Build Agent backend is required
   before semantic Agent nodes can advance the natural-language need toward an
   executable, independently judged and publishable EnvironmentPackage.
3. **Trust owner and evidence target:** The framework must mount one temporary
   nonce Skill, pass only isolated homes and the selected credential, invoke
   the pinned SDK, validate exact model-visible singleton plus bundle-only
   marker, preserve the physical digest, close once, and clean the fixture.
4. **Minimality assertion:** This is a temporary proof through the existing
   adapter. It adds no product code, Skill, node, route, retry, profile,
   permission, callback or compatibility path.
5. **Non-claims:** Passing proves only the Agent adapter surface. It does not
   prove semantic Research/Build work, Candidate, Integration, Judge, Registry,
   Repair, Expand, Consumer or E2E.
6. **Next permitted gate:** Run exactly one proof and record its terminal facts.
   Any failure starts a new diagnosis before another code change or retry.

## PAC-39 — singleton-Skill Agent backend passed

1. **Checkpoint and boundary:** One fresh real Luna SDK turn returned exactly
   the selected Runtime Skill and its bundle-only nonce. Physical singleton,
   unchanged digest, isolated `HOME/CODEX_HOME`, absence of bundled/plugin/user
   Skills, one close and cleanup all passed.
2. **North-star link:** The tool-enabled Agent backend is now a trustworthy
   executor prerequisite for Research and Build work in the path from a text
   need to an executable, independently judged, publishable package.
3. **Trust owner and evidence:** The Agent proposed the safe JSON; the framework
   retained environment, Skill mounting, digest, singleton, lifecycle and
   cleanup authority. The proof used the pinned SDK and configured Luna route.
4. **Minimality assertion:** The causal product repair was one child environment
   entry plus one regression assertion. The proof fixture was temporary and
   deleted; no SDK RPC, profile, permission layer, callback or node was added.
5. **Non-claims:** This adapter-only proof created no graph run or Observe scene.
   No semantic Research/Build Agent Artifact, Candidate, Integration, Judge,
   Registry package, Repair, Expand, Consumer or E2E is yet proved.
6. **Next permitted gate:** Resume the real Direct graph and prove semantic
   Agent work before CandidateBuild, offline Integration, Judge and Registry.

## PAC-40 — fresh Direct E2E proof entry

1. **Checkpoint and boundary:** Entering one fresh public `agent-world generate`
   run from an unfixed natural-language local-business need through the two
   fixed graphs. The first Direct node and Agent singleton backend have separate
   real proofs; no full run has passed.
2. **North-star link:** This is the actual product path from a text need toward
   an evidence-grounded executable EnvironmentPackage, isolated Integration,
   independent Judge, immutable Registry publication and safe Observe.
3. **Trust owner and evidence target:** Designer/Builder/Judge/Registry retain
   their declared authorities. Every successful node must commit a validated
   Artifact/WorkRecord; any terminal failure must remain unpublished and be
   diagnosed from its safe Observe scene.
4. **Minimality assertion:** The proof uses the existing CLI, configuration,
   two closed graphs and runtime routes. It adds no node, retry, router,
   compatibility path, repair loop or later-child behavior.
5. **Non-claims:** Starting or partially advancing the run is not product
   completion. Repair, Expand, multi-parent evolution and Consumer/SFT/RL stay
   unproved even if Direct releases.
6. **Next permitted gate:** Run once to its honest terminal. On failure, stop,
   read Observe, write one Diagnosis Record and pass a fresh plan through the
   cross-layer critic before changing code.

## PAC-41 — Direct E2E stopped at ToolSemantics

1. **Checkpoint and boundary:** Fresh run
   `run_b644e7e8c9134f099351a80ebd43ded7` terminated `rejected` at
   `tool_semantics[manage_parts]`; Observe confirms
   `release=not_published`.
2. **North-star link:** Research and the early Design prefix now have real
   evidence, but a text need has not produced an executable, independently
   judged or publishable EnvironmentPackage.
3. **Trust owner and evidence:** Designer/framework rejected two Luna proposals
   at the same `$.arguments[3]` condition and preserved attempts, usage,
   validation and Finding refs. Research/Agent isolation were not causal.
4. **Minimality assertion:** No retry, route switch or code change followed the
   failure. The draft plan changes one shape string and one focused test, but a
   critic must first decide whether that would preserve an already-underpowered
   semantic node.
5. **Non-claims:** Passed Research, Architecture, SharedToolSemantics and one
   tool shard are only a prefix. Candidate, Integration, Judge, Registry,
   Repair, Expand, Consumer and E2E remain unproved.
6. **Next permitted gate:** Independent Terra cross-layer review of the exact
   plan and the ToolSemantics-to-downstream semantic contract. Only `allow`
   permits implementation; `block` returns the minimum coherent revision.

## PAC-42 — shape-only ToolSemantics repair blocked and revised

1. **Checkpoint and boundary:** Independent review blocked digest `8dbbac0d`
   because it would preserve an echo-only ToolDraft and discard the only new
   semantic value before WorldRules, Builder, Judge and package.
2. **North-star link:** A structurally valid prompt is insufficient; the text
   need must produce executable tool behavior that an independent Judge and
   immutable package can actually bind.
3. **Trust owner and evidence:** The block traced the current producer and all
   actual consumers. Designer remains semantic owner; framework compiles and
   commits; Builder consumes; Judge verifies; Registry cold-revalidates.
4. **Minimality assertion:** R1 keeps the same node and two graphs, uses the
   already-declared RuleDraft ADT, derives bindings from frozen tool fields and
   adds one narrow Judge gate/package closure. It forbids a generic Rule
   platform, new state-model turn or later-child implementation.
5. **Non-claims:** R1 is plan-only. No semantic shard, Candidate, Integration,
   Judge, Registry, Repair, Expand, Consumer or E2E is newly proved.
6. **Next permitted gate:** Compute the R1 digest and obtain the second fresh
   independent critic verdict for this Diagnosis lineage. Only matching
   `allow` permits implementation.

## PAC-43 — ToolSemantics R1 executable gap and final R2

1. **Checkpoint and boundary:** R1 was blocked only because its private state
   bindings and Judge evaluator lacked an exact trace/ABI and deterministic
   coverage definition. The critic confirmed its producer-to-package scope was
   otherwise necessary and minimal.
2. **North-star link:** Independent verification requires executable evidence,
   not merely moving RuleDraft JSON through nodes. R2 binds two selected rules
   to a real isolated trace without changing the public Runtime contract.
3. **Trust owner and evidence:** Framework fixes snapshot paths, types,
   lifecycle, rule IDs and evaluator semantics; Designer supplies bounded
   business rules; Builder implements; Judge independently traces; Registry
   only checks bytes/digests.
4. **Minimality assertion:** R2 adds no new node, graph, public operation,
   generic engine or later-child code. It explicitly defers dynamic
   error/reject assurance rather than widening the ABI.
5. **Non-claims:** R2 is plan-only. No tool shard, Candidate, Integration,
   Judge, Registry, Repair, Expand, Consumer or E2E is newly proved.
6. **Next permitted gate:** Compute the final R2 identity and obtain one fresh
   independent matching verdict. `block` or `needs_human` stops this lineage;
   only `allow` permits a Terra implement worker.

## PAC-44 — ToolSemantics R2 allowed for implementation

1. **Checkpoint and boundary:** Fresh independent review allowed exact digest
   `c69de83b...` for the existing ToolSemantics producer and its declared
   Design/Builder/Judge/package consumers.
2. **North-star link:** The repair makes tool behavior an executable,
   independently checked package input instead of a field echo, directly
   supporting need-to-environment generation and future semantic evolution.
3. **Trust owner and evidence:** Designer/model proposes bounded RuleDrafts;
   framework owns bindings, assurance selection and compilation; Builder only
   implements; Judge traces independently; Registry only verifies closure.
4. **Minimality assertion:** The allow preserves both graphs and the public
   five-operation ABI, adds no generic Rule platform, model turn, retry,
   Repair, Expand or Consumer code, and excludes private values from durable
   surfaces.
5. **Non-claims:** No implementation or real proof is yet complete. Dynamic
   error/reject behavior is explicitly not assured in Direct v1.
6. **Next permitted gate:** One explicit Terra `trellis-implement` worker may
   implement only R1+R2; then deterministic and independent checks precede any
   real model/Candidate proof.

## PAC-45 — R2 implementation exposed one Observe consumer drift

1. **Checkpoint and boundary:** R2 implementation passes 56 focused tests and
   leaves only two full-suite failures: Observe rejects the new required
   `local_tool_semantics` Judge evidence.
2. **North-star link:** A valid independently judged package must remain
   safely observable after Registry publication; otherwise the
   need-to-publishable-environment path is not closed.
3. **Trust owner and evidence:** Judge and Registry agree on the new safe
   assurance field. Observe alone still enforces the old evidence shape. The
   evidence is deterministic test output, not a model-quality inference.
4. **Minimality assertion:** The proposed addendum changes one conditional
   expected object in existing Observe code and reuses existing tests. It adds
   no node, abstraction, compatibility path or later-child behavior.
5. **Non-claims:** Direct E2E, Candidate execution, Judge execution, Registry
   publication, Repair, Expand and Consumer remain unproved.
6. **Next permitted gate:** A fresh independent critic must allow the exact
   addendum before the one-site Observe repair; then all deterministic checks
   and the original R2 real-proof sequence resume.

## PAC-46 — Observe addendum revised with direct safety negatives

1. **Checkpoint and boundary:** The first Observe addendum was blocked because
   it proved only the valid release, not the exact negative evidence cases.
2. **North-star link:** Safe Observe must expose a genuinely verified package
   while refusing malformed Judge evidence; both halves are required for a
   publishable environment claim.
3. **Trust owner and evidence:** The revised tests intercept only Observe's
   gate-evidence read after a valid Registry fixture, so they exercise the
   consumer comparison rather than merely triggering artifact-integrity
   failure earlier in the chain.
4. **Minimality assertion:** The product edit remains one conditional exact
   object in `observe.py`; one parameterized test covers four mutations. No
   product helper, schema registry or compatibility behavior is added.
5. **Non-claims:** The revision is still plan-only. No real Candidate, Judge,
   Registry, Direct E2E or later child is proved.
6. **Next permitted gate:** Submit the new plan digest to one fresh independent
   critic. Only `allow` permits the one-site implementation and four tests.

## PAC-47 — Observe exact-evidence addendum allowed

1. **Checkpoint and boundary:** A fresh independent critic allowed digest
   `3efaf683...` for the final Judge-evidence-to-Observe consumer closure.
2. **North-star link:** This permits a valid independently judged Registry
   package to remain visible while malformed gate evidence still fails closed.
3. **Trust owner and evidence:** Observe derives the one conditional expected
   assurance only from the immutable Design artifact; Judge and Registry stay
   unchanged and retain their authorities.
4. **Minimality assertion:** Authorization covers one product comparison and
   one parameterized four-case test. It adds no helper framework, node, retry,
   model call, ABI or later-child behavior.
5. **Non-claims:** Passing these tests will not prove a live Candidate, Judge,
   Registry release, Direct E2E, Repair, Expand or Consumer.
6. **Next permitted gate:** Implement exactly the two-file addendum, run its
   focused tests, then resume the complete deterministic R2 acceptance.

## PAC-48 — Judge-to-Observe deterministic closure is green

1. **Checkpoint and boundary:** The allowed Observe change is implemented.
   Eleven focused Observe tests and the complete suite (`107 passed`) are
   green, including four post-integrity evidence-content negatives.
2. **North-star link:** The deterministic release chain can now expose an exact
   valid Registry package and fail closed on malformed local-rule assurance.
3. **Trust owner and evidence:** Design remains assurance owner, Judge emits the
   gate evidence, Registry publishes, and Observe only cold-rechecks/project
   facts. No authority moved between components.
4. **Minimality assertion:** Product code changed only one conditional expected
   dict; one parameterized test covers the safety matrix. No new abstraction,
   node, retry, ABI or compatibility route was added.
5. **Non-claims:** These are deterministic fixtures, not a live ToolSemantics,
   Candidate, Judge, Registry or Direct E2E proof. Later children are unproved.
6. **Next permitted gate:** Complete the already-authorized R2 evaluator test
   matrix, then run an independent whole-diff check before any live proof.

## PAC-49 — R2 deterministic acceptance complete

1. **Checkpoint and boundary:** The R2 evaluator now covers every declared
   predicate/effect plus fail-closed mismatch cases. Main-session verification
   reports `131 passed`; Ruff format/check, mypy, compileall and diff check pass.
2. **North-star link:** Tool semantics are retained as executable rules and can
   be checked against isolated traces rather than reduced to descriptive text.
3. **Trust owner and evidence:** Designer compiles, framework selects assurance,
   Builder receives the closed contract, Integration and Judge use separate
   candidate traces, Registry and Observe recheck only safe durable evidence.
4. **Minimality assertion:** The completion fixed two private predicate
   functions and added parameterized tests. It introduced no node, graph,
   public ABI, generic rule engine, retry or later-child implementation.
5. **Non-claims:** All evidence here is deterministic. No revised live
   ToolSemantics shard, CandidateBuild, Integration, Judge, Registry release or
   complete Direct E2E is yet proved.
6. **Next permitted gate:** A fresh independent Terra whole-diff check must
   verify actual node inputs, outputs, Skills/Prompts and downstream consumers
   before the ordered live proof sequence begins.

## PAC-50 — whole-diff check blocked live proof

1. **Checkpoint and boundary:** Independent check blocked the Direct proof:
   `shared_tool_semantics` produces an untyped payload that is discarded after
   the next prompt, broader Design cards differ from binding task contracts,
   and full test mypy reports eight mechanical errors.
2. **North-star link:** A model call that produces unused semantics cannot help
   turn a need into a faithful executable environment, regardless of Luna's
   instruction following or passing fixture tests.
3. **Trust owner and evidence:** The block maps each actual node projection,
   compiler, artifact and consumer. R2 local rules and Observe are internally
   closed; the new failure is a static cross-node contract audit, not a new
   Observe scene or live-model failure.
4. **Minimality assertion:** No live retry or speculative code change follows
   this block. Canonical requirements will decide whether the false shared
   node is removed or minimally compiled; task-document complexity alone will
   not be copied into code.
5. **Non-claims:** Direct E2E and all later children remain unproved despite
   `131 passed`.
6. **Next permitted gate:** Fix the eight annotation-only errors separately;
   write a narrow Design semantic-closure diagnosis/plan from the canonical
   source, then obtain a fresh cross-layer critic allow before behavioral edits.

## PAC-51 — typed Design semantic closure planned

1. **Checkpoint and boundary:** Full mypy is now mechanically green. A new
   static diagnosis and coordinated plan replace the lossy Research/Design
   producer-consumer chain before any live retry.
2. **North-star link:** The plan makes world state, tool/shared/global behavior
   and parameterized task semantics durable Builder/package inputs and future
   Expand anchors, rather than transient model text.
3. **Trust owner and evidence:** Models retain business source semantics;
   framework owns catalogs, schemas, coupling, digests, assurance classes and
   Modeling Gate; Builder implements; Judge proves only declared traces;
   Registry/Observe retain their existing authority.
4. **Minimality assertion:** It keeps the same two graphs and node families,
   adds no production module/dependency/control plane, uses one conservative
   framework-derived coupling group, and requires deletion of every lossy
   predecessor representation.
5. **Non-claims:** This is plan-only. Shared/global/task behavior is not yet
   dynamically assured, no live Design/Candidate/Judge/Registry path has run,
   and later children remain unproved.
6. **Next permitted gate:** Compute the complete plan digest and dispatch one
   fresh independent Terra cross-layer critic. Only `allow` permits the first
   contracts/graph implementation slice.

## PAC-52 — Design plan revised for executable task reachability

1. **Checkpoint and boundary:** The first semantic-closure plan was blocked
   because typed task data alone did not define the multi-family/tool
   Verifier→Integration→Judge handoff or an executable task evaluator.
2. **North-star link:** A training environment must prove that materialized
   tasks can reach trusted terminal success; merely packaging TaskRequirement
   JSON cannot satisfy need-to-executable-environment.
3. **Trust owner and evidence:** Revision 1 gives framework-owned recipes,
   exact identity goal bindings and reward/termination reduction to Judge;
   Challenger still supplies only bounded public check intent and never seeds,
   concrete mutations, answers or verdicts.
4. **Minimality assertion:** It reuses the existing RuleDraft evaluator,
   Materializer, Runtime, VerifierIntent and Judge nodes. It adds no test DSL,
   scheduler, second Judge, interactive Agent loop or future child.
5. **Non-claims:** Revision 1 is plan-only. Shared concurrency/global rule
   behavior and reality equivalence remain explicit unverified classes; no live
   proof has run.
6. **Next permitted gate:** Recompute the revised digest and obtain one fresh
   independent critic decision. Only `allow` permits implementation.

## PAC-53 — final bounded Design plan revision

1. **Checkpoint and boundary:** The revision-1 critic accepted the executable
   multi-family/task path but blocked three omitted persistence checks:
   Reward/Termination/Verification metadata, Registry equality and private-case
   recipe binding.
2. **North-star link:** A natural-language need is not a publishable executable
   environment unless the task semantics Judge used survive package cold read
   unchanged and each private check is tied to the public recipe it challenges.
3. **Trust owner and evidence:** Framework still owns the three fixed specs,
   their canonical digests and case binding; Judge executes them and Registry
   only reparses/recompares them. No authority moves to a model or candidate.
4. **Minimality assertion:** Revision 2 names fields in the existing
   `rule_ir`, `materializer_protocol` and `assurance` files and adds one compact
   binding check. It adds no node, module, metadata file, evaluator language or
   control plane.
5. **Non-claims:** This is still plan-only. The static suite is green, but the
   revised Design, CandidateBuild, Judge, Registry and full E2E have not run.
6. **Next permitted gate:** Digest `abbab652bfbd389bde56d4c9879948e0c6436faa4eb5ef2a72c8d1f220a3c219`
   receives the one remaining independent Terra critic review. Only `allow`
   permits implementation.

## PAC-54 — typed cross-node closure authorized

1. **Checkpoint and boundary:** The final independent Terra critic returned
   `allow` for the exact revision-2 digest. Implementation may now replace the
   lossy Design→Candidate→Judge→package→Registry handoffs.
2. **North-star link:** The authorized work preserves generated world/task
   semantics until an executable candidate is independently judged and the
   exact verified contract is cold-read for publication.
3. **Trust owner and evidence:** Models/Agents propose business content;
   framework compiles IDs, recipes, evaluator metadata and gates; candidate
   processes execute; Judge verifies; Registry only reparses and compares.
4. **Minimality assertion:** Same two graphs and existing node families; no new
   production module, dependency, generic rule/test language, scheduler or
   later-child implementation. Obsolete first-only DTOs must be deleted.
5. **Non-claims:** `allow` is plan approval only. No revised node, Candidate,
   Judge, Registry or E2E proof has run.
6. **Next permitted gate:** Dispatch bounded Terra implementation slices, then
   a separate whole-diff check before any live proof.

## PAC-55 — typed Design-to-Registry implementation checkpoint

1. **Checkpoint and boundary:** The approved producer/consumer replacement is
   integrated across the two existing graphs. Full deterministic verification
   is green: 156 tests, Ruff format/check, mypy over source+tests, compileall,
   diff check and the legacy firewall.
2. **North-star link:** Generated Research, Architecture, shared/local/world
   semantics and every task family/tool recipe now remain typed inputs through
   Builder, Integration, independent Judge, package metadata and Registry cold
   comparison instead of being truncated to a first tool/task.
3. **Trust owner and evidence:** Direct LLM/Agent nodes still propose only
   bounded source content; framework compiles catalogs, recipes, task evaluator
   specs and gates; candidate processes remain untrusted; Registry only
   canonical-reparses and exact-compares; Observe exposes safe coverage facts.
4. **Minimality assertion:** No production module, node kind, graph, dependency,
   generic rule/schema engine, scheduler or later-child path was added. Obsolete
   singleton DTOs and consumers were deleted. Production Python is 10,289 LOC,
   up 1,480 from the pre-closure 8,809 baseline, so the independent check must
   explicitly reject unused fields, duplicate helpers and future-only code.
5. **Non-claims:** This proves deterministic contract closure only. No revised
   real Direct node, CandidateBuild, Judge, Registry release or end-to-end run
   has executed; Repair, Expand and Consumer remain unproved.
6. **Next permitted gate:** A fresh independent Terra whole-diff check must
   return `allow` before the ordered real proofs begin.

## PAC-56 — whole-diff provenance block

1. **Checkpoint and boundary:** The independent whole-diff check blocked live
   proof despite 156 green tests: two ResearchPlan fields have no consumer and
   three Direct nodes use Evidence without complete graph provenance.
2. **North-star link:** A publishable generated environment cannot claim typed
   lineage when model/compiler inputs are absent from immutable dependencies or
   Agent output is retained without affecting execution.
3. **Trust owner and evidence:** This is static source/graph evidence, not an
   Observe terminal and not a Luna/Skill failure. Framework owns the missing
   dependency recording; the Research Agent owns only the bounded fields the
   framework actually consumes.
4. **Minimality assertion:** The new plan deletes unused fields and adds three
   existing Evidence bindings. It adds no type, node, module, route, metadata
   schema or future-child code, and requires non-increasing production LOC.
5. **Non-claims:** Deterministic green status does not prove the corrected
   implementation or any real node/E2E release.
6. **Next permitted gate:** Compute the new plan digest and obtain a fresh
   independent Terra critic `allow` before changing product code.

## PAC-57 — bounded provenance repair authorized

1. **Checkpoint and boundary:** A fresh Terra critic allowed digest
   `5df3243e47c0703e9e5ec64d79dbfeb7963f847a4ba9990497e180090d3cb2b9` for
   the exact ResearchPlan deletion and three-node Evidence dependency repair.
2. **North-star link:** The repair ensures only consumed Research intent enters
   the DesignGraph and every evidence-sensitive semantic commit carries the
   immutable dependency required for a trustworthy package lineage.
3. **Trust owner and evidence:** Research Agent output becomes smaller;
   GraphRunner remains the sole provenance/commit owner; Direct nodes gain no
   new authority and see only the safe citation catalog, never raw sources.
4. **Minimality assertion:** Existing nodes/ports/Artifact type are reused;
   no production type, module, graph, route or later child is added. Production
   LOC must be non-increasing.
5. **Non-claims:** This is plan authorization only, not implementation or live
   proof.
6. **Next permitted gate:** Implement with one bounded Terra worker, run all
   deterministic checks, then repeat the independent whole-diff review.

## PAC-58 — Design provenance repair implemented

1. **Checkpoint and boundary:** Unused ResearchPlan fields are removed and
   SharedToolSemantics, CurriculumPlan and TaskRequirement now declare, bind
   and commit their exact Evidence dependency. TaskRequirement sees the same
   safe citation catalog its compiler validates.
2. **North-star link:** Research intent and evidence now travel through exact
   node contracts into the verified environment lineage without dead model
   output or hidden compiler inputs.
3. **Trust owner and evidence:** Framework graph provenance remains
   authoritative; Agent/Direct ownership and all Candidate/Judge/Registry
   boundaries are unchanged. Deterministic evidence is 159 tests plus green
   Ruff, full mypy, compileall, diff check and firewall.
4. **Minimality assertion:** Production Python decreased from 10,289 to 10,267
   LOC. No new type, node, module, route or compatibility path was added.
5. **Non-claims:** No real provider/node/Candidate/Judge/Registry/E2E proof has
   run after this repair.
6. **Next permitted gate:** A fresh independent whole-diff recheck must allow
   the ordered live proof sequence.

## PAC-59 — ModelingGate shared dependency block

1. **Checkpoint and boundary:** The recheck accepted the Research/Evidence
   repair but found ModelingGate directly consumes committed shared contracts
   without naming their Artifact input.
2. **North-star link:** The final EnvironmentDesign and package must bind the
   exact SharedToolSemantics it contains; transitive ToolDraft digests do not
   replace the direct dependency.
3. **Trust owner and evidence:** Static source/graph evidence only; GraphRunner
   remains owner of the port/Edge/dependency, with no model or candidate
   authority change.
4. **Minimality assertion:** Revision 2 adds one existing optional port, one
   Edge and one input binding. Zero groups use an empty tuple, never a fake
   Artifact. No generic optional-port platform is allowed.
5. **Non-claims:** 159 green tests still do not prove this correction or any
   live execution.
6. **Next permitted gate:** Recompute revision-2 digest and obtain the final
   fresh critic decision for this static lineage before implementation.

## PAC-60 — ModelingGate shared dependency repair authorized

1. **Checkpoint and boundary:** A fresh independent Terra critic allowed
   revision-2 digest
   `e9f664ba3e6e27fcf30cd3c60cb3c0981c58f49fb13210f2cd23b4fa15c66137`
   for the exact ModelingGate shared-contract dependency closure.
2. **North-star link:** The final EnvironmentDesign must bind every committed
   SharedToolSemantics Artifact it contains before it can honestly proceed
   toward executable, independently verified and publishable output.
3. **Trust owner and evidence:** GraphRunner remains the transaction and
   provenance owner. The allowed evidence is the static plan and independent
   review only; model, candidate, Judge and Registry authority do not change.
4. **Minimality assertion:** The implementation is limited to one existing
   port, one edge, exact shared refs, a closed two-node optional allowlist and
   focused regressions. No module, node kind, generic framework or fake empty
   Artifact is permitted, and production LOC must remain non-increasing.
5. **Non-claims:** This authorization is not a live node, Candidate, Judge,
   Registry, Repair, Expand, Consumer or E2E proof.
6. **Next permitted gate:** Apply the bounded patch, run deterministic checks,
   then obtain a fresh independent whole-diff allow before any live proof.

## PAC-61 — Design input provenance closure implemented

1. **Checkpoint and boundary:** ModelingGate now declares the existing
   `shared_tools` input, receives the exact ordered SharedToolSemantics refs,
   commits them as direct dependencies and binds them in semantic identity.
   A single-tool world supplies an explicit empty optional tuple without a
   synthetic Artifact or WorkRecord.
2. **North-star link:** Every semantic value compiled into EnvironmentDesign
   is now attributable to the immutable DesignGraph lineage required by later
   Build, independent Judge and publishable package boundaries.
3. **Trust owner and evidence:** GraphRunner remains the only transaction and
   provenance owner. Deterministic evidence is 160 passing tests plus green
   Ruff, full mypy, compileall, diff check and legacy firewall.
4. **Minimality assertion:** No module, node kind, route, model call, generic
   optional mechanism or compatibility path was added. The complete
   provenance repair reduced production Python from 10,289 to 10,281 lines.
5. **Non-claims:** Static checks do not prove a real Direct call, Agent Skill,
   Candidate process, Judge, Registry publication, Repair, Expand, Consumer or
   end-to-end completion.
6. **Next permitted gate:** A fresh independent whole-diff review must return
   `allow` before the ordered real-execution proofs begin.

## PAC-62 — semantic identity whole-diff block

1. **Checkpoint and boundary:** The independent whole-diff accepted the
   declared ports, edges and direct Artifact dependencies, but blocked live
   proof because some Agent-visible or framework-consumed inputs are absent
   from their node semantic identity.
2. **North-star link:** A publishable EnvironmentPackage cannot claim an exact
   executable lineage if changing a frozen node input can retain the same
   semantic revision.
3. **Trust owner and evidence:** This is static source and deterministic-check
   evidence, not an Observe scene. GraphRunner remains the provenance owner;
   no model, Candidate, Judge or Registry authority changes.
4. **Minimality assertion:** Before planning a repair, audit every existing
   DesignGraph and CandidateGraph execution site once. Prefer correcting the
   existing semantic material in place; do not add a new node, framework,
   schema, compatibility path or retry.
5. **Non-claims:** The current 160 green tests do not prove semantic identity
   closure or any live Direct/E2E boundary.
6. **Next permitted gate:** Persist one bounded static diagnosis and one
   complete minimal plan, then obtain a fresh cross-layer critic `allow`.

## PAC-63 — semantic/release plan revision-1 block

1. **Checkpoint and boundary:** The independent critic accepted the bounded
   Design/Candidate/Registry scope and rejected generic identity machinery, but
   required the new Registry physical-ref equality predicate to have one
   explicit acceptance revision.
2. **North-star link:** Registry must prove both which physical package Package
   committed and which versioned predicate authorized its publication before
   Observe can expose a released environment.
3. **Trust owner and evidence:** Static plan/code evidence only; Registry keeps
   cold-verification authority and Controller keeps the sole release decision.
4. **Minimality assertion:** Revision 2 adds one literal semantic-material
   field and one regression. No NodeSpec field, reflection, source hashing,
   model/profile identity system, node, schema or config is introduced; the
   production growth ceiling is reduced to 18 lines.
5. **Non-claims:** No implementation, provider call, Candidate, Judge,
   Registry publication or E2E proof exists for this revision.
6. **Next permitted gate:** Obtain the final fresh critic decision for plan
   digest `ba30e6d969cdc66f4c848a743f50e7aade191440a4cafcd28a3923322a65c3a5`.

## PAC-64 — semantic and physical release closure authorized

1. **Checkpoint and boundary:** A fresh independent Terra critic allowed the
   final revision-2 plan digest
   `ba30e6d969cdc66f4c848a743f50e7aade191440a4cafcd28a3923322a65c3a5`.
2. **North-star link:** The approved repair binds actual node projections and
   ensures Registry publishes the exact physical package committed by Package,
   preserving the need-to-executable-and-publishable lineage.
3. **Trust owner and evidence:** Static plan/critic evidence only. GraphRunner,
   Controller and Registry keep their current provenance, release and
   publication authorities; Agents/Candidate/Judge gain none.
4. **Minimality assertion:** Existing semantic maps, one dependency selection,
   one equality gate and one explicit acceptance literal only. No new module,
   type, node, graph, config, reflection or generic identity framework; net
   production growth is capped at 18 lines.
5. **Non-claims:** Authorization is not implementation, live Provider,
   Candidate, Judge, Registry, E2E, Repair, Expand or Consumer proof.
6. **Next permitted gate:** Implement in disjoint bounded slices, run all
   deterministic checks, then obtain a fresh independent whole-diff `allow`.

## PAC-65 — Registry failed-record ZIP blocker

1. **Checkpoint and boundary:** The Registry mismatch gate now executes before
   publication, but deterministic proof found GraphRunner.fail cannot cold-read
   its existing ZIP dependency and therefore cannot commit the terminal failed
   Registry WorkRecord.
2. **North-star link:** A rejected physical package must leave an attributable
   terminal graph fact; otherwise Observe and later repair cannot distinguish a
   trustworthy rejection from an infrastructure crash.
3. **Trust owner and evidence:** Static pytest traceback only, not Observe.
   GraphRunner remains failure-record owner and Registry remains publisher.
4. **Minimality assertion:** The proposed repair mirrors the existing closed
   JSON/ZIP input rule in one loop, adds no helper/media type/node/schema/retry,
   and preserves the 10,299 production-line ceiling.
5. **Non-claims:** No current live Provider, Candidate, Judge, Registry or E2E
   proof is claimed.
6. **Next permitted gate:** Obtain a fresh critic decision for plan digest
   `fc89e935c0835c5c403927e55948d20ff720ee0e66feb027d24b88c50e24cc24`.

## PAC-66 — GraphRunner ZIP failure persistence authorized

1. **Checkpoint and boundary:** A fresh independent critic allowed the exact
   GraphRunner.fail JSON/ZIP closed-media branch for digest
   `fc89e935c0835c5c403927e55948d20ff720ee0e66feb027d24b88c50e24cc24`.
2. **North-star link:** Registry rejection can now be planned to leave the
   Validation/Finding/failed WorkRecord evidence needed for trustworthy
   Observe and later bounded repair.
3. **Trust owner and evidence:** Static review only; GraphRunner keeps failure
   persistence authority and Registry keeps publication authority.
4. **Minimality assertion:** One existing loop, two already-supported media
   branches, no helper/media type/node/schema/retry or architecture change.
5. **Non-claims:** Authorization is not implementation or live/E2E evidence.
6. **Next permitted gate:** Apply the bounded branch, rerun focused and full
   deterministic checks, then repeat independent whole-diff review.

## PAC-67 — semantic, release and failure persistence implemented

1. **Checkpoint and boundary:** ResearchSynthesis, ModelingGate,
   TaskRequirement, CandidateBuild, Package and Registry now bind their exact
   current semantic inputs; Registry verifies Package's exact physical ref;
   GraphRunner persists a failed Registry WorkRecord with ZIP dependencies.
2. **North-star link:** The Direct path now has deterministic evidence that
   generated semantics, executable candidate lineage and physical publication
   cannot silently diverge at these handoffs.
3. **Trust owner and evidence:** Framework owners and two fixed graphs are
   unchanged. Evidence is 163 passing tests plus green Ruff, full mypy,
   compileall, diff check and legacy firewall.
4. **Minimality assertion:** No module/type/node/graph/config/helper framework
   or compatibility path was added. Production Python is exactly 10,299 lines,
   the approved combined ceiling.
5. **Non-claims:** Deterministic success is not a real Direct/Agent/Candidate/
   Judge/Registry/E2E proof and says nothing yet about later children.
6. **Next permitted gate:** A fresh independent whole-diff `allow` is required
   before the ordered real-execution sequence.

## PAC-68 — WorldRules predicate validator block

1. **Checkpoint and boundary:** The final whole-diff retained one blocker: an
   inverted type condition accepts non-object predicate right values and can
   escape the typed DesignError/failed WorkRecord path.
2. **North-star link:** Invalid model-authored rules must be rejected with
   attributable evidence before they can enter Design, Builder, Judge or a
   published environment.
3. **Trust owner and evidence:** Static source review only; Designer compiler
   and GraphRunner failure authority remain unchanged.
4. **Minimality assertion:** The plan removes one `not` and adds one focused
   regression, with zero production LOC increase and no schema/helper/node/
   Prompt/retry change.
5. **Non-claims:** No live node or E2E proof is claimed.
6. **Next permitted gate:** Obtain critic decision for plan digest
   `8f4f8ef20126864a40cc9f400496d7e4a96824c8489ed433c68a7af53caa3ab1`.

## PAC-69 — WorldRules closed-shape repair revision

1. **Checkpoint and boundary:** Revision 1 was blocked because removing the
   inverted type check alone would still accept arbitrary objects and could
   call mapping operations on non-objects. Revision 2 instead requires one
   local exact closed-union check for the existing literal or semantic-ref
   shape.
2. **North-star link:** Model-authored rules must cross into executable design
   only through the declared predicate contract, while malformed output must
   leave attributable failure evidence rather than a raw Python exception.
3. **Trust owner and evidence:** Static diagnosis and critic feedback only;
   Designer compilation and GraphRunner failure ownership remain unchanged.
4. **Minimality assertion:** One local boolean and focused regressions only;
   no helper, schema, node, route, retry, Prompt, config or production LOC
   increase is authorized.
5. **Non-claims:** No implementation, live node, Candidate, Judge, Registry or
   E2E proof is claimed.
6. **Next permitted gate:** Obtain the final independent critic decision for
   revision-2 digest
   `f1c972c62d6b2d9478b70c24656453892901a3cd365c0938da46ae15954b1d0e`.

## PAC-70 — WorldRules closed-shape repair authorized

1. **Checkpoint and boundary:** An independent Terra critic allowed revision
   2/2 digest
   `f1c972c62d6b2d9478b70c24656453892901a3cd365c0938da46ae15954b1d0e`.
2. **North-star link:** The authorized guard keeps malformed model-authored
   rules out of executable Design and preserves safe, attributable failure
   evidence on the need-to-package path.
3. **Trust owner and evidence:** Static review only; Designer remains compiler
   and GraphRunner remains failure-record owner.
4. **Minimality assertion:** Only one local closed-union boolean and focused
   regressions are allowed; no new abstraction, node, route, retry or config.
5. **Non-claims:** Authorization is not implementation or a live/E2E proof.
6. **Next permitted gate:** Implement the bounded change, run focused and full
   deterministic checks, then obtain a fresh whole-diff decision.

## PAC-71 — WorldRules repair deterministically verified

1. **Checkpoint and boundary:** `_compile_rules` now admits only the two
   existing exact `right` shapes after confirming a mapping; malformed model
   output enters the existing typed correction/failure transaction.
2. **North-star link:** Invalid WorldRules cannot silently reach executable
   Design, downstream Candidate work or Registry publication, while valid
   literal and frozen-binding predicates remain unchanged.
3. **Trust owner and evidence:** Designer and GraphRunner ownership is
   unchanged. Evidence is 165 passing tests, green Ruff format/check, full
   mypy, compileall, diff check and legacy firewall.
4. **Minimality assertion:** No helper, type, node, route, retry, Prompt or
   config was added. Production Python is 10,297 lines, two below the prior
   ceiling.
5. **Non-claims:** Deterministic checks are not real Direct, Agent, Candidate,
   Judge, Registry or E2E proof.
6. **Next permitted gate:** Obtain a fresh independent whole-diff `allow`
   before starting the ordered real-execution proofs.

## PAC-72 — Direct whole-diff recheck allowed live proof

1. **Checkpoint and boundary:** A fresh independent Terra review, supervised
   through Trellis channel, allowed the current Direct diff after verifying the
   prior sole WorldRules blocker, typed failure transaction and downstream
   edge closure.
2. **North-star link:** The two fixed graphs may now be exercised toward the
   actual target: natural-language need to executable, independently verified,
   publishable `EnvironmentPackage`.
3. **Trust owner and evidence:** Static whole-diff and focused two-test evidence
   only; framework, Direct LLM, Agent, Candidate, Judge and Registry ownership
   remain unchanged.
4. **Minimality assertion:** The review authorized no new product code,
   abstraction, route, retry, config or future-child behavior.
5. **Non-claims:** No current live provider, Agent, Candidate, Judge, Registry
   or E2E success is claimed.
6. **Next permitted gate:** Run one current Direct LLM node, then one isolated
   Agent turn, Candidate/Integration/Judge, and finally a fresh CLI E2E; read
   Observe after every product-run terminal.

## PAC-73 — Entering current Direct LLM node proof

1. **Checkpoint and boundary:** Begin one fresh `world_architecture`
   `direct_llm` transaction through the current backend, exact graph inputs,
   compiler, Artifact commit and WorkRecord path.
2. **North-star link:** This is the first current real boundary on the route
   from a natural-language need to an executable, independently verified and
   publishable environment.
3. **Trust owner and evidence:** Direct LLM may propose only source semantics;
   Designer owns compilation and commit. Credentials remain environment
   handles and no value is persisted.
4. **Minimality assertion:** One node and one fresh proof run only; no code,
   retry, route, Prompt, Skill or configuration change.
5. **Non-claims:** A pass will not prove Research, Agent, Candidate,
   Integration, Judge, Registry or E2E completion.
6. **Next permitted gate:** Execute once on configured Luna primary, inspect
   the persisted WorkRecord and Observe scene, then record the exact result.

## PAC-74 — Direct WorldArchitecture proof failed at hidden field rules

1. **Checkpoint and boundary:** Real run
   `run_5c648fca95e64bc08107b70a48127854` reached the Luna-backed
   `world_architecture` compiler twice and failed on two `values` rules absent
   from the rendered output shape.
2. **North-star link:** No malformed architecture was committed or allowed to
   reach later Design, Candidate, Judge or Registry work; release remained
   `not_published`.
3. **Trust owner and evidence:** Observe records a Designer-owned blocking
   Finding, two attempts, two real Luna usage records, typed Validation and a
   failed WorkRecord with no outputs.
4. **Minimality assertion:** The proposed response is disclosure of existing
   rules in one Prompt literal, not another retry, model switch, validator
   weakening, Skill, node or schema system.
5. **Non-claims:** This is a failed single-node proof, not an E2E attempt or
   evidence about Agent/Candidate/Judge/Registry success.
6. **Next permitted gate:** Review plan lineage
   `world-architecture-visible-field-contract` before any model-facing edit or
   new real invocation.

## PAC-75 — WorldArchitecture repair revision closes typed compiler holes

1. **Checkpoint and boundary:** Revision 1 was blocked because Prompt
   disclosure alone left existing non-enum values, duplicate owner fields and
   duplicate actor indexes able to escape as raw dataclass exceptions.
2. **North-star link:** Revision 2 keeps malformed model output inside the
   attributable Designer failure transaction before any downstream executable
   environment work.
3. **Trust owner and evidence:** Real Observe plus static compiler review;
   Designer/GraphRunner ownership and all graph edges remain unchanged.
4. **Minimality assertion:** The final plan permits one compact shape literal,
   local prechecks and at most one local deduplicating field-array function;
   no schema layer, generic catch, retry, model switch, node or config.
5. **Non-claims:** Revision 2 is only a plan and does not prove a node or E2E.
6. **Next permitted gate:** Obtain the final critic decision for digest
   `322bfa3a31ed44a281045469aff9daa9a1a4df6fe27ba6afaa388b32b0642eec`.

## PAC-76 — WorldArchitecture recipient/compiler repair authorized

1. **Checkpoint and boundary:** A fresh independent critic allowed revision
   2/2 digest
   `322bfa3a31ed44a281045469aff9daa9a1a4df6fe27ba6afaa388b32b0642eec`.
2. **North-star link:** The approved repair makes the Direct architecture
   proposal contract visible and keeps malformed proposals inside safe
   Designer evidence before executable downstream work.
3. **Trust owner and evidence:** The allow is grounded in real Observe plus
   compiler review; no owner, NodeSpec, edge, route or Artifact ABI changes.
4. **Minimality assertion:** One literal, three local prechecks and at most one
   local deduplicating function; no schema layer, catch-all, retry or model
   switch.
5. **Non-claims:** Authorization is not implementation or real proof success.
6. **Next permitted gate:** Implement only the allowed two-file slice, run
   deterministic checks, independently verify, then rerun one real node.

## PAC-77 — WorldArchitecture recipient/compiler repair verified statically

1. **Checkpoint and boundary:** The current Direct recipient now sees the
   compact Field contract across entity/tool collections, while matching local
   prechecks keep the three invalid-owner cases in typed failure persistence.
2. **North-star link:** The Architecture source boundary can now be honestly
   exercised again without teaching Luna hidden schema rules one retry at a
   time or allowing invalid state toward executable downstream work.
3. **Trust owner and evidence:** Designer/GraphRunner ownership is unchanged.
   Evidence is 171 passing tests plus green Ruff, full mypy, compileall, diff
   check and legacy firewall.
4. **Minimality assertion:** No public helper, type, module, schema framework,
   catch-all, node, route, retry, model or config was added. Production Python
   remains 10,297 lines.
5. **Non-claims:** Static success is not a real node, Candidate, Judge,
   Registry or E2E proof.
6. **Next permitted gate:** Obtain one independent implementation check, then
   rerun the same-class real Luna `world_architecture` proof and Observe it.

## PAC-78 — WorldArchitecture SourceDraft authority re-opened

1. **Checkpoint and boundary:** The old full-key Field wire contract passed
   static checks, but review against the source of truth showed that it makes
   Luna emit framework-normalizable empty/null placeholders and actor indexes.
2. **North-star link:** Natural-language need must become an executable,
   independently verified, publishable `EnvironmentPackage`; making the model
   own mechanical schema serialization weakens that path rather than advancing
   it.
3. **Trust owner and evidence:** Real Luna evidence proves transport and JSON
   generation worked. The canonical document assigns business semantics to the
   Direct LLM and schema/index/reference compilation to the Designer framework.
4. **Minimality assertion:** The new plan keeps one node and existing internal
   contracts, normalizes two optional Field keys, and resolves actor names to
   existing indexes; it adds no graph, retry, Skill, schema layer or config.
5. **Non-claims:** The 172 passing deterministic tests and the prior scoped
   check do not prove this corrected boundary, a live node, or product E2E.
6. **Next permitted gate:** Obtain an independent cross-layer decision for the
   new exact plan before editing code or invoking Luna again.

## PAC-79 — SourceDraft plan revision closes critic block

1. **Checkpoint and boundary:** Revision 1 was blocked until sparse-key
   rejection, semantic presence, deterministic actor resolution and the exact
   production-line budget were explicit; revision 2 now fixes all four.
2. **North-star link:** It keeps Luna responsible only for business semantics
   while preserving the compiled WorldArchitecture consumed on the path to an
   executable, independently verified, publishable `EnvironmentPackage`.
3. **Trust owner and evidence:** Direct LLM chooses field presence/domain and
   actor scope; Designer alone normalizes defaults, resolves actor indexes,
   validates, persists and later compiles schema mechanics.
4. **Minimality assertion:** One existing parser, one shape literal and focused
   tests only, with production Python capped at 10,299 lines from a measured
   10,297 baseline; no new type, helper layer, node, edge, Skill or config.
5. **Non-claims:** Revision 2 is a plan, not implementation, schema assembly,
   live node success or E2E evidence.
6. **Next permitted gate:** A fresh independent critic must allow the new exact
   digest before implementation.

## PAC-80 — Sparse WorldArchitecture SourceDraft repair authorized

1. **Checkpoint and boundary:** An independent Terra critic allowed revision
   2/2 digest `4548902cbc2faede2d12b2e94dc1f87b4c3e6913ccb57ac5aaf39b165e94831f`
   for the Direct proposal-to-Designer compiler seam.
2. **North-star link:** The repair removes framework mechanics from the model
   output while preserving the compiled architecture used toward an executable,
   independently verified and publishable `EnvironmentPackage`.
3. **Trust owner and evidence:** Luna owns sparse business source semantics;
   Designer owns defaults, actor-index resolution, validation, persistence and
   downstream compiled shape. Evidence is plan/source/code review only.
4. **Minimality assertion:** Exactly `design.py` plus focused tests, no new
   abstraction or topology, and production Python remains at most 10,299 lines.
5. **Non-claims:** Allow is not implementation, live Direct success, schema
   assembly, Candidate/Judge/Registry proof or E2E completion.
6. **Next permitted gate:** Implement the exact allow, run deterministic checks
   and an independent implementation check before one fresh Luna proof.

## PAC-81 — Sparse SourceDraft implementation verified locally

1. **Checkpoint and boundary:** WorldArchitecture now receives sparse field
   semantics and actor names; Designer normalizes defaults and emits the same
   internal FieldDeclaration, ToolSurface indexes and downstream Artifact shape.
2. **North-star link:** The first Direct modeling boundary is smaller and more
   reliable on the route from natural-language need to executable,
   independently verified, publishable environment.
3. **Trust owner and evidence:** Direct LLM retains only business presence,
   type/domain/relation and actor-scope choices. Designer owns all validation,
   index resolution and WorkRecord persistence. Local evidence is 180 passing
   tests plus green Ruff, mypy, compileall and diff check.
4. **Minimality assertion:** Only `design.py` and focused tests changed; no node,
   edge, type, module, route or config was added. Production Python is exactly
   10,299 lines, the approved ceiling.
5. **Non-claims:** Offline checks do not prove Luna, later Design nodes,
   Candidate, Judge, Registry or E2E success.
6. **Next permitted gate:** Obtain an independent implementation `allow`, then
   run one fresh Luna WorldArchitecture proof and inspect Observe.

## PAC-82 — Entering sparse WorldArchitecture real proof

1. **Checkpoint and boundary:** Independent implementation review allowed the
   exact sparse proposal-to-compiler change with no findings; begin one fresh
   Luna `world_architecture` transaction through normal GraphRunner persistence.
2. **North-star link:** This is one necessary real boundary toward converting a
   natural-language need into an executable, independently verified,
   publishable `EnvironmentPackage`.
3. **Trust owner and evidence:** Luna proposes only source business semantics;
   Designer validates/normalizes/commits and Observe remains read-only. No
   credential value enters artifacts.
4. **Minimality assertion:** One node, configured Luna primary, existing single
   correction and no code/config/model/retry/topology change.
5. **Non-claims:** Even a pass proves neither full Design nor Agent, Candidate,
   Integration, Judge, Registry, Expand, Consumer or E2E behavior.
6. **Next permitted gate:** Execute once, persist the terminal WorkRecord, read
   Observe immediately, and diagnose any new failure before another change.

## PAC-83 — Sparse WorldArchitecture real proof passed

1. **Checkpoint and boundary:** Run `run_b934bc068ebc4dd99c711fed6df6f75c`
   passed `design/world_architecture` on its first real Luna invocation and
   committed one compiled architecture plus passed WorkRecord.
2. **North-star link:** A natural-language equipment-checkout need now crosses
   the current first Direct modeling boundary toward an executable,
   independently verified, publishable `EnvironmentPackage`.
3. **Trust owner and evidence:** Luna proposed sparse semantics using 688 input
   and 2,352 output tokens; Designer compiled 4 entities, 5 tools and 26 fields,
   converted actor names to indexes, and persisted no Finding. Observe confirms
   the passed Work and `not_published` release.
4. **Minimality assertion:** One call, no correction, no fallback, no code or
   configuration change, and no source-only `actor_names` in the Artifact.
5. **Non-claims:** The run remains a partial `running` proof and does not prove
   remaining Design, Agent, Candidate, Judge, Registry, Expand, Consumer or E2E.
6. **Next permitted gate:** Exercise one current Codex Agent + singleton Skill
   boundary before Candidate/Integration/Judge and final fresh CLI E2E.

## PAC-84 — Entering semantic Codex Agent proof

1. **Checkpoint and boundary:** Begin one fresh `research_plan` graph
   transaction through the real Codex SDK backend with only the product-owned
   `research-world-evidence` Runtime Skill and a disposable workspace/home.
2. **North-star link:** A trustworthy semantic Research Agent is required to
   turn arbitrary natural-language needs into grounded designs before building
   executable, independently verified and publishable environments.
3. **Trust owner and evidence:** Agent may propose only bounded queries and
   questions; framework owns Skill singleton/digest checks, JSON validation,
   Artifact/WorkRecord commit and cleanup.
4. **Minimality assertion:** One existing node, one SDK turn, one existing Skill,
   no product edit, fixture Skill, retry, route, profile or configuration change.
5. **Non-claims:** Passing does not prove search/fetch/extract, synthesis,
   remaining Design, Candidate, Judge, Registry or E2E.
6. **Next permitted gate:** Execute once and inspect its Observe facts before
   advancing to Candidate proof.

## PAC-85 — Semantic ResearchPlan Agent passed

1. **Checkpoint and boundary:** Run `run_c180b70173214a6b86e48b4bedaa7fb8`
   passed `design/research_plan` on one real Codex SDK Luna turn with the sole
   `research-world-evidence` Runtime Skill.
2. **North-star link:** The current Agent backend can turn the same natural
   language need into bounded research intent on the path to an executable,
   independently verified and publishable environment.
3. **Trust owner and evidence:** Agent returned 4 queries and 12 questions;
   framework persisted one passed attempt, Luna usage and exact Skill digest.
   Observe has no Finding and release remains not published.
4. **Minimality assertion:** One existing semantic node, no correction,
   fallback, fixture Skill, code/config/profile or route change.
5. **Non-claims:** This does not prove acquisition/synthesis, remaining Design,
   Candidate, Judge, Registry, Expand, Consumer or E2E.
6. **Next permitted gate:** Start one fresh full CLI run and stop at the first
   honest terminal for Observe-driven diagnosis or released-package proof.

## PAC-86 — First full CLI run stopped at hidden text bound

1. **Checkpoint and boundary:** Run `run_d825291601a741da8d854a94400e2d01`
   passed real Research plan/acquisition/synthesis, then failed twice at
   `world_architecture $.boundary.purpose` because its 160-character limit was
   absent from both Prompt and numeric correction feedback.
2. **North-star link:** The graph correctly blocked an invalid architecture
   before Candidate or release, preserving the path toward an executable,
   independently verified and publishable package rather than accepting it.
3. **Trust owner and evidence:** Observe has three passed upstream WorkRecords,
   one Designer-owned failed Work, two real Luna operations, one blocking
   Finding and `not_published` release.
4. **Minimality assertion:** Proposed repair is one compact existing-bound
   clause plus exact `_text` feedback; no retry, model switch, limit relaxation,
   node split, schema system or downstream change.
5. **Non-claims:** Research prefix success and this honest rejection are not
   Design, Candidate, Judge, Registry or E2E completion.
6. **Next permitted gate:** Independently review the exact repair plan before
   any code change or new full run.

## PAC-87 — Text-bound repair localized after critic block

1. **Checkpoint and boundary:** Revision 1 was blocked because changing shared
   `_text` would alter correction inputs across Research, Direct and task nodes;
   revision 2 leaves it untouched and localizes exact feedback to the observed
   WorldArchitecture boundary-purpose field.
2. **North-star link:** The repair makes the blocked semantic handoff actionable
   without destabilizing other nodes on the route to an executable,
   independently verified and publishable environment.
3. **Trust owner and evidence:** Designer retains the same accepted normalized
   value and Work transaction; only the local rejected proposal receives the
   exact existing 160-character fact.
4. **Minimality assertion:** One in-place local precheck, one compact shape
   clause and focused tests; shared helper, graph, retry, route and downstream
   contracts remain unchanged, with production LOC <=10,299.
5. **Non-claims:** Revision 2 is only a plan, not implementation, node success,
   later Design progress or E2E completion.
6. **Next permitted gate:** Fresh independent critic on the revised digest.

## PAC-88 — Local WorldArchitecture text-bound repair authorized

1. **Checkpoint and boundary:** Independent critic allowed revision 2/2 digest
   `9d799e5635ef9debe187032deefc1138a89982e533ad07640c53dd9a05cb1d30`,
   limited to WorldArchitecture shape and boundary-purpose precheck.
2. **North-star link:** Exact existing limits become actionable without
   confusing LLM semantic authorship with framework validation on the path to
   an executable, independently verified and publishable environment.
3. **Trust owner and evidence:** Luna still proposes semantics; Designer alone
   enforces/normalizes and GraphRunner owns correction/commit. Evidence is
   Observe, code audit and independent plan review.
4. **Minimality assertion:** Shared `_text`, all other nodes and downstream
   contracts stay unchanged; no helper/module/retry/config, LOC <=10,299.
5. **Non-claims:** Authorization is not implementation, real proof or E2E.
6. **Next permitted gate:** Implement the two-file slice, run deterministic and
   independent checks, then local real node proof before another full run.

## PAC-89 — Local text-bound repair verified deterministically

1. **Checkpoint and boundary:** WorldArchitecture now discloses its existing
   textual bounds; only boundary-purpose rejection carries exact local 160
   feedback, while valid normalization and compiled output remain unchanged.
2. **North-star link:** The real Research-to-Architecture handoff is now
   actionable toward executable, independently verified, publishable output.
3. **Trust owner and evidence:** LLM remains semantic proposer; Designer owns
   precheck/correction and GraphRunner persistence. Evidence is 181 passing
   tests plus green Ruff, mypy, compileall and diff check.
4. **Minimality assertion:** Shared helper and every other node are unchanged;
   only `design.py` and focused tests changed, production Python 10,298 lines.
5. **Non-claims:** Deterministic success is not a real node or E2E proof.
6. **Next permitted gate:** Independent implementation check, then the ordered
   real WorldArchitecture and full CLI proofs.

## PAC-90 — Entering real-evidence WorldArchitecture repro proof

1. **Checkpoint and boundary:** Independent implementation review allowed the
   local repair with no findings; begin one fresh WorldArchitecture run using
   the prior failed full run's real 28-claim/6-citation evidence class.
2. **North-star link:** This directly rechecks the failed Research-to-Design
   handoff before spending another full run toward executable, independently
   verified, publishable output.
3. **Trust owner and evidence:** Frozen evidence is copied into a new provenance
   envelope; Luna proposes semantics and Designer owns all validation/commit.
4. **Minimality assertion:** One node and configured Luna only; no code, retry,
   route, config or synthetic simplification of the evidence projection.
5. **Non-claims:** Passing proves this causal repair only, not later nodes/E2E.
6. **Next permitted gate:** Read WorkRecord/Observe; only a pass permits a fresh
   full CLI run.

## PAC-91 — Exact numeric disclosure still failed real evidence

1. **Checkpoint and boundary:** Run `run_1b81e19380194e13a406f10dfcf3d0df`
   used the prior full run's 28 claims/6 citations and failed twice at the same
   boundary-purpose condition despite exact 160-character shape/correction.
2. **North-star link:** The failure shows a framework storage policy is blocking
   semantic world generation before executable, independently verified,
   publishable output can be built.
3. **Trust owner and evidence:** Two real Luna responses reached Designer;
   GraphRunner correctly blocked release and Observe records no output plus one
   blocking Finding.
4. **Minimality assertion:** New plan moves only descriptive purpose bounding to
   deterministic framework normalization; identity/execution constraints stay
   strict, with no retry, split, helper system or model switch.
5. **Non-claims:** This failed node is not later Design or E2E evidence.
6. **Next permitted gate:** Independent critic must review the new ownership
   correction before code or another provider call.

## PAC-92 — Non-lossy WorldArchitecture purpose repair authorized

1. **Checkpoint and boundary:** Independent Terra/max critic allowed revision
   2/2 digest `a3ed41405a5597299fcc7f7e669489304541ebfff5042f0ab45885afb562e0a8`.
2. **North-star link:** The repair retains complete business purpose semantics
   at the Research-to-Architecture handoff so later Design, Builder, package
   and Registry consumers can keep progressing toward an executable,
   independently verified, publishable `EnvironmentPackage`.
3. **Trust owner and evidence:** Direct LLM owns the business description;
   Designer hardcodes only strip, a finite 4096-code-point safety bound,
   validation and commit; GraphRunner retains correction and revision
   authority. The allow traces the same committed value through all existing
   consumers.
4. **Minimality assertion:** No Agent Skill, candidate process, graph edge,
   route/model, retry, helper, schema platform, Judge or release behavior may
   change; accepted text is never truncated and production LOC may not grow.
5. **Non-claims:** The allow is not implementation, a real node pass, complete
   Design, Candidate, Judge, Registry, Repair, Expand, Consumer or E2E proof.
6. **Next permitted gate:** Bounded implementation and deterministic plus
   independent checks; only then one same-evidence live node proof.

## PAC-93 — Non-lossy purpose repair verified deterministically

1. **Checkpoint and boundary:** The three-file implementation passed an
   independent Terra/max check with 184 tests, Ruff, mypy, compileall and diff
   checks green; production Python is 10,296 lines.
2. **North-star link:** WorldArchitecture can now preserve the complete
   evidence-grounded business purpose that later Design, Builder, package and
   Registry consumers need on the path to an executable, independently
   verified, publishable `EnvironmentPackage`.
3. **Trust owner and evidence:** Direct LLM still proposes only semantic prose;
   Designer deterministically strips, validates a 4096-code-point ceiling and
   commits it; GraphRunner rotates semantic identity and owns correction. Tests
   trace the same full value through Artifact, Direct consumers, Builder and
   package metadata.
4. **Minimality assertion:** The implementation reduced production code by two
   lines and changed no Agent Skill, candidate process, graph, route, retry,
   Judge, Registry authority or compatibility path.
5. **Non-claims:** Deterministic checks do not prove a real Luna node, complete
   Design, Candidate, Judge, Registry, Repair, Expand, Consumer or E2E release.
6. **Next permitted gate:** One same-evidence real WorldArchitecture invocation
   and immediate WorkRecord/Artifact/Observe inspection.

## PAC-94 — Entering non-lossy same-evidence Luna proof

1. **Checkpoint and boundary:** Begin one fresh Designer-owned
   `world_architecture` Direct LLM transaction using the copied real bicycle
   repair evidence class: 28 claims and six citations.
2. **North-star link:** This tests the exact Research-to-Architecture handoff
   before another full request spends work on later Design, Candidate, Judge
   and Registry boundaries.
3. **Trust owner and evidence:** Luna receives only the rendered Direct Prompt,
   frozen need/evidence and closed shape with no Skill, tools or workspace;
   Designer/GraphRunner alone validate, correct and commit.
4. **Minimality assertion:** One node, one configured Luna route and the
   existing one-correction policy; no model fallback, retry increase, manual
   proposal edit or downstream execution.
5. **Non-claims:** A pass proves only live acceptance and persistence of the
   repaired purpose contract, not complete Design or product E2E.
6. **Next permitted gate:** Inspect WorkRecord, operation evidence, Artifact
   purpose length/digest and Observe. Continue only if purpose is >160 and
   <=4096 code points, complete, and accepted without correction.

## PAC-95 — Non-lossy WorldArchitecture live proof passed

1. **Checkpoint and boundary:** Real run
   `run_0bc34874311541c4bd0b7a9135887393` passed the Designer-owned
   `world_architecture` transaction once with Luna and no correction.
2. **North-star link:** A complete 228-code-point business purpose from real
   research evidence was committed for downstream Design and Candidate work on
   the path from a natural-language need to a verified, publishable package.
3. **Trust owner and evidence:** Direct LLM proposed semantics with no Skill or
   tools; framework code owned the 4096 bound, compilation, Artifact and
   WorkRecord. Observe reports the work passed and release `not_published`.
4. **Minimality assertion:** No route, node, retry, fallback, compatibility
   path, or later-child feature was added for this proof.
5. **Non-claims:** This is not a Candidate, Judge, Registry, repair, Expand,
   Consumer, or full E2E proof.
6. **Next permitted gate:** Run one fresh public Direct E2E and stop at the
   first terminal failure for Observe-driven diagnosis.

## PAC-96 — Fresh Direct E2E stopped at WorldArchitecture

1. **Checkpoint and boundary:** Public run
   `run_fac8d0b2961842c996837d2f035e3102` passed Research Plan, acquisition and
   synthesis, then failed the Designer-owned WorldArchitecture transaction.
2. **North-star link:** No executable, independently verified, publishable
   `EnvironmentPackage` exists from this request; Registry is `not_published`.
3. **Trust owner and evidence:** Two real Luna Direct calls completed. The first
   violated entity-reference closure; after exact correction the second reached
   and violated the tools cardinality check. Framework rejected the run and
   committed one blocking Finding without accepting partial model output.
4. **Minimality assertion:** The terminal was observed before any code change,
   provider fallback, extra retry, output editing, or later-child execution.
5. **Non-claims:** Passing Research and a prior isolated Architecture proof are
   not Direct E2E, Candidate, Judge, Registry, Expand, or Consumer completion.
6. **Next permitted gate:** Persist diagnosis and a minimal repair plan, then
   obtain an independent Terra/max cross-layer critic decision.

## PAC-97 — Minimal WorldArchitecture prompt-contract plan awaiting critic

1. **Checkpoint and boundary:** The proposed repair clarifies only the existing
   Direct node's whole-object instructions and aligns its task contract text.
2. **North-star link:** It aims to preserve evidence-grounded global world
   coherence so the unchanged downstream Design/Candidate/Judge/Registry chain
   can receive one valid Architecture.
3. **Trust owner and evidence:** LLM still owns semantic entities and tools;
   framework still owns the `1..8` bound, references, compiler, correction,
   Finding and release. The latest Observe scene is the trigger.
4. **Minimality assertion:** No hardcoded business semantics, new node, split,
   retry, prompt framework, model route, compatibility path, or child feature.
5. **Non-claims:** A reviewed or implemented prompt change alone proves no live
   node and no E2E release.
6. **Next permitted gate:** Independent cross-layer critic; only `allow` permits
   implementation.

## PAC-98 — Critic block corrected a cross-field contract error

1. **Checkpoint and boundary:** Revision-1 was blocked because its prose would
   apply declared-entity closure to tool argument/result relation names.
2. **North-star link:** Preserving valid tool relations protects downstream Tool
   Semantics, Candidate generation and package behavior while repairing the
   Architecture handoff needed for an eventual verified release.
3. **Trust owner and evidence:** Framework continues to close entity-owned
   fields; Direct LLM may still name optional external tool-field relations;
   the existing compiler/test distinction is the compatibility evidence.
4. **Minimality assertion:** Revision-2 changes the plan only and adds no product
   code, global reference policy, retry, node, or child path.
5. **Non-claims:** A corrected plan is neither an implementation nor live proof.
6. **Next permitted gate:** Fresh independent review of revision-2 digest
   `sha256:9e2c17c8ca5979d1493b22c405d20dbe034717e21b8d373110e3f1e866cfa8be`.

## PAC-99 — Location-qualified prompt repair authorized

1. **Checkpoint and boundary:** Independent Terra/max review allowed revision-2
   for the Designer-owned WorldArchitecture source boundary.
2. **North-star link:** The repair should let one coherent evidence-grounded
   Architecture feed the unchanged Design/Candidate/Judge/Registry path toward
   an executable, independently verified, publishable package.
3. **Trust owner and evidence:** Direct LLM owns semantic architecture;
   framework owns cardinality, entity-field closure, compiler and release.
   External tool argument/result relation names remain valid by compiler and
   regression evidence.
4. **Minimality assertion:** Allowed files are one local shape block, focused
   tests, and the WorldArchitecture task-contract prose; no node, retry, route,
   helper, schema generator, business fixture, or child feature.
5. **Non-claims:** Critic `allow` authorizes implementation only; it is not
   model, Candidate, Judge, Registry, E2E, Expand, or Consumer evidence.
6. **Next permitted gate:** Bounded implementation followed by deterministic
   and independent whole-diff checks before any provider call.

## PAC-100 — Whole-object prompt repair verified deterministically

1. **Checkpoint and boundary:** The allowed three-file implementation passed an
   independent check: 186 tests, Ruff, mypy, compileall and diff check; product
   Python remains 10,296 lines.
2. **North-star link:** WorldArchitecture now gives Luna an actionable complete
   semantic contract while preserving the typed handoff needed by downstream
   Design, Candidate, Judge and Registry.
3. **Trust owner and evidence:** Direct LLM still proposes semantics;
   framework compiler still rejects nine tools and invalid entity-owned refs,
   accepts valid external tool-field relations, and permits only one correction.
4. **Minimality assertion:** No compiler, dataclass, node, edge, route, retry,
   helper, Agent Skill, candidate process, or child path changed.
5. **Non-claims:** Deterministic tests do not prove Luna compliance, Candidate,
   Judge, Registry publication, E2E, Expand, or Consumer.
6. **Next permitted gate:** One real frozen-evidence WorldArchitecture
   transaction, followed immediately by Work/Artifact/operation/Observe review.

## PAC-101 — Frozen-evidence WorldArchitecture proof passed

1. **Checkpoint and boundary:** Run
   `run_9c2ce8e229be4f198e4934e77ec77468` passed the repaired Direct transaction
   once with Luna, zero correction, seven entities and seven tools.
2. **North-star link:** The failed E2E's exact evidence now yields a coherent
   committed Architecture that can feed the unchanged path toward an
   executable, independently verified and Registry-published package.
3. **Trust owner and evidence:** Direct LLM proposed semantics with no Skill or
   tools; framework validated/compiled and committed Artifact/Work. Observe has
   no Finding and release remains `not_published`.
4. **Minimality assertion:** No fallback, extra attempt, output edit, hardcoded
   tool list, new node, compiler relaxation, or later-child execution.
5. **Non-claims:** A passed Architecture node is not complete Design,
   Candidate, Judge, Registry, E2E, Repair, Expand, or Consumer proof.
6. **Next permitted gate:** One fresh public Direct request through terminal
   Observe; stop at the first new failure for a new diagnosis.

## PAC-102 — Fresh E2E advanced to SharedToolSemantics and stopped

1. **Checkpoint and boundary:** Run
   `run_4528cf8a411a4d8a82b6390465c6d138` passed Research and WorldArchitecture,
   then failed `shared_tool_semantics[1-2-3-4-5-6]` on non-JSON model content.
2. **North-star link:** No executable, independently verified, publishable
   package exists; Registry remains `not_published` despite the deeper graph
   prefix.
3. **Trust owner and evidence:** Luna used the no-Skill Direct route; framework
   rejected content, committed no SharedTool Artifact, one failed Work and one
   blocking Finding. Observe is terminal `rejected`.
4. **Minimality assertion:** The run stopped before any output scraping,
   fallback, extra model call, compiler relaxation, group split, or later child.
5. **Non-claims:** Passing Architecture is not complete Design or E2E.
6. **Next permitted gate:** Persist the recipient-view diagnosis and submit a
   bounded prompt/feedback plan to independent cross-layer review.

## PAC-103 — SharedTool JSON-contract plan awaiting critic

1. **Checkpoint and boundary:** Revision-1 proposes an exact current output
   grammar plus use of GraphRunner's existing one local correction for model
   content that is not JSON.
2. **North-star link:** A valid shared contract is required before per-tool
   semantics, Candidate generation, independent Judge and Registry release.
3. **Trust owner and evidence:** Direct LLM owns shared semantics; framework
   owns JSON parsing, exact group/bounds, correction eligibility and release.
4. **Minimality assertion:** No model/route/token/timeout change, response
   scraping, group split, node/helper/retry system, or later-child feature.
5. **Non-claims:** The plan does not prove a real SharedTool node or E2E.
6. **Next permitted gate:** Independent Terra/max review of
   `sha256:7c47c0571028a26a0d59a5c346331852592f8a81e6b3944b64513bbfc84b35ce`.

## PAC-104 — SharedTool plan reduced after critic block

1. **Checkpoint and boundary:** Revision-1 was blocked because it reclassified
   parser-level non-JSON failures across all Direct nodes without reconciling
   common policy or preserving safe failed-call provenance.
2. **North-star link:** Revision-2 retains fail-closed parser behavior while
   repairing only the incomplete semantic handoff needed before ToolSemantics,
   Candidate, Judge and Registry can proceed.
3. **Trust owner and evidence:** Framework keeps parser/correction/release
   authority; Direct LLM receives only a complete current SharedTool grammar.
4. **Minimality assertion:** Generic Direct correction, provenance, native
   response mode, parser, model, group, retry and later-child changes are all
   removed. Native response-format support remains explicitly unproven.
5. **Non-claims:** A revised plan is not implementation or live proof.
6. **Next permitted gate:** Fresh independent review of revision-2 digest
   `sha256:74fb7af2f3efec6e479c2befece2ac83e0a9ffe6704b104bd0c63e5498e563b7`.

## PAC-105 — Minimal SharedTool grammar repair authorized

1. **Checkpoint and boundary:** Independent Terra/max review allowed the local
   SharedTool recipient-contract revision at the exact revision-2 digest.
2. **North-star link:** A valid compiled shared contract is needed before
   per-tool semantics and the downstream executable/Judge/Registry path.
3. **Trust owner and evidence:** Direct LLM owns shared business semantics;
   framework retains exact group, parser, compiler, correction and release.
4. **Minimality assertion:** Only SharedTool shape/objective, focused tests and
   its task card may change; parser, common Direct policy, response mode,
   compiler, group, node/edge/route, Agent/candidate and children are forbidden.
5. **Non-claims:** Native response-format support and strict domain partition
   uniqueness remain unproven; critic allow is not live or E2E evidence.
6. **Next permitted gate:** Bounded implementation and independent deterministic
   check before one frozen-parent SharedTool provider proof.

## PAC-106 — SharedTool grammar repair verified deterministically

1. **Checkpoint and boundary:** Independent check allowed the three-file
   implementation with 189 tests and all static gates green; production Python
   remains 10,296 lines.
2. **North-star link:** The exact SharedTool contract is now visible before
   downstream ToolSemantics and the eventual executable/Judge/Registry path.
3. **Trust owner and evidence:** Direct owns semantic values; framework parser,
   compiler, exact group, corrections and release remain unchanged. Tests prove
   non-JSON stays terminal and parsed invalid JSON retains one correction.
4. **Minimality assertion:** No common Direct, response-mode, compiler, group,
   node/edge/route, Agent, candidate or child change.
5. **Non-claims:** Static success does not prove Luna JSON output, SharedTool
   live acceptance, E2E, Expand or Consumer.
6. **Next permitted gate:** One exact frozen Architecture/Evidence SharedTool
   provider transaction and immediate Observe inspection.

## PAC-107 — Exact-parent SharedTool proof passed

1. **Checkpoint and boundary:** Run
   `run_11f4549f984443d08a77acf5b66227b0` passed the six-tool SharedTool Direct
   shard once with Luna and zero correction.
2. **North-star link:** The exact failed E2E parents now yield a committed
   shared contract for downstream ToolSemantics on the path to a verified
   Registry package.
3. **Trust owner and evidence:** Direct proposed shared semantics with no Skill
   or tools; framework validated and committed Artifact/Work. Observe has no
   Finding and release remains `not_published`.
4. **Minimality assertion:** No response mode, parser, model, retry, group,
   compiler, output edit, hardcoded semantics or later child.
5. **Non-claims:** SharedTool pass is not remaining Design, Candidate, Judge,
   Registry, E2E, Expand or Consumer completion.
6. **Next permitted gate:** One fresh public Direct request; stop at the first
   new terminal for Observe-driven diagnosis.

## PAC-108 — Fresh E2E advanced through SharedTool and stopped at ToolSemantics

1. **Checkpoint and boundary:** Run
   `run_1bec958e41ae4207beb4a7b40149f9c0` passed Research,
   WorldArchitecture and the seven-tool SharedTool shard, then rejected the
   first `tool_semantics[register_member]` proposal and its one authorized
   correction at the same hidden RuleDraft root shape.
2. **North-star link:** The run reached a deeper semantic handoff but produced
   no complete Design, executable candidate, independent Judge evidence or
   Registry package; release remains `not_published`.
3. **Trust owner and evidence:** Luna completed both no-Skill Direct calls;
   framework validation rejected both, committed no ToolSemantics Artifact,
   and persisted failed Work/Finding evidence. Agent and candidate-process
   boundaries were never entered.
4. **Minimality assertion:** No fallback, output edit, extra retry, validator
   relaxation, hardcoded tool behavior or later-child path was used.
5. **Non-claims:** A passed Research/Architecture/Shared prefix is not Design,
   Candidate, Judge, Registry, E2E, Repair, Expand or Consumer completion.
6. **Next permitted gate:** Review the persisted diagnosis and coordinated
   remaining-Design contract plan; no code or provider retry before `allow`.

## PAC-109 — Remaining Design Direct closure plan awaiting critic

1. **Checkpoint and boundary:** Revision-1 digest
   `sha256:7c80f044a8bfeaeef59ffbdeb445b646b0abd82de34dccb3a86b6ff5e8dd2af8`
   closes the shared RuleDraft/Curriculum recipient contracts, removes only
   redundant frozen echoes, and hardens their local validators.
2. **North-star link:** These typed semantics must be consumable by Modeling
   Gate and CandidateGraph before an executable, independently verified and
   Registry-published EnvironmentPackage is possible.
3. **Trust owner and evidence:** Direct LLMs retain only semantic proposals;
   framework retains frozen coordinates, indexes, compiler, validation,
   digests, correction budget, Judge and release. Agents and the untrusted
   candidate process are unchanged.
4. **Minimality assertion:** No new node, graph, call, response mode, retry
   system, Agent Skill, candidate path, compatibility layer or later child is
   proposed; obsolete echoes/string duplication must be deleted.
5. **Non-claims:** Diagnosis and plan identity are not implementation, provider
   proof, complete Design, Candidate, Judge, Registry or E2E evidence.
6. **Next permitted gate:** Fresh independent Terra/max cross-layer review of
   this exact plan digest.

## PAC-110 — Revision-1 blocked on exact grammar and proof boundary

1. **Checkpoint and boundary:** Independent Terra/max review blocked revision
   1 because its allegedly exact contract omitted the 1..64 error-name bound,
   mislabeled hyphen-permitting Curriculum names as snake names, and did not
   state the diagnostic suffix's non-resume/non-release boundary explicitly.
2. **North-star link:** Recipient contracts must match the compiler exactly
   before they can safely feed ModelingGate, Candidate, Judge and Registry.
3. **Trust owner and evidence:** The reviewer confirmed the core ownership:
   Direct proposes semantics; framework owns all frozen coordinates, grammar,
   validation, retries, digests, Judge and release; Agent/candidate paths stay
   unchanged.
4. **Minimality assertion:** The block requests three plan clarifications, not
   a new node, schema engine, route, retry, Agent or downstream implementation.
5. **Non-claims:** A blocked review authorizes no product-code change or live
   retry and proves no Design, Candidate, Judge, Registry or E2E completion.
6. **Next permitted gate:** Revise the same lineage once, compute a fresh
   digest, and obtain a fresh independent decision.

## PAC-111 — Exact revision-2 plan awaiting final critic

1. **Checkpoint and boundary:** Revision 2/2 digest
   `sha256:df78d60dce150753a8251e5f809321dc303f6fe0417fd02ae47ed7d6b7e13650`
   now states the exact RuleDraft error-name and current Curriculum grammars and
   marks the suffix proof as diagnostic, immutable-parent, non-resume and
   non-release.
2. **North-star link:** The bounded repair still advances only the semantic
   handoff needed before executable candidate generation and independent
   release verification.
3. **Trust owner and evidence:** Framework/Direct/Agent/candidate ownership is
   unchanged; no model gains IDs, hashes, validation, routing or release power.
4. **Minimality assertion:** All original node/edge/route/call/retry and
   downstream constraints remain; only blocked plan text was corrected.
5. **Non-claims:** Revision 2 is neither implementation nor a provider/E2E
   proof.
6. **Next permitted gate:** Fresh independent Terra/max review of this exact
   final lineage digest; unresolved block would become `needs_human`.

## PAC-112 — Remaining Design contract closure authorized

1. **Checkpoint and boundary:** Fresh independent Terra/max review allowed the
   exact revision-2 digest and classified it as the smallest coordinated
   Design-source/validator repair.
2. **North-star link:** The repair makes the remaining Direct semantic outputs
   consumable by ModelingGate without claiming the executable/Judge/Registry
   portion of the natural-language-to-EnvironmentPackage path.
3. **Trust owner and evidence:** Direct LLM owns only semantic drafts;
   framework owns frozen coordinates, grammar, compiler, validation, digests,
   correction bounds, Judge and release. Research/Build Agents and candidate
   process behavior remain unchanged.
4. **Minimality assertion:** Allowed writes are only `design.py`, the existing
   `SharedToolContract`, five node cards and focused existing tests; production
   Python must stay at or below 10,320 lines.
5. **Non-claims:** Critic allow is not implementation, provider proof, complete
   Design, Candidate, Judge, Registry, E2E, Repair, Expand or Consumer proof.
6. **Next permitted gate:** One bounded implementation, then an independent
   deterministic whole-scope check before any live provider call.

## PAC-113 — Bounded Design contract implementation is deterministically green

1. **Checkpoint and boundary:** The allowed four-file implementation completed:
   two production files, one focused test file and the five Design node cards.
   Full pytest reports 203 passed; the legacy firewall reports 2 passed; Ruff,
   mypy, compileall and diff checks pass; production Python is 10,311 lines.
2. **North-star link:** Remaining Design Direct recipients now expose compiler-
   exact semantics that can feed ModelingGate and the downstream executable,
   Judge and Registry path.
3. **Trust owner and evidence:** Direct outputs only semantic rules/scopes;
   framework injects frozen Shared/Tool/Task coordinates and retains exact
   partition, typed validation, digest, correction, Judge and release authority.
   Agent and candidate-process code did not change.
4. **Minimality assertion:** No module, node, edge, route, response mode, retry,
   Skill, Candidate, Registry, compatibility or later-child code was added.
5. **Non-claims:** Static gates do not prove Luna output, a complete Design,
   Candidate execution, Judge, Registry publication or E2E completion.
6. **Next permitted gate:** Fresh independent Terra/max implementation review;
   only an `allow` permits the immutable-parent diagnostic provider proof.

## PAC-114 — Static check gap closed within the existing allow

1. **Checkpoint and boundary:** Independent check blocked only because the
   approved real-failure regression was omitted: malformed ToolSemantics
   RuleDraft root -> exact correction -> valid second proposal commit.
2. **North-star link:** The added regression now exercises the precise Direct
   semantic handoff that failed before downstream Design/Candidate work.
3. **Trust owner and evidence:** The test proves framework validation/correction
   remains authoritative while Direct supplies a complete replacement with a
   legal 64-code-point error kind; no production authority changed.
4. **Minimality assertion:** Only `tests/test_design_semantics.py` changed; this
   was explicit deterministic-check item 3 in the already allowed plan, so no
   new semantic plan, product code or architecture was introduced.
5. **Non-claims:** Sixteen focused tests passing do not prove Luna, full Design,
   Candidate, Judge, Registry or E2E.
6. **Next permitted gate:** Fresh independent implementation check, including
   full tests/static gates, before any provider call.

## PAC-115 — Remaining Design contract implementation independently allowed

1. **Checkpoint and boundary:** A fresh independent Terra/max re-check allowed
   the exact revision-2 implementation after 204 tests, the legacy firewall,
   Ruff, mypy, compileall and diff checks passed at 10,311 production lines.
2. **North-star link:** The compiler-visible Direct contracts are now ready for
   the smallest real handoff proof toward an executable, independently judged,
   publishable `EnvironmentPackage`.
3. **Trust owner and evidence:** Direct LLM still owns only semantic drafts;
   framework owns frozen coordinates, validation, correction bounds, digests,
   Work/Artifact records, Judge and release. Agent and candidate-process paths
   were not changed or exercised.
4. **Minimality assertion:** The final closure added only the already-approved
   focused regression; no node, edge, route, retry, Skill or later-child path
   was introduced.
5. **Non-claims:** Independent static allow is not a Luna provider result,
   complete Design, Candidate, Judge, Registry, Direct E2E, Repair, Expand or
   Consumer/SFT/RL proof.
6. **Next permitted gate:** Run only the immutable-parent, non-resume,
   non-publish SharedTool plus first ToolSemantics diagnostic suffix.

## PAC-116 — Immutable-parent diagnostic suffix starting

1. **Checkpoint and boundary:** The proof will reuse the exact immutable
   Evidence and WorldArchitecture bytes from failed run
   `run_1bec958e41ae4207beb4a7b40149f9c0`, regenerate SharedTool, and invoke only
   `tool_semantics[register_member]` in a fresh diagnostic run.
2. **North-star link:** This isolates the real Direct semantic handoff before
   permitting another full natural-language-to-EnvironmentPackage attempt.
3. **Trust owner and evidence:** Luna proposes the bounded SharedTool and
   ToolSemantics source objects; framework alone validates, injects frozen
   indexes, compiles, persists Work/Artifacts and reports Observe. No Agent,
   Skill, candidate process, Judge, Registry or release authority participates.
4. **Minimality assertion:** This is a temporary proof harness over existing
   code and exact parent artifacts, not a product-code or graph change.
5. **Non-claims:** Even a pass proves only this two-boundary suffix and cannot
   be adopted, resumed, published or treated as Registry evidence.
6. **Next permitted gate:** Inspect safe Work/Artifact/operation and Observe;
   only a pass within the existing correction bound permits one fresh public
   Direct E2E.

## PAC-117 — Diagnostic suffix stopped honestly at SharedTool

1. **Checkpoint and boundary:** Real run
   `run_13b48bda4cde4498a95c0a7e0d402f6a` stopped before ToolSemantics after two
   Luna calls failed the same `$.atomicity` exact-partition condition.
2. **North-star link:** Rejecting an incomplete shared contract protects every
   later ToolDraft, Candidate, Judge and Registry package from false progress.
3. **Trust owner and evidence:** Direct LLM proposed JSON; framework enforced
   the frozen `[1..7]` partition and two-call bound. Observe records one failed
   Direct Work/Finding and `release=not_published`; no Agent or candidate ran.
4. **Minimality assertion:** Diagnosis identifies a missing canonical
   conservative construction in the existing recipient contract; the plan
   changes no field, compiler, graph, retry, Skill or downstream ABI.
5. **Non-claims:** The prior static allow did not prove this provider boundary;
   no complete Design, Candidate, Judge, Registry, E2E or later child exists.
6. **Next permitted gate:** Fresh independent cross-layer review of the local
   plan before any implementation or provider retry.

## PAC-118 — Conservative SharedTool recipient repair authorized

1. **Checkpoint and boundary:** Independent Terra/max review allowed exact plan
   `sha256:85bd13a73024d4accd7e7b2cdcb066c016b594a7807e815124a495b16d56c2a3`
   as a local Direct recipient-contract repair.
2. **North-star link:** A mechanically usable but semantically honest shared
   contract is required before ToolDrafts can feed executable Candidate/Judge
   and immutable Registry publication.
3. **Trust owner and evidence:** Luna retains semantic partition, ordering,
   compensation and policy ownership; framework retains frozen coordinates,
   exact validation, correction budget, digest, Work/Artifact and release.
   Agent and candidate-process ownership are unchanged.
4. **Minimality assertion:** The allowed implementation is one output-shape
   sentence, one safe correction condition, one node-card sentence and focused
   assertions. No field, compiler, graph, route, retry or downstream ABI moves.
5. **Non-claims:** Critic allow and code edits do not prove Luna, ToolSemantics,
   complete Design, Candidate, Judge, Registry, E2E or later children.
6. **Next permitted gate:** Deterministic checks and a fresh independent
   implementation review before repeating the exact diagnostic suffix.

## PAC-119 — Conservative SharedTool repair is deterministically green

1. **Checkpoint and boundary:** The exact allowed implementation passes 14
   focused SharedTool tests, 204 full tests, the two-test legacy firewall,
   Ruff format/check, mypy, compileall and diff checks.
2. **North-star link:** The Direct recipient now exposes the canonical valid
   whole-group construction while preserving exact downstream compilation
   toward Candidate, Judge and Registry.
3. **Trust owner and evidence:** Luna still chooses semantic domains and text;
   framework still validates and compiles without normalizing invalid output.
   No Agent, Skill or candidate-process code changed.
4. **Minimality assertion:** Production code is 10,315 lines, below the 10,320
   ceiling; no module, field, node, edge, route, retry or compatibility path was
   added.
5. **Non-claims:** Deterministic success does not prove a Luna response,
   ToolSemantics, complete Design, Candidate, Judge, Registry or E2E.
6. **Next permitted gate:** Fresh independent implementation check; only an
   `allow` permits the immutable-parent diagnostic suffix retry.

## PAC-120 — Conservative SharedTool implementation independently allowed

1. **Checkpoint and boundary:** Fresh independent Terra/max check allowed the
   exact implementation after 110 focused and 204 full tests plus all static
   gates passed at 10,315 production lines.
2. **North-star link:** The SharedTool source can now be attempted at the true
   provider boundary without weakening the path to executable, independently
   judged and publishable packages.
3. **Trust owner and evidence:** The checker confirmed framework does not
   synthesize, fill, sort or normalize partitions; Direct remains Skill-less
   and owns semantic grouping; Agent/candidate code is untouched.
4. **Minimality assertion:** Six source fields, compiled ABI, graph, route,
   one-correction/two-call bound and all downstream consumers remain unchanged.
5. **Non-claims:** Static allow proves no provider response, ToolSemantics,
   Design closure, Candidate, Judge, Registry, E2E or later-child behavior.
6. **Next permitted gate:** Repeat only the immutable-parent diagnostic suffix
   and read Observe before any fresh public E2E.

## PAC-121 — Repaired immutable-parent suffix starting

1. **Checkpoint and boundary:** A fresh diagnostic run will use the exact
   Evidence/Architecture bytes from `run_1bec958e41ae4207beb4a7b40149f9c0`,
   real Luna SharedTool and only `tool_semantics[register_member]`.
2. **North-star link:** This is the smallest live semantic handoff required
   before resuming the full natural-language-to-EnvironmentPackage proof.
3. **Trust owner and evidence:** Framework retains exact validation and stops
   before the second tool; Direct LLM alone supplies the two bounded semantic
   drafts. No Agent, Skill, candidate, Judge, Registry or release runs.
4. **Minimality assertion:** The same temporary harness and immutable inputs
   are reused; no product code, route or proof scope changes.
5. **Non-claims:** A suffix pass remains non-adoptable, non-resumable and
   non-publishable diagnostic evidence only.
6. **Next permitted gate:** Safe Observe; pass permits one fresh public Direct
   E2E, while any failure starts a new diagnosis.

## PAC-122 — Suffix exposed remaining SharedTool coordinate echo

1. **Checkpoint and boundary:** Real run
   `run_bb6693c8de48462b992686c4272f0439` stopped at `$.error_policy` after two
   Luna responses failed the exact seven-item source cardinality.
2. **North-star link:** Refusing an incomplete shared contract still protects
   every later executable/Judge/Registry boundary; the new diagnosis removes a
   mechanical model burden instead of weakening acceptance.
3. **Trust owner and evidence:** Direct supplied parseable source proposals;
   framework enforced cardinality and non-publication. No Agent, Skill,
   ToolSemantics, candidate, Judge or Registry ran.
4. **Minimality assertion:** The proposed repair makes one shared policy string
   model-owned and its frozen coordinate binding framework-owned, retaining the
   existing compiled tuple/digest and all consumers.
5. **Non-claims:** The conservative-partition repair is not disproved, but its
   provider proof did not pass; no downstream or product completion is claimed.
6. **Next permitted gate:** Fresh independent critic of the new exact plan;
   no implementation or retry before `allow`.

## PAC-123 — Shared-policy coordinate binding authorized

1. **Checkpoint and boundary:** Independent Terra/max review allowed exact plan
   `sha256:fa1d572a39fe6c1fd23e4e2a1f67e625cf11e06bcc3030dcff51f7a76fd247ca`
   as the smallest coherent source/compiler-to-consumer slice.
2. **North-star link:** Removing a mechanical seven-item echo makes the shared
   semantic contract more reliable without weakening executable/Judge/Registry
   acceptance.
3. **Trust owner and evidence:** Direct authors one group-wide policy; framework
   binds it to frozen members. Per-tool semantic errors remain in later Direct
   ToolSemantics; Agent/candidate authority is unchanged.
4. **Minimality assertion:** No optional override, union, helper, contract
   field, graph, retry, Agent or downstream change is allowed.
5. **Non-claims:** Critic allow and source edits prove no provider, Design,
   Candidate, Judge, Registry, E2E or later-child completion.
6. **Next permitted gate:** Deterministic tests and independent implementation
   check before the same immutable-parent suffix.

## PAC-124 — Shared-policy binding is deterministically green and smaller

1. **Checkpoint and boundary:** The exact allowed source/compiler edit passes
   17 focused SharedTool tests, 207 full tests, 20 Registry/package/firewall
   tests, Ruff, mypy, compileall and diff checks.
2. **North-star link:** A smaller, correctly owned shared-policy source now
   feeds the unchanged compiled contract toward executable Candidate, Judge and
   Registry boundaries.
3. **Trust owner and evidence:** Direct authors one semantic policy string;
   framework only binds it to frozen coordinates. Existing compiled fixtures
   with distinct per-member pairs still pass, proving the global ABI was not
   tightened.
4. **Minimality assertion:** Production code fell from 10,315 to 10,303 lines;
   no field, module, node, edge, route, retry, Skill or compatibility path was
   added.
5. **Non-claims:** Deterministic success proves no Luna response, ToolSemantics,
   complete Design, Candidate, Judge, Registry, E2E or later child.
6. **Next permitted gate:** Fresh independent implementation check before the
   same immutable-parent diagnostic suffix.

## PAC-125 — Shared-policy binding independently allowed

1. **Checkpoint and boundary:** Fresh Terra/max check allowed the exact
   implementation after 113 focused, 29 package/Registry, 207 full and two
   firewall tests plus all static gates passed.
2. **North-star link:** The unchanged compiled shared contract is now ready for
   the smallest true provider/consumer handoff toward an executable package.
3. **Trust owner and evidence:** Direct remains Skill-less and authors policy
   meaning; framework repeats only that exact text across frozen coordinates.
   Distinct compiled fixture policies remain valid, so global ABI/Expand seams
   were not tightened.
4. **Minimality assertion:** Production code is 10,303 lines; graph, route,
   correction bound, Agent, Candidate, Registry and Observe are unchanged.
5. **Non-claims:** Independent static allow proves no Luna suffix, complete
   Design, Candidate, Judge, Registry, E2E or later child.
6. **Next permitted gate:** Run the same immutable-parent SharedTool plus first
   ToolSemantics suffix and read safe Observe.

## PAC-126 — Shared-policy suffix proof starting

1. **Checkpoint and boundary:** The fresh diagnostic will reuse exact parent
   refs `design.world_architecture:8b0f1bcda8f37a24` and
   `design.evidence_graph:8cea941a9168ce53`, invoke real Luna SharedTool and
   only `tool_semantics[register_member]`.
2. **North-star link:** This proves the repaired Direct source and its immediate
   consumer before spending another full E2E run.
3. **Trust owner and evidence:** Framework validates and stops before the
   second tool; no Agent, Skill, candidate, Judge, Registry or release runs.
4. **Minimality assertion:** Same immutable inputs, route, two-call bound and
   temporary harness; no product behavior changes for the proof.
5. **Non-claims:** A pass remains diagnostic, non-resumable, non-adoptable and
   non-publishable.
6. **Next permitted gate:** Observe; pass permits one fresh public Direct E2E,
   failure starts a new diagnosis.

## PAC-127 — Suffix stopped on collapsed text feedback

1. **Checkpoint and boundary:** Real run
   `run_f3a75200f65f4c93b84aa749eadac11e` stopped twice at SharedTool
   `$.ordering`; the safe condition cannot distinguish type, empty text or the
   disclosed 160-code-point limit.
2. **North-star link:** Actionable bounded correction is needed to obtain a
   valid semantic contract without weakening downstream executable/Judge/
   Registry gates.
3. **Trust owner and evidence:** Luna returned parseable JSON; framework
   rejected it and did not publish. No Agent, ToolSemantics, candidate, Judge
   or Registry ran.
4. **Minimality assertion:** The proposed action changes only safe feedback
   precision in the existing validator; acceptance, Prompt shape, ABI, graph,
   retry and model remain unchanged.
5. **Non-claims:** Evidence does not yet establish whether the actual value was
   wrong-type, empty or overlength, and proves no downstream completion.
6. **Next permitted gate:** Fresh independent critic before any validator edit
   or provider retry.

## PAC-128 — Precise Design text correction authorized

1. **Checkpoint and boundary:** Independent Terra/max review allowed exact plan
   `sha256:ce30e40cd25b65758400e27636c3a7df85ea4bb27658fb8fbe9f1a6376b5f669`
   as a common feedback/observability repair with unchanged acceptance.
2. **North-star link:** An actionable one-shot correction helps semantic drafts
   reach the unchanged executable/Judge/Registry gates without relaxing them.
3. **Trust owner and evidence:** Framework identifies only type, emptiness or
   declared bound; it persists no Provider value/actual length and grants no
   new model, Agent or release authority.
4. **Minimality assertion:** Only the existing `_text` rejection messages and
   focused tests change; source shapes, limits, outputs, graph and ABIs remain.
5. **Non-claims:** The allow does not identify the old hidden value and proves
   no Luna response or downstream completion.
6. **Next permitted gate:** Deterministic and independent implementation checks
   before a fresh non-resume same-parent suffix.

## PAC-129 — Precise text correction is deterministically green

1. **Checkpoint and boundary:** The exact feedback-only implementation passes
   21 focused, 211 full and 20 package/Registry/firewall tests plus Ruff, mypy,
   compileall and diff checks.
2. **North-star link:** The existing one-shot correction can now act on exact
   safe causes while all executable/Judge/Registry acceptance remains intact.
3. **Trust owner and evidence:** Framework discloses only type, emptiness or the
   caller's public bound; no hidden text/actual length, model role, Skill,
   candidate or release authority changes.
4. **Minimality assertion:** Production code is 10,318 lines under the 10,320
   ceiling; no field, module, node, route, retry or ABI was added.
5. **Non-claims:** Static success proves no real Luna correction, complete
   Design, Candidate, Judge, Registry, E2E or later child.
6. **Next permitted gate:** Fresh independent implementation allow, then only
   the fresh same-parent suffix and Observe.

## PAC-130 — Precise text correction independently allowed

1. **Checkpoint and boundary:** Fresh Terra/max check allowed the exact
   feedback-only implementation after 211 full and all focused/static gates
   passed at 10,318 production lines.
2. **North-star link:** The existing correction boundary can now help semantic
   drafts reach unchanged executable/Judge/Registry acceptance safely.
3. **Trust owner and evidence:** Framework forwards only exact public condition,
   path and category; rejected Provider value and actual length are absent.
   Direct/Agent roles, routes, Skills and retry authority remain unchanged.
4. **Minimality assertion:** Acceptance, stripped normalization, semantic
   identity, graph, ABI and every caller limit are unchanged.
5. **Non-claims:** Static allow proves no Luna suffix, Design closure,
   Candidate, Judge, Registry, E2E or later child.
6. **Next permitted gate:** Fresh non-resume immutable-parent suffix, then
   immediate Observe.

## PAC-131 — Precise-feedback suffix starting

1. **Checkpoint and boundary:** A fresh run will reuse the same exact Evidence
   and Architecture refs, real Luna SharedTool and only first ToolSemantics.
2. **North-star link:** It tests the live semantic correction handoff before a
   full natural-language-to-EnvironmentPackage run.
3. **Trust owner and evidence:** Framework retains exact two-call bound and
   stops before the second tool; no Agent, candidate, Judge, Registry or
   release executes.
4. **Minimality assertion:** No state adoption/resume, input or harness scope
   change.
5. **Non-claims:** A pass is still non-publishable diagnostic evidence.
6. **Next permitted gate:** Observe; any failure starts a new diagnosis, while
   a pass permits one fresh public Direct E2E.

## PAC-132 — SharedTool-to-first-tool live suffix passed

1. **Checkpoint and boundary:** Diagnostic run
   `run_d1fcdd4d28264a1c965f318211a34582` committed SharedTool after one exact
   280-code-point correction and committed `register_member` ToolSemantics on
   its first call.
2. **North-star link:** The repaired shared semantic contract now reaches its
   immediate per-tool consumer, clearing the smallest gate toward full Design,
   executable Candidate, independent Judge and Registry publication.
3. **Trust owner and evidence:** Two Direct Luna Works passed; framework owned
   correction, validation and artifacts. Observe has no Findings and reports
   `not_published`; no Agent/candidate/Judge/Registry ran.
4. **Minimality assertion:** Exact immutable parents and existing two-call
   topology were used; the harness stopped before tool two.
5. **Non-claims:** This is non-resumable diagnostic evidence, not complete
   Design, Candidate, Judge, Registry, E2E or later-child proof.
6. **Next permitted gate:** Exactly one fresh public Direct E2E from natural
   language to terminal Observe.

## PAC-133 — Fresh public Direct E2E starting

1. **Checkpoint and boundary:** A new public `agent-world generate` request will
   start from natural language and run the normal DesignGraph, CandidateGraph,
   Judge and Registry path without adopting diagnostic state.
2. **North-star link:** Success requires an executable, independently verified,
   immutable published `EnvironmentPackage`, not merely graph progress.
3. **Trust owner and evidence:** Framework controls graph/gates/release; Direct
   LLM handles bounded semantics; Codex Agents use only their named Runtime
   Skills; candidate runs as untrusted process; Judge and Registry remain
   framework-owned.
4. **Minimality assertion:** Same public CLI, config, routes and fixed graphs;
   no test seam, resume, compatibility path or extra retry.
5. **Non-claims:** Prefix progress will not be called E2E success; only terminal
   released Observe plus Registry evidence qualifies.
6. **Next permitted gate:** Read Observe at the first terminal; failure starts a
   new diagnosis, release advances Direct handoff only.

## PAC-134 — Public E2E stopped on exact SharedTool relation bound

1. **Checkpoint and boundary:** Public run
   `run_358570ae622f423f9a7d0607717bfc3e` passed ResearchPlan,
   ResearchAcquire, ResearchSynthesis and WorldArchitecture, then failed both
   Luna SharedTool attempts at `$.ordering` over 160 code points.
2. **North-star link:** Honest rejection prevents incomplete shared semantics
   from reaching Candidate/Judge/Registry, but the derived compactness cap now
   blocks the natural-language-to-executable path.
3. **Trust owner and evidence:** Upstream Agent/framework/Direct Works passed;
   framework applied the exact 160 correction and did not publish. No Skill was
   present at SharedTool and no candidate process ran.
4. **Minimality assertion:** The proposed repair changes only symmetric
   ordering/compensation item bounds to 500; roles, arrays, compiler type, graph,
   retries and downstream ABI remain.
5. **Non-claims:** E2E is rejected; no complete Design, Candidate, Judge,
   Registry or later child is proven.
6. **Next permitted gate:** Fresh independent critic of the exact local plan;
   no implementation/provider retry before `allow`.

## PAC-135 — Relation-bound revision 1 blocked as too broad

1. **Checkpoint and boundary:** Independent critic blocked the plan because the
   real E2E proves only ordering overlength, not compensation overlength.
2. **North-star link:** Narrowing prevents a node-local unblock from silently
   widening unrelated semantics used by Candidate/Registry and future Expand.
3. **Trust owner and evidence:** Direct still owns both relation meanings;
   framework may change only an evidence-supported declared acceptance bound.
4. **Minimality assertion:** Feedback requests plan clarification only; no code,
   test or provider call is authorized.
5. **Non-claims:** Block proves no repair or downstream progress.
6. **Next permitted gate:** Final revision 2/2, then fresh independent review.

## PAC-136 — Final ordering-only bound plan awaiting critic

1. **Checkpoint and boundary:** Revision 2 narrows to ordering 500 only,
   retains compensation 160 and names the exact failed E2E parents/shard/tool.
2. **North-star link:** It removes only the demonstrated blocker while keeping
   the executable/Judge/Registry contract stable.
3. **Trust owner and evidence:** Semantic revision rotates; Source/Package/
   Registry ABI versions and Direct/Agent/framework ownership do not change.
4. **Minimality assertion:** Maximum is 4,000 ordering code points per group;
   any `>500` real failure starts a new diagnosis. No implicit widening.
5. **Non-claims:** Final plan is not implementation, provider proof, E2E,
   Expand or Consumer proof.
6. **Next permitted gate:** Fresh exact-digest Terra/max critic; only `allow`
   permits implementation.

## PAC-137 — Ordering-only bound repair authorized

1. **Checkpoint and boundary:** Fresh independent review allowed final plan
   `sha256:377db0d17fe74c459112f40a749fe702e5a24766d777296ba99647c25cc977d7`
   after all revision-1 blocks were closed.
2. **North-star link:** Only the demonstrated ordering blocker is widened so
   full Design can proceed toward executable Candidate/Judge/Registry.
3. **Trust owner and evidence:** Direct retains ordering meaning; framework
   changes only its declared bound and validation. Compensation stays 160;
   Agent/candidate roles are unchanged.
4. **Minimality assertion:** Ordering remains 0..8 x 500; any `>500` terminal
   starts a new diagnosis. No ABI, node, route, retry or later-child code.
5. **Non-claims:** Allow/code edits prove no provider suffix, E2E, Expand or
   Consumer behavior.
6. **Next permitted gate:** Deterministic and independent implementation checks
   before the exact latest-parent suffix.

## PAC-138 — Ordering-only bound repair is deterministically green

1. **Checkpoint and boundary:** The exact implementation passes 19 SharedTool,
   212 full and 31 firewall/package/Registry tests plus Ruff, mypy, compileall
   and diff checks.
2. **North-star link:** The demonstrated ordering blocker is removed while the
   unchanged compiled contract still feeds executable/Judge/Registry paths.
3. **Trust owner and evidence:** Direct authors ordering; framework enforces
   500. Compensation remains 160 and all Agent/candidate ownership is intact.
4. **Minimality assertion:** Production code stays 10,318 lines; no field,
   module, graph, route, retry or ABI is added.
5. **Non-claims:** Static success proves no real latest-parent suffix, E2E,
   Expand or Consumer behavior.
6. **Next permitted gate:** Fresh independent implementation check, then only
   the exact run-358 latest-parent suffix.

## PAC-139 — Ordering-only implementation independently allowed

1. **Checkpoint and boundary:** Fresh independent check allowed the exact
   revision-2 implementation after 212 full and all static/package gates passed
   at 10,318 production lines.
2. **North-star link:** The SharedTool producer is ready for the exact real
   consumer proof before another full EnvironmentPackage attempt.
3. **Trust owner and evidence:** Ordering alone is 500; compensation remains
   160. Direct is Skill-less, framework owns validation, and all compiled/
   package/Registry/Expand seams remain unchanged.
4. **Minimality assertion:** No field, ABI version, graph, route, retry, Agent or
   later-child code changed.
5. **Non-claims:** Static allow proves no latest-parent suffix, E2E, Judge,
   Registry release, Repair, Expand or Consumer.
6. **Next permitted gate:** Exact run-358 parent suffix and immediate Observe.

## PAC-140 — Latest-parent ordering suffix starting

1. **Checkpoint and boundary:** Fresh Luna will use exact run-358 Evidence
   `a6a8...b62fa` and Architecture `84fe...98506`, execute SharedTool
   `[1-2-3-4-5-6]`, then only `tool_semantics[register_member]`.
2. **North-star link:** It tests the repaired relation contract against the
   actual public-E2E input before another complete run.
3. **Trust owner and evidence:** Framework stops before tool two and retains all
   Work/Artifact/release authority; no Agent/candidate/Judge/Registry executes.
4. **Minimality assertion:** Fresh non-resume diagnostic, same route and
   two-call bound.
5. **Non-claims:** Pass remains non-adoptable and non-publishable.
6. **Next permitted gate:** Observe; pass permits one fresh public Direct E2E,
   failure starts a new diagnosis.

## PAC-141 — Latest-parent suffix exposed policy bound

1. **Checkpoint and boundary:** Run
   `run_d9fe033caff941c1a7bc385f019efaf3` used exact run-358 parents and failed
   both Luna SharedTool calls only at group-wide policy over 280 code points.
2. **North-star link:** The unchanged strict compiler prevented incomplete
   shared semantics from reaching executable/Judge/Registry paths.
3. **Trust owner and evidence:** Direct authored policy; framework applied the
   exact 280 correction and did not publish. No Agent, ToolSemantics, candidate,
   Judge or Registry ran.
4. **Minimality assertion:** New plan changes only policy 280->500; ordering500,
   compensation160, graph, retry, compiled/package ABI and roles remain.
5. **Non-claims:** No suffix/E2E/Registry/later-child success is claimed.
6. **Next permitted gate:** Fresh independent critic before implementation or
   another provider call.

## PAC-142 — Policy-bound revision 1 blocked on value/digest precision

1. **Checkpoint and boundary:** Independent critic blocked imprecise claims
   that downstream digest values stay unchanged and that source growth equals
   persisted growth.
2. **North-star link:** Honest lineage requires recomputed policy-derived
   digests to propagate through ToolDraft/Design/Package/Registry.
3. **Trust owner and evidence:** Framework owns deterministic recomputation;
   Direct owns the fresh policy value. Interfaces remain framework-owned.
4. **Minimality assertion:** Block changes plan wording only, not code scope.
5. **Non-claims:** 500 remains unproven until the real suffix.
6. **Next permitted gate:** Final revision 2/2 and fresh critic.

## PAC-143 — Final policy-bound plan awaiting critic

1. **Checkpoint and boundary:** Revision 2 states that values/digests may change
   while digest recipe, fields and ABI versions remain; compiled policy text is
   bounded at 8 x 500 = 4,000 code points per group.
2. **North-star link:** This preserves traceable semantics through
   ToolDraft/Design/Package/Registry rather than pretending byte equality.
3. **Trust owner and evidence:** Direct authors value; framework recomputes all
   dependent digests and retains release authority.
4. **Minimality assertion:** Code scope remains only policy 280->500 plus
   card/tests; no graph, retry, Agent or child work.
5. **Non-claims:** 500 is a hypothesis pending exact-parent live proof.
6. **Next permitted gate:** Fresh exact-digest Terra/max allow.

## PAC-144 — Policy-bound revision 2 independently allowed

1. **Checkpoint and boundary:** Independent Terra/max critic allowed exact plan
   `1253f873...e99f8`; implementation is limited to SharedTool source policy
   280->500, its visible card, and focused tests.
2. **North-star link:** This removes a demonstrated Design suffix blocker while
   retaining the path toward executable, independently judged and publishable
   EnvironmentPackage output.
3. **Trust owner and evidence:** Direct LLM owns the policy meaning; framework
   owns the 500-code-point bound, frozen-member binding, digest recomputation,
   graph state, Judge, Registry and Observe. No Agent or candidate process is
   involved in this node.
4. **Minimality assertion:** No field, ABI, route, model, Skill, retry, helper,
   graph node or later-child implementation is allowed.
5. **Non-claims:** The new bound, remaining ToolSemantics, Candidate, Judge,
   Registry, E2E, Repair, Expand and Consumer are still unproven.
6. **Next permitted gate:** Exact implementation followed by deterministic
   checks and an independent implementation review.

## PAC-145 — Policy-bound implementation is deterministically green

1. **Checkpoint and boundary:** The exact policy-only implementation passes 55
   focused Design tests, 213 full tests, 31 firewall/release tests, Ruff,
   mypy, compileall and diff checks.
2. **North-star link:** Shared policy can now carry complete bounded semantics
   into ToolSemantics without weakening the executable/Judge/Registry path.
3. **Trust owner and evidence:** Direct LLM still authors only policy text;
   framework enforces 500, performs frozen-member expansion and recomputes all
   value-derived digests. Agent and candidate boundaries are unchanged.
4. **Minimality assertion:** Production Python remains 10,318 lines; only two
   product literals, the matching node card and focused tests changed.
5. **Non-claims:** Deterministic success does not prove Luna sufficiency,
   remaining Design, Candidate, Judge, Registry, E2E or later children.
6. **Next permitted gate:** Fresh independent implementation check, then only
   the exact run-358 same-parent suffix.

## PAC-146 — Implementation check blocked only on untracked-file provenance

1. **Checkpoint and boundary:** Independent reviewer confirmed current behavior
   and role boundaries but blocked because plain Git diff omits the untracked
   cleanroom files, so it lacked an explicit delta against PAC-139.
2. **North-star link:** The product behavior remains green; this checkpoint
   preserves honest change provenance before a real model claim.
3. **Trust owner and evidence:** No runtime owner changed. The block concerns
   development evidence only, not Direct LLM, Agent, candidate or framework
   execution.
4. **Minimality assertion:** No code change follows this block. Re-review gets
   only the exact prior state and authorized before/after list.
5. **Non-claims:** No independent allow and no live suffix/E2E success yet.
6. **Next permitted gate:** Same independent reviewer re-checks the explicit
   policy-only delta and returns allow/block.

## PAC-147 — Policy-bound implementation independently allowed

1. **Checkpoint and boundary:** The same read-only Terra/max reviewer allowed
   the exact implementation after receiving PAC-139 as the explicit base and
   checking the five authorized before/after sites.
2. **North-star link:** SharedTool is ready for the smallest real producer-to-
   consumer proof on the actual failed E2E parents.
3. **Trust owner and evidence:** Direct LLM authors policy meaning without
   Skill/tools/workspace; framework owns bound, validation, frozen binding,
   digests and Work. Agent/candidate/Judge/Registry boundaries remain intact.
4. **Minimality assertion:** 55 focused tests and static checks pass at 10,318
   production lines; no runtime scope expanded.
5. **Non-claims:** No real Luna suffix, E2E, release or later-child success is
   claimed.
6. **Next permitted gate:** Fresh exact-parent suffix through only
   `tool_semantics[register_member]`, then immediate Observe.

## PAC-148 — Exact-parent policy suffix starting

1. **Checkpoint and boundary:** Fresh Luna Work will use only run-358 Evidence
   `a6a8...b62fa` and Architecture `84fe...98506`, execute SharedTool
   `[1-2-3-4-5-6]`, then only `tool_semantics[register_member]`.
2. **North-star link:** This tests whether the corrected semantic producer can
   feed its immediate consumer before another full environment generation.
3. **Trust owner and evidence:** Both calls are Direct LLM with no Skill/tools/
   workspace; framework owns correction, compile and stop. Agent, candidate,
   Judge and Registry will not execute.
4. **Minimality assertion:** Fresh non-resume diagnostic with existing routes,
   two-call local correction ceiling and no release path.
5. **Non-claims:** A pass is non-adoptable and non-publishable; it proves no E2E
   or later child.
6. **Next permitted gate:** Read Observe immediately; pass permits one fresh
   public Direct E2E, failure starts a new diagnosis.

## PAC-149 — Exact-parent policy suffix passed

1. **Checkpoint and boundary:** Run `run_83585dd10e854697b4ba5b83f9a41419`
   committed SharedTool and `register_member` ToolSemantics in one Luna call
   each on the exact run-358 parents.
2. **North-star link:** A real semantic producer now feeds its first real
   consumer, unblocking one complete need-to-package attempt.
3. **Trust owner and evidence:** Observe shows two passed Direct LLM Work
   records, exact parent/output IDs, no Findings and no release. Framework kept
   all compile/commit authority; no Agent/candidate/Judge/Registry ran.
4. **Minimality assertion:** The bounded harness stopped before tool two and
   created no resumable/adoptable shortcut.
5. **Non-claims:** Remaining Design, Candidate, Judge, Registry, E2E and later
   children remain unproven.
6. **Next permitted gate:** One fresh public Direct E2E and immediate Observe.

## PAC-150 — Fresh public Direct E2E starting

1. **Checkpoint and boundary:** A new CLI `generate` run starts from only a
   natural-language community tool-lending need and no adopted diagnostic Work.
2. **North-star link:** This is the required proof path toward an evidence-
   grounded executable, independently verified and publishable package.
3. **Trust owner and evidence:** Framework owns graph/routes/contracts/gates;
   Direct LLM owns bounded Design semantics; research/codegen/verifier Agents
   use their exact Skills/tools; candidate runs untrusted; Judge and Registry
   retain release authority.
4. **Minimality assertion:** Existing two graphs and configured backends only;
   no special environment ID, replay or compatibility path.
5. **Non-claims:** Start is not success; every reached node and terminal must be
   verified through Observe.
6. **Next permitted gate:** Read terminal output and Observe. Any failure starts
   the normal diagnosis/critic loop before another modification.

## PAC-151 — Public E2E stopped at final ToolSemantics format boundary

1. **Checkpoint and boundary:** Run `run_dc28dcded7fe49ce9a2d9a017511831d`
   rejected at `tool_semantics[route_tool_to_maintenance]` with
   `direct_response_not_json`; seven sibling ToolSemantics passed.
2. **North-star link:** Strict parsing correctly prevented malformed semantic
   source from reaching ModelingGate or executable/package/release paths.
3. **Trust owner and evidence:** Observe records Research Agent/framework/
   Agent, Architecture/SharedTool and seven Direct shards passed; the final
   Direct shard failed with no output, one blocking Finding and no release.
   No Candidate, Judge or Registry executed.
4. **Minimality assertion:** No code or prompt change is authorized from the
   symptom. Diagnosis first tests the exact failed shard and existing feedback
   behavior.
5. **Non-claims:** Direct E2E, executable candidate, independent Judge,
   Registry, Repair, Expand and Consumer remain unproven.
6. **Next permitted gate:** Exact read-only failure reconstruction and one
   bounded shard replay, then a Diagnosis Record and repair-plan critic.

## PAC-152 — Direct parse-feedback diagnosis and plan awaiting critic

1. **Checkpoint and boundary:** Exact failed-input replay parsed in one Luna
   call; code tracing shows `correctable=False` discards the node's declared
   second call on malformed JSON.
2. **North-star link:** Returning actionable bounded feedback helps arbitrary
   need-to-environment generation survive a transient format error without
   weakening independent validation or release gates.
3. **Trust owner and evidence:** Direct LLM owns the corrected semantic object;
   framework owns parse rejection, root-object correction, two-call ceiling,
   compiler, Work and release. Agent/candidate behavior is out of scope.
4. **Minimality assertion:** Plan changes one error classification and focused
   tests; no prompt, parser, model, fallback, node, graph or child code.
5. **Non-claims:** No corrected live transaction, E2E, Candidate, Judge,
   Registry or later-child success is claimed.
6. **Next permitted gate:** Fresh exact-digest cross-layer critic; only allow
   permits implementation.

## PAC-153 — Root-object correction plan blocked

1. **Checkpoint and boundary:** Independent critic blocked plan
   `f1cbad92...99559`: generic root JSON errors are mechanical framework/output-
   contract failures and may not spend semantic correction authority.
2. **North-star link:** Preserving this distinction prevents retry behavior and
   incomplete usage lineage from being hidden inside semantic generation.
3. **Trust owner and evidence:** Framework still owns strict parsing and
   terminal rejection. No Direct, Agent or candidate authority changes.
4. **Minimality assertion:** No code followed the block. One profile-matched
   probe tests provider-native JSON-object enforcement before final revision.
5. **Non-claims:** No response-mode support, repair, E2E or later-child success
   is currently established.
6. **Next permitted gate:** Exact failed-input Luna/8317 response-format probe,
   then final plan revision 2/2 and a fresh critic.

## PAC-154 — Mechanical JSON response plan awaiting final critic

1. **Checkpoint and boundary:** Both configured Direct routes accepted
   `json_object` on the exact failed ToolSemantics input; revision 2 changes
   only the fixed request field.
2. **North-star link:** Mechanically reliable structured source reduces
   arbitrary need-to-environment failure without weakening typed validation or
   release authority.
3. **Trust owner and evidence:** Framework owns response mode and strict parse;
   Direct LLM owns semantics. Root format failure stays terminal; Agent and
   candidate process boundaries are unchanged.
4. **Minimality assertion:** One product dictionary field plus one request-body
   test; no SDK, prompt, retry, parser, node, graph or child code.
5. **Non-claims:** Probe compatibility is not compiler, E2E, release or later-
   child success.
6. **Next permitted gate:** Fresh exact-digest independent critic; only allow
   permits implementation.

## PAC-155 — Mechanical JSON response plan independently allowed

1. **Checkpoint and boundary:** Independent critic allowed exact plan
   `b71cce77...e1e94c`; both fixed routes support the requested JSON-object
   wire contract.
2. **North-star link:** More reliable typed Direct source advances a complete
   need-to-executable-package run without relaxing any compiler or release gate.
3. **Trust owner and evidence:** Framework owns the hardcoded response mode and
   strict parser; Direct LLM owns semantics. Root errors stay terminal; Agent,
   candidate, Judge and Registry authority is unchanged.
4. **Minimality assertion:** Only one request-body field and one adapter test are
   authorized; no capability negotiation or compatibility code.
5. **Non-claims:** Implementation, product-backend proof, E2E and later children
   remain unproven.
6. **Next permitted gate:** Exact implementation and deterministic checks,
   followed by independent implementation review.

## PAC-156 — Mechanical JSON response implementation is green

1. **Checkpoint and boundary:** The one-field implementation passes 76 focused,
   214 full and 31 firewall/release tests plus Ruff, mypy, compileall and diff.
2. **North-star link:** Direct semantic source is now mechanically requested as
   JSON before strict compilation toward an executable package.
3. **Trust owner and evidence:** Framework hardcodes `json_object`; Direct LLM
   authors semantics. Malformed output remains terminal and no Agent/candidate/
   Judge/Registry behavior changed.
4. **Minimality assertion:** Production Python is 10,319 lines; one product line
   and one decoded-body test were added.
5. **Non-claims:** Static success proves no actual product-backend call, Design
   completion, E2E, release or later-child behavior.
6. **Next permitted gate:** Fresh independent implementation check, then exact
   product-backend failed-input proof.

## PAC-157 — SDK and user-wish Feedback plan supersedes the one-field patch

1. **Checkpoint and boundary:** The prior one-field response-format result is
   incomplete: Direct still uses raw `urllib`, carries an explicit 4,096-token
   output cap and cannot turn an attributable malformed model response into the
   node's one authorized Feedback turn. Plan `9267a585...e59710` replaces that
   boundary with the official SDK and an explicit complete-replacement user
   instruction.
2. **North-star link:** Reliable, self-correctable Direct proposals advance an
   arbitrary natural-language request toward a real executable and independently
   verified EnvironmentPackage without weakening validation or release.
3. **Trust owner and evidence:** Framework owns SDK configuration, strict parse,
   feedback facts, one-correction ceiling and release. Direct owns semantic
   generation; Agent keeps one Skill; Candidate/Judge/Registry are unchanged.
   Public Observe and exact-input no-cap SDK probes are the current evidence.
4. **Minimality assertion:** One SDK adapter and one shared feedback rendering
   rule; no feedback service, Critic node, extra retry, graph change, validator
   rewrite or compatibility path.
5. **Non-claims:** The plan and provider probes do not prove product adapter,
   Design completion, Candidate, Judge, Registry, E2E or later children.
6. **Next permitted gate:** Fresh independent cross-layer critic at exact digest
   `sha256:9267a5855fda23918e8aa520406938b3046470d47ffe008a67a18f3dc8e59710`;
   only `allow` permits implementation.

## PAC-158 — Context/Feedback research and final Direct repair revision

1. **Checkpoint and boundary:** Official documentation, primary papers, three
   independent research records and one project-independent synthesis now
   distinguish Context, Prompt, Skill, tool observation, Feedback, Observe,
   local correction, transport fallback and workflow Repair. Revision 2/2
   applies only the Direct SDK/evidence/feedback findings.
2. **North-star link:** A model-visible contract plus actionable next-user-turn
   Feedback should help an arbitrary natural-language need survive stochastic
   proposal failure on its path to an executable, independently judged and
   publishable `EnvironmentPackage`.
3. **Trust owner and evidence:** Framework still owns facts, correction
   admission, validation and release; Direct owns only a replacement semantic
   proposal. OpenViking Resources are low-authority development references,
   not runtime context or product memory. The failed public Observe terminal
   and static adapter/validator code remain the product evidence.
4. **Minimality assertion:** The plan permits one official SDK adapter, one
   bounded evolution of the existing correction packet and one deterministic
   Feedback renderer. It explicitly forbids a context manager, feedback
   service, validator Agent, memory/RAG layer, prompt platform, new node or
   compatibility path.
5. **Non-claims:** Research, Resource ingestion, plan revision and SDK probes
   prove no corrected product call, complete Design, Candidate, Judge,
   Registry, E2E, Expand or Consumer/SFT/RL result.
6. **Next permitted gate:** Fresh independent cross-layer critic at exact
   digest
   `sha256:07cee5d8e3746bcf4417777a4a0846678e6b6a0987f9848826befd303d92cc63`;
   only `allow` permits implementation.

## PAC-159 — New minimal Direct format-Feedback lineage

1. **Checkpoint and boundary:** The mixed SDK/Feedback revision was blocked for
   overdesign. A new 78-line plan keeps only the official Direct SDK and one
   first-attempt format Feedback for the observed `direct_response_not_json`.
2. **North-star link:** Recovering one attributable malformed Direct proposal
   helps the same natural-language request continue toward an executable,
   independently judged and publishable package without weakening any later
   Gate.
3. **Trust owner and evidence:** Framework owns the eligible
   `stop + nonempty + non-JSON` predicate, one-turn admission and strict
   revalidation. Direct owns only the complete replacement. The real failed
   Observe terminal and official-SDK route probes are the current evidence.
4. **Minimality assertion:** Multi-issue aggregation, Agent feedback changes,
   compiler rewrites and generic context/evidence services are deleted. No
   fallback is added for refusal, transport, truncation, empty results or a
   second malformed answer.
5. **Non-claims:** This plan proves no corrected model call, Design, Candidate,
   Judge, Registry, E2E, Expand or Consumer result.
6. **Next permitted gate:** Fresh independent critic at exact digest
   `sha256:12fc5ef06c9d829acd7b70a6d4f13dc4151f9027919cec838eef60e2bb07e621`;
   only `allow` permits implementation.

## PAC-160 — Minimal Direct format Feedback independently allowed

1. **Checkpoint and boundary:** Independent critic allowed exact plan
   `12fc5ef0...e621` for the current Direct-only format terminal.
2. **North-star link:** One attributable malformed proposal may now receive one
   precise user-style correction before the unchanged executable/Judge/release
   path.
3. **Trust owner and evidence:** Framework owns SDK transport, eligibility,
   evidence and the two-call ceiling; Direct owns only the replacement. The
   rejected answer remains ephemeral and untrusted.
4. **Minimality assertion:** Agent feedback, multi-issue packets, compiler
   aggregation, new nodes/services and downstream ABIs are explicitly outside
   the allow.
5. **Non-claims:** No code, test, real SDK node, E2E or release has yet passed
   under this digest.
6. **Next permitted gate:** Dispatch one bounded implement worker, then an
   independent implementation check before any real provider proof.

## PAC-161 — Minimal Direct SDK/format Feedback implementation is green

1. **Checkpoint and boundary:** The exact allowed Direct-only implementation
   replaces raw HTTP with the official SDK and adds one ephemeral format
   Feedback handoff. Full deterministic suite reports 223 passed.
2. **North-star link:** The current malformed Direct proposal can now receive
   one precise complete-replacement request before the unchanged Design,
   Candidate, Judge and release path.
3. **Trust owner and evidence:** Framework owns strict eligibility,
   operation evidence and the two-attempt ceiling. Direct owns only generated
   content; malformed text remains in memory and is not a persisted fact.
4. **Minimality assertion:** Production code changed from 10,319 to 10,424
   lines (+105), chiefly replacing transport and exposing the local handoff.
   No Agent, Candidate, graph topology, Observe schema or generic Feedback
   service changed.
5. **Non-claims:** Deterministic tests establish no real SDK call, corrected
   Luna node, full Design, Candidate, Judge, Registry, E2E or release.
6. **Next permitted gate:** Fresh independent implementation check against the
   exact allow before any real provider proof.

## PAC-162 — Direct SDK/format Feedback implementation independently allowed

1. **Checkpoint and boundary:** Independent check allowed the exact
   implementation after 122 focused and 225 full tests plus Ruff, mypy and
   compileall.
2. **North-star link:** The smallest real proof may now test whether one Direct
   semantic proposal can progress toward the still-unproven executable package.
3. **Trust owner and evidence:** Static evidence confirms framework-owned SDK,
   safe operation facts, one Feedback turn, strict revalidation and no raw
   persistence.
4. **Minimality assertion:** +105 production lines were judged proportionate;
   no generic Feedback, Agent, graph, Candidate, Judge, Registry or Observe
   expansion was found.
5. **Non-claims:** Static allow is not a provider call, Design completion, E2E
   or release.
6. **Next permitted gate:** One profile-matched Luna Direct ToolSemantics node,
   followed immediately by Observe. Only a pass permits fresh public E2E.

## PAC-163 — Official-SDK ToolSemantics true boundary passed

1. **Checkpoint and boundary:** Exact frozen
   `route_tool_to_maintenance` input passed one real Luna call through the
   official SDK and committed one typed ToolSemantics Artifact.
2. **North-star link:** The previously blocked Direct leaf now advances an
   arbitrary need toward the still-required executable, independently judged
   and publishable package.
3. **Trust owner and evidence:** Framework retained Prompt-only Direct
   isolation, strict compilation, model/usage evidence, Work/Artifact and
   release authority. Observe reports no Finding and no publication.
4. **Minimality assertion:** The provider passed on attempt one; no extra
   Feedback, fallback, Agent, Skill, Candidate or downstream node ran.
5. **Non-claims:** This does not prove live malformed-output recovery, full
   Design, Candidate, Judge, Registry, E2E, Expand or Consumer/SFT/RL.
6. **Next permitted gate:** One fresh public natural-language Direct E2E,
   followed immediately by Observe; any new terminal returns to diagnosis.

## PAC-164 — Fresh public Direct E2E stopped at ToolSemantics Feedback

1. **Checkpoint and boundary:** Fresh public run
   `run_bb8b2474bfd34507b1b73f7856c77ee3` passed Research, Architecture,
   SharedToolSemantics and two ToolSemantics shards, then rejected
   `tool_semantics[reserve_tool]`; nothing was published.
2. **North-star link:** The run advanced a new natural-language need through
   real evidence and semantic generation, but did not produce the executable,
   independently judged and immutable Registry package required for completion.
3. **Trust owner and evidence:** Framework safely recorded exact issue A on
   proposal one and different issue B on proposal two. Luna completed both
   calls; Direct had no Skill/tools/workspace and release stayed framework-owned.
4. **Minimality assertion:** The Diagnosis proposes only a correct user-style
   Direct Feedback handoff and one ToolSemantics-only strict-progress allowance.
   It excludes aggregation, services, fallback chains, new nodes and validator
   relaxation.
5. **Non-claims:** A -> B progress is not a successful node, Design, Candidate,
   Judge, Registry, E2E, Expand or Consumer result.
6. **Next permitted gate:** Independent cross-layer review of the exact new
   plan digest; no production edit or retry before `allow`.

## PAC-165 — ToolSemantics Feedback repair independently allowed

1. **Checkpoint and boundary:** A fresh Terra critic allowed exact plan
   `a8431783...d241e9` for the failed Direct ToolSemantics correction boundary.
2. **North-star link:** One uncommitted tool-semantics proposal may now make a
   final bounded self-revision toward the complete Design still required before
   executable Candidate, independent Judge and Registry publication.
3. **Trust owner and Artifact effect:** Framework alone owns A-to-B comparison,
   the three-proposal ceiling, validation and commit. Direct sees only the same
   frozen task, its ephemeral prior answer and safe user Feedback; no Artifact
   or downstream contract changes.
4. **No-hybrid assertion:** The approved path remains the cleanroom
   DesignGraph and official Direct SDK adapter; no legacy authority is added.
5. **Evidence:** Independent decision
   `cross-layer-review-a8431783-tool-feedback-progress.md` verifies the exact
   digest and ToolSemantics-only restriction. This is review evidence, not a
   deterministic or real-execution pass.
6. **Non-claims and remaining risk:** No implementation, corrected live leaf,
   full Design, Candidate, Judge, Registry, E2E, Expand or Consumer result is
   yet proven. Luna may still terminate safely on the final proposal.
7. **Bad-case effect:** The allowed change addresses issue A becoming issue B
   and then being discarded solely by a hard two-proposal ceiling; identical
   issues and all format/transport paths still stop.
8. **Next permitted gate:** One bounded implementation, deterministic suite and
   independent implementation check before the exact-parent live leaf proof.

## PAC-166 — Minimal ToolSemantics Feedback repair implemented

1. **Checkpoint and boundary:** The exact allowed Direct correction change is
   implemented in the existing Design transaction and GraphRunner only.
2. **North-star link:** A still-uncommitted ToolSemantics proposal can now use
   one proven-progress final revision before the unchanged Design, executable
   Candidate, independent Judge and Registry path.
3. **Trust owner and Artifact effect:** Framework alone admits A-to-B progress
   and caps the transaction at three proposals. Direct receives the prior
   ephemeral proposal plus user Feedback and still cannot commit, route, judge
   or release. Accepted Artifact/Work/Observe shapes are unchanged.
4. **No-hybrid assertion:** The implementation remains inside cleanroom
   `agent_world/graph.py` and `agent_world/design.py`; it adds no legacy import
   or compatibility path.
5. **Evidence:** The implement worker reports 100 focused tests passed and Ruff
   green. These are deterministic regression facts only.
6. **Non-claims and remaining risk:** No independent whole-diff check, real
   Luna correction, complete Design, Candidate, Judge, Registry, E2E, Expand or
   Consumer result is yet proven.
7. **Bad-case effect:** Same issue and any format/transport path remain bounded;
   only exact parsed ToolSemantics A-to-B may receive the final user Feedback.
8. **Next permitted gate:** Fresh independent `trellis-check`, then the exact
   immutable-parent `reserve_tool` real boundary only if it allows.

## PAC-167 — ToolSemantics Feedback implementation independently allowed

1. **Checkpoint and boundary:** Independent check allowed the exact existing
   GraphRunner/Direct Feedback implementation after its self-contained review.
2. **North-star link:** The smallest real boundary may now test whether the
   blocked tool semantic can advance the natural-language request toward the
   still-unproven executable, judged and published package.
3. **Trust owner and Artifact effect:** Static review confirms framework-owned
   A-to-B admission and unchanged successful ToolSemantics Artifact/Work shape;
   Direct remains proposal-only and no rejected answer/Feedback is durable.
4. **No-hybrid assertion:** Legacy firewall passed; the reviewed files add no
   legacy import or compatibility route.
5. **Evidence:** 100 focused and 233 full tests, 2 firewall tests, Ruff format
   and check, mypy over 13 source files, and compileall all passed.
6. **Non-claims and remaining risk:** These checks prove no real Luna turn,
   Design completion, Candidate, Judge, Registry, E2E, Expand or Consumer path.
7. **Bad-case effect:** Deterministic regressions prove A-to-B can reach a
   bounded third proposal while A-to-A, any format path and any fourth call are
   closed.
8. **Next permitted gate:** Run only the exact old-run `reserve_tool` parents
   through real Luna and immediately inspect safe Observe.

## PAC-168 — Exact-parent reserve_tool proof starting

1. **Checkpoint and boundary:** A diagnostic, non-resume, non-adoptable proof is
   starting only at `design/tool_semantics[reserve_tool]`.
2. **North-star link:** It tests the current Design leaf needed before the
   request can reach executable Candidate, independent Judge and Registry.
3. **Trust owner and Artifact effect:** The framework reuses three exact
   immutable parent bytes, owns validation/attempts/commit and cannot release
   from this harness. Luna owns only the replacement semantic proposal.
4. **No-hybrid assertion:** The selected path is current `DesignExecutor`,
   `GraphRunner` and official Direct SDK; the legacy firewall is green.
5. **Evidence:** Parent digests and the before/after falsifiable claim are frozen
   in `tool-semantics-feedback-progress-live-proof.md` before dispatch.
6. **Non-claims and remaining risk:** The run may pass first call, use bounded
   Feedback, or stop safely; none is full Design or product E2E evidence.
7. **Bad-case effect:** This directly exercises the previously failing frozen
   input without re-running Research or hand-editing a proposal.
8. **Next permitted gate:** Execute once, read Observe immediately, and either
   permit fresh public E2E on pass or start a new diagnosis on failure.

## PAC-169 — Exact-parent reserve_tool stopped on repeated format failure

1. **Checkpoint and boundary:** Diagnostic run
   `run_5d7bd3a844d4458daa56670f4c0003b9` stopped at the exact Direct
   `reserve_tool` leaf after two format-invalid Luna answers.
2. **North-star link:** The required ToolSemantics Artifact did not commit, so
   downstream Design, executable Candidate, Judge and Registry correctly stayed
   closed.
3. **Trust owner and Artifact effect:** Framework sent one format Feedback,
   enforced no third format call, committed one failed Work/Finding and retained
   release authority; no model content became an Artifact.
4. **No-hybrid assertion:** Exact current cleanroom parents and SDK path were
   used; legacy firewall remained green.
5. **Evidence:** Observe reports two attempt refs, two measured Luna operation
   refs, one safe failure ref, one blocking Finding and
   `release=not_published`.
6. **Non-claims and remaining risk:** Parsed-semantic A-to-B was not exercised;
   the non-JSON structural subtype is still unknown. No full Design, Candidate,
   Judge, Registry, E2E, Expand or Consumer result exists.
7. **Bad-case effect:** The new policy prevented format failure from borrowing
   the ToolSemantics third semantic proposal; repeated format error stopped
   exactly as designed.
8. **Next permitted gate:** One no-commit exact-input closed-shape diagnostic;
   then write and independently review any repair plan suggested by its result.

## PAC-170 — Actionable Direct Feedback repair allowed

1. **Checkpoint and boundary:** The failed exact-parent ToolSemantics proof was
   re-diagnosed at the private Direct parser-to-Feedback handoff; independent
   Terra review allowed plan digest
   `d94bba5476f326f34778c0cff4b602fed697e9cd09c482bc258bdb59e4b35f90`.
2. **North-star link:** More actionable Feedback may let the blocked
   ToolSemantics leaf produce the unchanged Artifact required before Design,
   executable Candidate, independent Judge and Registry publication.
3. **Trust owner and Artifact effect:** Framework may preserve one safe closed
   parse condition and turn it into the existing next-user correction. Direct
   still only proposes; Artifact, retry, Judge and release authority remain
   unchanged.
4. **No-hybrid assertion:** The allowed files are the cleanroom Direct SDK
   adapter, Design executor and focused tests/spec guidance; no legacy control
   import or compatibility route is authorized.
5. **Evidence:** Two real Luna responses ended `stop`; the current generic
   Feedback discarded actionable parser distinctions. The critic classified
   the repair local and `allow`.
6. **Non-claims and remaining risk:** The exact malformed subtype is still
   unknown. No Design completion, Candidate, Judge, Registry, E2E, Repair,
   Expand or Consumer/SFT/RL is proven.
7. **Bad-case effect:** The repair must prevent a recipient from receiving only
   an error label when framework has a safely disclosable correction. It must
   not accept malformed output or add another call.
8. **Next permitted gate:** Implement only the allowed parser-to-actionable-
   Feedback change, run independent deterministic checks, then execute only the
   frozen-parent `reserve_tool` real proof.

## PAC-171 — Actionable Feedback implementation independently allowed

1. **Checkpoint and boundary:** The private Direct parser-to-Feedback repair
   was implemented and independently checked against plan digest
   `d94bba5476f326f34778c0cff4b602fed697e9cd09c482bc258bdb59e4b35f90`.
2. **North-star link:** The exact blocked ToolSemantics leaf may now be tested
   before any claim about complete Design, executable Candidate, Judge or
   Registry publication.
3. **Trust owner and Artifact effect:** Framework classifies only a safe parse
   condition and authors the next user instruction. Direct remains proposal-
   only; successful ToolSemantics Artifact and every downstream ABI are
   unchanged.
4. **No-hybrid assertion:** Reviewed cleanroom files contain no legacy control
   import or compatibility route; legacy firewall is green.
5. **Evidence:** 133 focused and 236 full tests, Ruff format/check, mypy over 13
   source files and compileall passed. These are deterministic facts only.
6. **Non-claims and remaining risk:** Luna has not yet received the revised
   Feedback. No Design suffix, Candidate, Integration, Judge, Registry, E2E,
   Repair, Expand or Consumer/SFT/RL is proven.
7. **Bad-case effect:** Fenced, outer-content, non-object and invalid-syntax
   answers remain rejected, but their one authorized correction now states an
   actionable safe condition and complete-replacement requirement. No third
   format call exists.
8. **Next permitted gate:** Run only the exact frozen-parent
   `design/tool_semantics[reserve_tool]` Luna proof and read Observe at its first
   terminal.

## PAC-172 — Exact-parent actionable Feedback proof starting

1. **Checkpoint and boundary:** A non-resume diagnostic starts only at the
   frozen `design/tool_semantics[reserve_tool]` Direct leaf.
2. **North-star link:** It tests one necessary Design Artifact before the
   request can reach executable Candidate, independent Judge and Registry.
3. **Trust owner and Artifact effect:** Framework copies and verifies three
   exact immutable parents, owns correction admission/validation/commit, and
   cannot release from this harness. Luna owns only semantic proposals.
4. **No-hybrid assertion:** The path uses current cleanroom `DesignExecutor`,
   `GraphRunner`, `ArtifactStore` and official SDK; deterministic legacy
   firewall is green.
5. **Evidence:** The before/after claim, exact parent digests, model route and
   non-claims are frozen in `direct-actionable-feedback-live-proof.md` before
   dispatch.
6. **Non-claims and remaining risk:** The malformed subtype and Luna response
   remain unknown until execution. This is not full Design or E2E evidence.
7. **Bad-case effect:** The proof can falsify whether safely observed format
   facts actually reach one useful correction while strict rejection and the
   no-third-format-call rule remain intact.
8. **Next permitted gate:** Execute once, read Observe immediately, then either
   continue to the immediate Design suffix on pass or begin a fresh diagnosis
   at the first new terminal.

## PAC-173 — Actionable Feedback proof stopped after semantic progress

1. **Checkpoint and boundary:** Real run
   `run_9916d45626bf4ab3b11535c96fe50aa1` stopped at the exact
   `reserve_tool` leaf after three parsed semantic proposals.
2. **North-star link:** The ToolSemantics Artifact still did not commit, so
   downstream Design, Candidate, Judge and Registry remain honestly closed.
3. **Trust owner and Artifact effect:** Framework delivered two safe next-user
   corrections, enforced the existing three-proposal ceiling and retained all
   commit/release authority. Luna changed proposals only.
4. **No-hybrid assertion:** The proof used exact cleanroom parents and current
   SDK/DesignGraph; no legacy authority ran.
5. **Evidence:** Observe records A at precondition cardinality, B at one
   transition effect value, then C at a different transition effect value;
   three Luna operations have measured usage and release is `not_published`.
6. **Non-claims and remaining risk:** A-to-B-to-C proves correction progress,
   not ToolSemantics success. Raw proposals are unavailable, so whether C
   pre-existed is unknown. No later product boundary is proven.
7. **Bad-case effect:** Generic format failure is gone and point-level Feedback
   worked, but “change at that path” did not reliably cause whole-condition
   repair before the hard ceiling.
8. **Next permitted gate:** Diagnose and independently review only a minimal
   whole-condition recipient wording repair; do not add calls or split fields.

## PAC-174 — Whole-condition Feedback repair entry

1. **Checkpoint and boundary:** Entering the uncommitted Direct semantic
   Feedback boundary under plan digest
   `76c57a0fd6aff39b39f105936d9952b14539a9917aa193a08ae0bab1ab478cd8`.
2. **North-star link:** This may unblock one ToolSemantics Artifact on the path
   from natural-language need through executable Candidate, independent Judge
   and Registry EnvironmentPackage; it is not a product-completion claim.
3. **Trust owner and Artifact effect:** EnvironmentDesigner/framework owns
   the safe next-user Feedback and EffectDraft diagnostic. The correction
   packet, GraphRunner admission, Artifact schema, candidate/Judge/Registry
   handoffs and release authority remain untouched.
4. **No-hybrid assertion:** The change is confined to the cleanroom Direct
   feedback/validator path and focused tests; it introduces no legacy control
   authority or compatibility route.
5. **Evidence:** The matching independent `allow` records the safe A-to-B-to-C
   terminal, its raw-proposal uncertainty, and the existing three-proposal
   ceiling.
6. **Non-claims and remaining risk:** No ToolSemantics Artifact, later Design,
   Candidate, Integration, Judge, Registry, E2E, Repair, Expand or Consumer
   result is proven yet.
7. **Bad-case effect:** The repair targets path-only correction of a condition
   that reappeared elsewhere; it must preserve strict validation and never add
   a fourth proposal.
8. **Next permitted gate:** Apply only the reviewed renderer/diagnostic and
   focused-test change, then run deterministic checks.

## PAC-175 — Whole-condition Feedback deterministic exit

1. **Checkpoint and boundary:** Exiting the bounded Direct semantic Feedback
   implementation under plan digest
   `76c57a0fd6aff39b39f105936d9952b14539a9917aa193a08ae0bab1ab478cd8`.
2. **North-star link:** The renderer can now direct one uncommitted
   ToolSemantics replacement to repair its observed occurrence and all matching
   occurrences before the existing Design-to-Candidate-to-Judge-to-Registry
   path can continue.
3. **Trust owner and Artifact effect:** EnvironmentDesigner/framework still
   owns safe diagnostics and validation. CorrectionPacket, GraphRunner's
   three-proposal ceiling, compiled Artifact schema, downstream consumers and
   release authority are unchanged.
4. **No-hybrid assertion:** The changed Direct feedback path has no legacy
   control import, alternate route, provider fallback, retry, or compatibility
   authority.
5. **Evidence:** Deterministic only: 102 focused Design/graph tests passed;
   Ruff format/check passed on the changed code/test files.
6. **Non-claims and remaining risk:** This does not prove that Luna will find
   all matching occurrences, commit ToolSemantics, complete Design, or reach
   Candidate, Integration, Judge, Registry, E2E, Repair, Expand or Consumer.
7. **Bad-case effect:** The prior A-to-B-to-C terminal now receives a
   whole-condition instruction while direct literal wrappers remain rejected
   and no fourth proposal or persisted rejected content is permitted.
8. **Next permitted gate:** An independent implementation check of this exact
   scope; only after it allows may the frozen-parent `reserve_tool` leaf run
   once and its Observe terminal be read.

## PAC-176 — Whole-condition exact leaf proof starting

1. **Checkpoint and boundary:** One frozen-parent
   `design/tool_semantics[reserve_tool]` Luna proof is starting.
2. **North-star link:** It tests one required ToolSemantics Artifact before any
   complete Design, Candidate, Judge or Registry claim.
3. **Trust owner and Artifact effect:** Framework retains validation, three-
   proposal ceiling, commit and release authority; Luna proposes semantics.
4. **No-hybrid assertion:** Exact current cleanroom parents and official SDK
   path are used; legacy firewall is green.
5. **Evidence:** The exact claim and immutable parent digests are frozen in
   `tool-semantics-whole-condition-feedback-live-proof.md` before dispatch.
6. **Non-claims and remaining risk:** Feedback efficacy and leaf success remain
   unproven until terminal Observe; no downstream boundary is included.
7. **Bad-case effect:** This can disprove whether whole-condition wording avoids
   path-by-path recurrence within the unchanged attempt ceiling.
8. **Next permitted gate:** Execute once and read Observe immediately; continue
   only on a committed leaf, otherwise begin a fresh diagnosis.

## PAC-177 — Whole-condition exact leaf proof passed

1. **Checkpoint and boundary:** The frozen-parent Direct
   `design/tool_semantics[reserve_tool]` correction transaction passed.
2. **North-star link:** Useful Feedback moved one semantic proposal toward a
   compiled ToolSemantics Artifact on the path from natural-language need to
   executable Candidate, independent Judge and Registry package.
3. **Trust owner and Artifact effect:** Luna proposed replacements; framework
   retained the three-proposal ceiling, strict compiler, commit and release
   authority. It committed `design.tool_semantics:3d912f62acd894d1` and
   WorkRecord `control.work_record:f5de2feb2511fecd`.
4. **No-hybrid assertion:** The proof used the cleanroom official-SDK Direct
   route and exact frozen parent Artifacts; no legacy authority was loaded.
5. **Evidence:** Run `run_1c2c30385a1842d89449e1072e9db5de` passed on
   proposal 3; immediate Observe showed no Finding and no publication.
6. **Non-claims and remaining risk:** Remaining Design, Candidate,
   Integration, Judge, Registry, E2E, Repair, Expand and Consumer are unproven.
7. **Bad-case effect:** The result proves that actionable whole-condition
   Feedback can escape the observed path-by-path repair pattern without a
   fourth proposal; it does not guarantee every future proposal will pass.
8. **Next permitted gate:** Continue with the immediate downstream Design
   closure, stopping at the first new terminal for a fresh diagnosis.

## PAC-178 — Fresh public Direct E2E starting

1. **Checkpoint and boundary:** A fresh natural-language public `generate`
   request is entering the full Direct DesignGraph and CandidateGraph.
2. **North-star link:** The only success target is an executable Candidate,
   independent Judge pass, immutable Registry `EnvironmentPackage`, and safe
   Observe evidence—not graph traversal by itself.
3. **Trust owner and Artifact effect:** Direct LLM and single-Skill Agent
   backends propose semantic/build outputs; framework owns validation,
   candidate-process isolation, Judge gates and release.
4. **No-hybrid assertion:** The cleanroom CLI, official SDK adapters and current
   fixed graphs are used; no legacy compatibility authority is permitted.
5. **Evidence:** The exact route, models and falsifiable claim are frozen in
   `direct-public-e2e-live-proof.md`; deterministic checks and the repaired
   ToolSemantics leaf are green.
6. **Non-claims and remaining risk:** No fresh E2E terminal exists yet; Repair,
   Expand/multi-parent and Consumer/SFT/RL remain outside this proof.
7. **Bad-case effect:** Any first new terminal stops chaining and is diagnosed
   from Observe; there is no blind provider retry or hand-edited Artifact.
8. **Next permitted gate:** Execute the public route once and read its terminal
   Observe scene immediately.

## PAC-179 — Fresh public Direct E2E stopped at CurriculumPlan

1. **Checkpoint and boundary:** Public run
   `run_fb7f87b4307346b3ae2e6843b27f650a` stopped safely at the Direct LLM ->
   CurriculumPlan compiler boundary.
2. **North-star link:** Research, Architecture, all eight ToolSemantics shards
   and WorldRules progressed toward the environment Design, but no executable
   Candidate or independently judged Registry package exists.
3. **Trust owner and Artifact effect:** Luna proposed Curriculum source data;
   framework rejected it, committed no Curriculum Artifact, wrote a blocking
   Finding and retained release authority.
4. **No-hybrid assertion:** The public cleanroom route and legacy firewall were
   used; no legacy control authority or compatibility fallback participated.
5. **Evidence:** Observe terminal `curriculum_plan_invalid`; attempt 1 corrected
   `$.families[0].dimensions[0]`, attempt 2 advanced to the distinct
   `$.families[6]` condition, and Registry stayed `not_published`.
6. **Non-claims and remaining risk:** Curriculum, TaskRequirement, Modeling
   Gate, Candidate, Integration, Judge, Registry, E2E, Repair, Expand and
   Consumer remain unproven.
7. **Bad-case effect:** The run demonstrates useful Feedback progress but also
   exposes an ambiguous combined diagnostic and an unavailable strict-progress
   correction turn; neither permits blind rerun.
8. **Next permitted gate:** Persist a diagnosis, revise the minimal repair
   plan, obtain a fresh cross-layer `allow`, then prove only this frozen
   Curriculum boundary first.

## PAC-180 — Curriculum actionable Feedback repair allowed

1. **Checkpoint and boundary:** Entering the independently allowed local
   Curriculum compiler/GraphRunner correction change under plan digest
   `77d5ec2da849fea1258dec535cd59b053fd4f6c4454cfad2ea681bae9f509b74`.
2. **North-star link:** The repair may unblock the frozen Curriculum Artifact
   required before TaskRequirement, executable Candidate, independent Judge
   and Registry package; it is not product completion.
3. **Trust owner and Artifact effect:** Framework retains exact diagnostics,
   correction admission, strict compiler, Artifact commit and release
   authority. Luna receives only the next-user correction and cannot route or
   publish.
4. **No-hybrid assertion:** Scope is confined to the cleanroom Direct graph and
   compiler; no legacy authority or compatibility route is introduced.
5. **Evidence:** Independent Terra-max critic record
   `cross-layer-review-77d5ec2d-curriculum-feedback.md` says `allow` after the
   real public Curriculum terminal and exact downstream compatibility trace.
6. **Non-claims and remaining risk:** No changed code, compiled Curriculum,
   TaskRequirement, Candidate, Judge, Registry, E2E, Repair, Expand or Consumer
   result is proven yet.
7. **Bad-case effect:** The approved change targets an ambiguous two-field
   diagnostic and unavailable strict-progress correction without adding blind
   retries or a fourth proposal.
8. **Next permitted gate:** Implement only the reviewed source/tests, run
   deterministic checks, then obtain an independent implementation review.

## PAC-181 — Curriculum actionable Feedback implementation exit

1. **Checkpoint and boundary:** Exiting the bounded Curriculum
   compiler/GraphRunner implementation; entering independent check.
2. **North-star link:** Exact field Feedback and a final strict-progress
   proposal may allow Curriculum to commit before TaskRequirement, Candidate,
   Judge and Registry, but none of those outcomes is claimed.
3. **Trust owner and Artifact effect:** Framework still owns validation and the
   declaration-driven proposal ceiling. Accepted Curriculum/DifficultySchema
   data, Artifact ABI, edges, Prompt, model and downstream consumers are
   unchanged.
4. **No-hybrid assertion:** Only cleanroom `design.py`, `graph.py` and focused
   tests changed; no compatibility path or legacy authority was added.
5. **Evidence:** Implement worker reports 107 focused tests passed and Ruff
   passed on the four owned files. This is deterministic evidence only.
6. **Non-claims and remaining risk:** Independent whole-diff check and real
   Curriculum proof are pending; TaskRequirement, Candidate, Judge, Registry,
   E2E, Repair, Expand and Consumer remain unproven.
7. **Bad-case effect:** Exact task-family and actor diagnostics replace the
   merged parent-path message; repeated/format issues still stop by proposal 2
   and proposal 3 can never lead to proposal 4.
8. **Next permitted gate:** Independent check of exact scope and all
   deterministic quality gates; only an `allow` permits the frozen-parent live
   proof.

## PAC-182 — Frozen-parent Curriculum proof starting

1. **Checkpoint and boundary:** The independently allowed
   `design/curriculum_plan` live transaction is starting with the exact three
   parent Artifacts from failed public run
   `run_fb7f87b4307346b3ae2e6843b27f650a`.
2. **North-star link:** A compiled Curriculum is required before Tasks,
   executable Candidate, independent Judge and Registry package; a leaf pass
   alone is not the product outcome.
3. **Trust owner and Artifact effect:** Luna may propose at most three complete
   source objects. Framework owns exact Feedback, strict compilation, commit,
   work evidence and release authority.
4. **No-hybrid assertion:** The proof uses the cleanroom graph, official OpenAI
   Python SDK Direct route and frozen ArtifactRefs; it loads no legacy control
   authority and mounts no Skill/tool/workspace.
5. **Evidence:** Independent Terra-max implementation check says `allow`; 109
   focused and 245 full serial tests plus Ruff, mypy, compileall and legacy
   firewall pass.
6. **Non-claims and remaining risk:** No changed Luna proposal or Curriculum
   Artifact exists yet. TaskRequirement, ModelingGate, Candidate, Judge,
   Registry, E2E, Repair, Expand and Consumer remain unproven.
7. **Bad-case effect:** Any repeated/format/provider/postcompile issue stops at
   its declared ceiling; a third invalid proposal stops without a fourth and
   starts a new Observe-led diagnosis.
8. **Next permitted gate:** Invoke only the frozen-parent Curriculum leaf once,
   persist its diagnostic run, and inspect Observe immediately.

## PAC-183 — Frozen-parent Curriculum proof stopped without progress

1. **Checkpoint and boundary:** Diagnostic run
   `run_16b5772c5d2c45d787ec3057b4b3a96c` stopped at the Direct
   Curriculum compiler after two parsed proposals reached the same field-set
   condition.
2. **North-star link:** No Curriculum means no TaskRequirement, executable
   Candidate, independent Judge or publishable Registry package; graph progress
   is not claimed as completion.
3. **Trust owner and Artifact effect:** Luna proposed twice; framework retained
   strict validation and correctly denied proposal 3 because the correction
   tuple did not change. No Curriculum Artifact or release was committed.
4. **No-hybrid assertion:** Exact frozen cleanroom parents, official OpenAI SDK
   JSON mode and Direct no-Skill execution were used; no legacy authority
   participated.
5. **Evidence:** Both attempts failed at `$.families[0]` with `object must use
   exactly the declared fields`; immediate Observe shows failed WorkRecord
   `control.work_record:ae3bc7a94e10668b`, one blocking Finding and
   `release=not_published`.
6. **Non-claims and remaining risk:** Actual missing/extra proposal keys remain
   intentionally unknown. All downstream Design, Candidate, Judge, Registry,
   E2E, Repair, Expand and Consumer work is unproven.
7. **Bad-case effect:** The result proves the strict-progress guard is real and
   prevents blind retry. It exposes that the recipient Feedback discards the
   safe expected field set already known by the compiler.
8. **Next permitted gate:** Review the minimal expected-field Feedback plan
   through a fresh cross-layer critic before any code or model call.

## PAC-184 — Expected-field Feedback repair allowed

1. **Checkpoint and boundary:** Entering the independently allowed shared
   `_object` compiler-to-Direct-Feedback wording change under plan digest
   `c98eb85128760cdff40a0b7566dc6090659834b8f59a19bb8899639d347d3238`.
2. **North-star link:** More actionable deterministic Feedback may unblock the
   Curriculum needed before Tasks, executable Candidate, independent Judge and
   Registry package; this is not product completion.
3. **Trust owner and Artifact effect:** Framework discloses only its own
   expected field names and retains strict validation, correction admission,
   Artifact commit and release authority. Luna cannot route or publish.
4. **No-hybrid assertion:** Scope is confined to the cleanroom Design compiler
   and tests; no legacy or compatibility authority is introduced.
5. **Evidence:** Independent Terra-max critic returned `allow`, preferring one
   homologous helper change over a duplicated Curriculum-only special case.
6. **Non-claims and remaining risk:** No implementation check or changed Luna
   result exists. Curriculum and every downstream product boundary remain
   unproven.
7. **Bad-case effect:** The change targets a repeated non-actionable condition
   without adding retries, weakening acceptance, or persisting rejected output.
8. **Next permitted gate:** Change one condition and its focused assertions,
   then run deterministic checks and independent implementation review.

## PAC-185 — Expected-field Feedback implementation exit

1. **Checkpoint and boundary:** Exiting the bounded `_object`
   compiler-to-Feedback implementation and entering independent check.
2. **North-star link:** The changed recipient story may unblock Curriculum on
   the path to Tasks, executable Candidate, independent Judge and Registry;
   tests alone are not product completion.
3. **Trust owner and Artifact effect:** Framework still owns the identical
   closed field-set comparison and proposal ceiling. No accepted semantic
   object, Artifact ABI, edge, model route or release authority changed.
4. **No-hybrid assertion:** Only cleanroom `design.py` and focused tests changed;
   no legacy authority or compatibility path was introduced.
5. **Evidence:** 109 focused and 245 full serial tests pass with Ruff, mypy,
   compileall and the legacy firewall.
6. **Non-claims and remaining risk:** Independent implementation review and
   real Luna replay are pending. Curriculum and every downstream boundary
   remain unproven.
7. **Bad-case effect:** Feedback now names the safe complete expected field set
   while retaining rejected model output as ephemeral; no retry was added.
8. **Next permitted gate:** Independent review of the exact helper/test change;
   only an `allow` permits the frozen-parent replay.

## PAC-186 — Expected-field Curriculum replay starting

1. **Checkpoint and boundary:** The independently checked frozen-parent
   `design/curriculum_plan` proof is starting with the exact Architecture,
   WorldRules and EvidenceGraph from the public failure lineage.
2. **North-star link:** Only a compiled Curriculum can advance to Tasks,
   executable Candidate, independent Judge and Registry; a leaf pass remains
   partial evidence.
3. **Trust owner and Artifact effect:** Luna may propose only within the existing
   ceiling; framework owns the enhanced safe Feedback, strict compiler,
   Artifact commit and release authority.
4. **No-hybrid assertion:** Official OpenAI SDK Direct JSON mode, cleanroom graph
   and exact frozen ArtifactRefs are used; there is no Skill/tool/workspace or
   legacy authority.
5. **Evidence:** Independent implementation check is `allow`; 109 focused and
   245 full tests plus all static gates pass.
6. **Non-claims and remaining risk:** No changed live proposal exists yet;
   downstream Design, Candidate, Judge, Registry, E2E, Repair, Expand and
   Consumer remain unproven.
7. **Bad-case effect:** The proof can show whether listing expected fields makes
   the next-user Feedback actionable without adding a blind retry.
8. **Next permitted gate:** Invoke the exact frozen-parent Curriculum leaf once
   and read Observe immediately at its terminal.

## PAC-187 — Frozen-parent Curriculum leaf passed

1. **Checkpoint and boundary:** Diagnostic run
   `run_b3535644e34a41aa80c179b69534e835` passed the exact frozen-parent
   Direct Curriculum compiler/commit boundary.
2. **North-star link:** A valid Curriculum can now feed TaskRequirement and
   ModelingGate on the route to executable Candidate, independent Judge and
   Registry; the leaf is not product completion.
3. **Trust owner and Artifact effect:** Luna proposal 1 passed strict framework
   compilation; framework committed
   `design.curriculum_plan:1c76f639217b1b8e` and WorkRecord
   `control.work_record:2e4fec0c39259ae2`. No release was attempted.
4. **No-hybrid assertion:** Exact cleanroom parents and official SDK Direct
   JSON mode were used, with no Skill/tool/workspace or legacy authority.
5. **Evidence:** 36.8-second real Luna call; eight families; immediate Observe
   shows one passed Work, no Finding and `release=not_published`.
6. **Non-claims and remaining risk:** Proposal 1 passed, so this run did not
   live-consume the enhanced Feedback. TaskRequirement, Candidate, Judge,
   Registry, E2E, Repair, Expand and Consumer remain unproven.
7. **Bad-case effect:** The exact formerly blocked leaf can now commit without
   weakening validation; deterministic tests retain the expected-field
   correction and no-progress ceilings.
8. **Next permitted gate:** Run one fresh public Direct E2E, stop at its first
   new terminal, and inspect Observe before any further change.

## PAC-188 — Fresh public Direct E2E starting after Curriculum pass

1. **Checkpoint and boundary:** A fresh natural-language request is entering
   the public cleanroom DesignGraph and CandidateGraph.
2. **North-star link:** Success requires an executable Candidate, independent
   Judge pass and immutable Registry `EnvironmentPackage`, not merely Design
   graph completion.
3. **Trust owner and Artifact effect:** Direct/Agent backends may propose
   semantics/code; framework owns validation, candidate-process isolation,
   Judge, package and Registry release.
4. **No-hybrid assertion:** The current public CLI, official SDK adapters and
   fixed cleanroom graphs are used; no legacy compatibility authority is
   permitted.
5. **Evidence:** Deterministic gates and the exact Curriculum leaf are green;
   the fresh E2E claim is frozen in
   `direct-public-e2e-after-curriculum-live-proof.md`.
6. **Non-claims and remaining risk:** No fresh terminal exists yet; Repair,
   Expand and Consumer remain outside this proof.
7. **Bad-case effect:** The run stops at the first new terminal and does not
   hand-edit Artifacts or blindly retry a semantic failure.
8. **Next permitted gate:** Execute the public route once and read its Observe
   scene immediately.

## PAC-189 — Fresh E2E stopped at ToolSemantics format boundary

1. **Checkpoint and boundary:** Public run
   `run_804e6cc894674e69b7ea72d0714c8daa` stopped safely at Direct
   `tool_semantics[manage_equipment]` after two outer-content format failures.
2. **North-star link:** Research, Architecture, SharedTool and one ToolSemantics
   shard advanced, but no complete Design, executable Candidate, independent
   Judge or Registry package exists.
3. **Trust owner and Artifact effect:** Luna proposed twice; framework rejected
   both, denied a third format call, committed no shard output and retained
   release authority.
4. **No-hybrid assertion:** The public cleanroom route, official OpenAI SDK and
   legacy firewall were used; no compatibility authority participated.
5. **Evidence:** Observe shows terminal `direct_response_not_json`, failed shard
   `manage_equipment`, two operation/attempt records, one blocking Finding and
   `release=not_published`.
6. **Non-claims and remaining risk:** Raw outer content remains intentionally
   unknown. All later Design, Candidate, Judge, Registry, E2E, Repair, Expand
   and Consumer boundaries are unproven.
7. **Bad-case effect:** Strict parsing and bounded correction prevented wrapped
   or multi-value content from becoming a semantic Artifact. The format action
   remains too generic for the recipient.
8. **Next permitted gate:** Submit the one-sentence format-action plan to a
   fresh cross-layer critic before editing or another model call.

## PAC-190 — Direct format action repair allowed

1. **Checkpoint and boundary:** Entering the independently allowed shared
   Direct format-Feedback sentence change under plan digest
   `4c19d42f5eb87e0ca872f1a3e7084557cd12df2b6102fd07b9bfe7d345099dba`.
2. **North-star link:** An actionable format correction may unblock the missing
   ToolSemantics shard before complete Design, executable Candidate,
   independent Judge and Registry package; it is not completion.
3. **Trust owner and Artifact effect:** Framework retains strict parsing,
   two-call admission, Artifact commit and release authority. Luna receives one
   clearer user action only.
4. **No-hybrid assertion:** Scope is the cleanroom Feedback renderer, focused
   test and one guide sentence; no legacy authority is introduced.
5. **Evidence:** Independent Terra-max critic returned `allow` for revision 1
   and explicitly rejected parser, SDK schema, retry and topology expansion.
6. **Non-claims and remaining risk:** No changed code or Luna replay exists;
   the shard and every downstream boundary remain unproven.
7. **Bad-case effect:** The plan converts a repeated diagnosis-only message into
   a concrete deletion/replacement instruction without accepting wrapped data.
8. **Next permitted gate:** Dispatch the bounded implementation, then run
   deterministic checks and an independent implementation review.

## PAC-191 — Direct format action implementation exit

1. **Checkpoint and boundary:** Exiting the bounded Direct format-Feedback
   sentence/test/guide implementation and entering independent check.
2. **North-star link:** The explicit action may unblock the missing
   ToolSemantics shard before complete Design, executable Candidate,
   independent Judge and Registry; tests do not prove that outcome.
3. **Trust owner and Artifact effect:** Strict parsing, two-call admission,
   compilation, Artifact and release authority remain framework-owned and
   unchanged.
4. **No-hybrid assertion:** Only cleanroom `design.py`, one existing test region
   and the concise debugging guide changed; no legacy path was added.
5. **Evidence:** 3 targeted, 109 focused and 245 full tests plus Ruff, mypy,
   compileall and legacy firewall pass.
6. **Non-claims and remaining risk:** Independent review and real Luna replay
   are pending; no shard, Design, Candidate, Judge or Registry result is proven.
7. **Bad-case effect:** The recipient now gets a concrete deletion/replacement
   action while wrapped content remains rejected and unpersisted.
8. **Next permitted gate:** Independent exact-scope check; only `allow` permits
   the frozen `manage_equipment` proof.

## PAC-192 — Frozen manage-equipment proof starting

1. **Checkpoint and boundary:** The independently checked Direct
   `tool_semantics[manage_equipment]` proof is starting with exact frozen
   Architecture, SharedToolSemantics and EvidenceGraph parents from the failed
   public run.
2. **North-star link:** This shard is required for complete Design before an
   executable Candidate, independent Judge and Registry package; leaf success
   remains partial evidence.
3. **Trust owner and Artifact effect:** Luna has the unchanged proposal ceiling;
   framework owns explicit Feedback, strict compilation, commit and release.
4. **No-hybrid assertion:** Official SDK Direct JSON mode and cleanroom frozen
   Artifacts are used; no Skill/tool/workspace or legacy authority participates.
5. **Evidence:** Independent implementation check is `allow`; 3 targeted, 109
   focused and 245 full tests plus all static gates pass.
6. **Non-claims and remaining risk:** No changed live response exists; the shard
   and every downstream boundary remain unproven.
7. **Bad-case effect:** A format failure may receive one actionable correction,
   but a second still stops without a third and without release.
8. **Next permitted gate:** Invoke only the frozen `manage_equipment` shard and
   read Observe immediately.

## PAC-193 — Frozen manage-equipment Feedback proof passed

1. **Checkpoint and boundary:** Diagnostic run
   `run_2099d3669a4b4963bc2b4ae7eb7d4eea` passed the exact frozen Direct
   `tool_semantics[manage_equipment]` correction transaction.
2. **North-star link:** One required ToolSemantics shard can now feed later
   Design nodes on the route to executable Candidate, independent Judge and
   Registry; it is not product completion.
3. **Trust owner and Artifact effect:** Luna proposal 1 was rejected; framework
   authorized one concrete user Feedback; proposal 2 passed strict compilation
   and framework committed `design.tool_semantics:cba69d5863417bfb`.
4. **No-hybrid assertion:** Exact cleanroom parents and official SDK Direct
   mode were used with no Skill/tool/workspace or legacy authority.
5. **Evidence:** Two real Luna calls, second passed; WorkRecord
   `control.work_record:d52202215e3d35f3`; immediate Observe has no Finding and
   `release=not_published`.
6. **Non-claims and remaining risk:** Remaining Design, Candidate, Judge,
   Registry, E2E, Repair, Expand and Consumer paths remain unproven.
7. **Bad-case effect:** Outer prose/extra JSON was not accepted; actionable
   Feedback corrected it within the existing ceiling and no third call existed.
8. **Next permitted gate:** Run one fresh public Direct E2E and stop at its
   first new terminal for Observe-led diagnosis.

## PAC-194 — Fresh public Direct E2E restarting after format proof

1. **Checkpoint and boundary:** A fresh natural-language request is entering
   the full public cleanroom DesignGraph and CandidateGraph after the exact
   format leaf passed.
2. **North-star link:** Success still requires executable Candidate,
   independent Judge and immutable Registry `EnvironmentPackage`.
3. **Trust owner and Artifact effect:** Direct/Agent backends propose only;
   framework owns validation, candidate isolation, Judge and release.
4. **No-hybrid assertion:** Public CLI, official SDK adapters and cleanroom
   fixed graphs are used; no legacy compatibility authority participates.
5. **Evidence:** Deterministic gates and exact two-call format correction proof
   pass; no full-run claim exists yet.
6. **Non-claims and remaining risk:** Repair, Expand and Consumer remain later
   children; no fresh release exists.
7. **Bad-case effect:** Any new terminal stops the run and is read through
   Observe; no hand-edited Artifact or blind semantic retry is permitted.
8. **Next permitted gate:** Execute the public route once and inspect its
   terminal Observe immediately.

## PAC-195 — Fresh E2E stopped at repeated ToolSemantics outer content

1. **Checkpoint and boundary:** Public run
   `run_6df6b3046ae64983847f44621ac81a1c` stopped at Direct
   `tool_semantics[manage_maintenance]` after two completed outer-content
   proposals.
2. **North-star link:** Research, Architecture, SharedToolSemantics and five
   ToolSemantics shards passed, but there is still no complete Design,
   executable Candidate, independent Judge or Registry package.
3. **Trust owner and Artifact effect:** Luna proposed twice; framework rejected
   both, committed no failed-shard semantic Artifact and kept release closed.
4. **No-hybrid assertion:** Public cleanroom CLI, official SDK adapters and the
   fixed DesignGraph ran without legacy authority.
5. **Evidence:** Observe records two Direct operation/attempt refs, terminal
   `direct_response_not_json`, one blocking Finding and
   `release=not_published`.
6. **Non-claims and remaining risk:** `verify_task`, later Design, Candidate,
   Judge, Registry, Repair, Expand and Consumer remain unproven.
7. **Bad-case effect:** Strict parsing and secrecy worked, but the control
   branch discarded the node's declared second correction under a stale
   one-format-Feedback policy.
8. **Next permitted gate:** Review the persisted Diagnosis and minimal
   two-correction plan independently before any code edit or model replay.

## PAC-196 — Second format Feedback plan entering critic

1. **Checkpoint and boundary:** Planning a bounded control-plane correction for
   Direct nodes that already declare `local_corrections=2`.
2. **North-star link:** The change may let the missing ToolSemantics shard reach
   complete Design and later Candidate/Judge/Registry; it is not completion.
3. **Trust owner and Artifact effect:** Framework remains the sole owner of
   admission, the three-proposal ceiling, validation, commit and release.
4. **No-hybrid assertion:** The plan touches only cleanroom source guidance,
   GraphRunner eligibility, focused tests and one concise guide sentence.
5. **Evidence:** The failed run consumed one actionable Feedback yet stopped
   with one declared correction unused; five sibling shards passed.
6. **Non-claims and remaining risk:** No repair is implemented or proven;
   downstream boundaries remain unproven.
7. **Bad-case effect:** The plan corrects the policy mismatch without weakening
   parser/compiler contracts or adding a retry subsystem.
8. **Next permitted gate:** Compute the plan digest and obtain an independent
   Terra-max cross-layer decision; only `allow` permits implementation.

## PAC-197 — Bounded second format Feedback implementation entering

1. **Checkpoint and boundary:** Entering implementation under independent
   `allow` for plan digest
   `85541065b75f17ec5509bb7cc7be2d61173365a7a58c414122f765e3345483a8`.
2. **North-star link:** Correct bounded self-revision may unblock the missing
   ToolSemantics shard on the route to executable Candidate, independent Judge
   and Registry package; it is not product completion.
3. **Trust owner and Artifact effect:** Framework alone admits at most two
   Feedback turns and three proposals, compiles output and owns every commit,
   Finding and release decision; Luna gains no control authority.
4. **No-hybrid assertion:** Scope is the cleanroom GraphRunner eligibility,
   focused tests and matching normative wording only; no legacy authority,
   parser relaxation or retry platform is permitted.
5. **Evidence:** Terra-max critic independently returned `allow`; the real
   public terminal proves two completed outer-content proposals and an unused
   declared correction.
6. **Non-claims and remaining risk:** No code or real Luna replay has passed;
   Candidate, Judge, Registry, E2E, Repair, Expand and Consumer remain
   unproven.
7. **Bad-case effect:** The implementation must support format-to-format and
   format-to-semantic progress, stop semantic-to-format regression, and never
   issue a fourth proposal.
8. **Next permitted gate:** Dispatch the bounded implementer, run focused/full
   deterministic gates, then obtain an independent implementation check.

## PAC-198 — Bounded second format Feedback implementation exit

1. **Checkpoint and boundary:** Exiting the approved runner/test/normative-text
   implementation and entering independent quality review.
2. **North-star link:** The bounded correction may unblock complete Design on
   the path to executable Candidate, independent Judge and Registry package;
   deterministic green checks are not product completion.
3. **Trust owner and Artifact effect:** Framework still owns exact admission,
   compilation, Artifact/Finding persistence and release; no model authority or
   downstream schema changed.
4. **No-hybrid assertion:** Only cleanroom runner, existing tests and approved
   normative wording changed; SDK/parser/routes/topology/legacy paths did not.
5. **Evidence:** 13 targeted, 115 focused and 251 full tests, Ruff, mypy,
   compileall and the legacy firewall all pass.
6. **Non-claims and remaining risk:** Independent check and real Luna replay
   remain pending; Candidate, Judge, Registry, E2E, Repair, Expand and Consumer
   remain unproven.
7. **Bad-case effect:** Both predeclared two-correction Direct nodes now have
   tested format-first final Feedback, semantic-to-format terminal behavior and
   a no-fourth-call ceiling; rejected text remains ephemeral.
8. **Next permitted gate:** Independent Terra-max whole-scope check; only
   `allow` permits the exact frozen `manage_maintenance` proof.

## PAC-199 — Frozen manage-maintenance correction proof starting

1. **Checkpoint and boundary:** Starting one exact-parent Direct
   `tool_semantics[manage_maintenance]` transaction after independent
   implementation `allow`.
2. **North-star link:** A committed shard can feed the remaining Design suffix
   toward executable Candidate, independent Judge and Registry package; this
   leaf remains partial evidence.
3. **Trust owner and Artifact effect:** Luna proposes complete semantics;
   framework owns at most three proposals, strict compilation, commit/failure
   evidence and non-release.
4. **No-hybrid assertion:** Exact cleanroom parent bytes and official SDK
   Direct mode are used; no Skill/tool/workspace or legacy authority enters.
5. **Evidence:** 251 full tests and all static gates pass; independent
   implementation check is `allow`; parent digests and claim are frozen in the
   live-proof record.
6. **Non-claims and remaining risk:** No live terminal exists yet; downstream
   Design, Candidate, Judge, Registry, E2E and later children are unproven.
7. **Bad-case effect:** The proof must expose whether proposal three is
   admitted after format-first repetition and must never issue proposal four.
8. **Next permitted gate:** Run only this leaf and read its Observe scene
   immediately at terminal.

## PAC-200 — Frozen manage-maintenance leaf passed

1. **Checkpoint and boundary:** Diagnostic run
   `run_f98e5178e3b04c0c9b7b960a7ac26817` passed exact-parent Direct
   `tool_semantics[manage_maintenance]` on proposal one.
2. **North-star link:** The required shard can feed the remaining Design suffix
   toward executable Candidate, independent Judge and Registry; leaf success
   is not product completion.
3. **Trust owner and Artifact effect:** Luna proposed once; framework strictly
   compiled and committed `design.tool_semantics:8c24aad938fb88ed` and
   `control.work_record:2796fc5f4c550689`; no release was attempted.
4. **No-hybrid assertion:** Exact frozen parent bytes, official SDK Direct mode
   and no Skill/tool/workspace or legacy authority were used.
5. **Evidence:** 6,397 input/2,398 output tokens, one passed attempt, compiled
   digest `4b7e7ce4…`, immediate Observe with no Finding and non-release.
6. **Non-claims and remaining risk:** Proposal one passed, so the live run did
   not consume the newly admitted final Feedback. Remaining Design, Candidate,
   Judge, Registry, E2E and later children remain unproven.
7. **Bad-case effect:** The exact previously failing shard now commits without
   weakening acceptance; deterministic tests retain the three-proposal/no-fourth
   control proof.
8. **Next permitted gate:** Start one fresh public Direct E2E and stop at its
   first terminal for Observe-led handling.

## PAC-201 — Fresh public Direct E2E starting after bounded-feedback proof

1. **Checkpoint and boundary:** A fresh natural-language request is entering
   the complete public cleanroom DesignGraph and CandidateGraph.
2. **North-star link:** Success requires an executable isolated Candidate,
   independent Judge pass and immutable Registry `EnvironmentPackage`, not a
   Design prefix.
3. **Trust owner and Artifact effect:** Models/Agents propose semantics/code;
   framework owns validation, candidate process, Judge and release.
4. **No-hybrid assertion:** Public CLI, official SDK adapters and fixed
   cleanroom graphs are used; no legacy compatibility path participates.
5. **Evidence:** Full deterministic gates, independent implementation check and
   exact frozen leaf pass are complete.
6. **Non-claims and remaining risk:** No new full-run terminal exists; Repair,
   Expand and Consumer remain later children.
7. **Bad-case effect:** The run stops at its first new terminal and never
   hand-edits an Artifact or blindly repeats a failure.
8. **Next permitted gate:** Execute the public route once and read Observe
   immediately at terminal.

## PAC-202 — Fresh public Direct E2E stopped at first TaskRequirement

1. **Checkpoint and boundary:** Public run
   `run_a4cc77f4344e4aeba96ad081223bca70` ended `rejected` at
   `task_requirement[member_registration]`; release is `not_published`.
2. **North-star link:** Research and the preceding Design semantics advanced
   the natural-language request, but no executable Candidate, independent
   Judge, or Registry EnvironmentPackage exists, so this is not product
   completion.
3. **Trust owner and Artifact effect:** Luna proposed task semantics; Designer
   framework rejected a non-error rule's non-null `error_kind` twice. No failed
   proposal was committed and no downstream node ran.
4. **No-hybrid assertion:** The public cleanroom CLI used the fixed DesignGraph,
   Direct SDK route without Skill/tools/workspace, and no legacy authority.
5. **Evidence:** Safe Observe records passed Research, World/Tool semantics,
   WorldRules and CurriculumPlan, two Luna operations, the exact failed path,
   a blocking Finding, and non-release.
6. **Non-claims and remaining risk:** Remaining TaskRequirements, Modeling,
   Candidate, Integration, Judge, Registry, Direct E2E, and all later children
   remain unproven.
7. **Bad-case effect:** The run stopped at its first new terminal; no blind
   retry, Artifact edit, parser weakening, or model switch occurred.
8. **Next permitted gate:** Persist diagnosis and a minimal plan, obtain a fresh
   independent cross-layer allow, then change only the task source boundary.

## PAC-203 — TaskRequirement source slimming implemented deterministically

1. **Checkpoint and boundary:** The allowed TaskRequirement-only source and
   compiler correction is implemented; deterministic checks pass.
2. **North-star link:** Removing duplicate model context and a framework-fixed
   output field makes the first task-family handoff more likely to reach the
   executable Candidate and independent Judge without weakening semantics.
3. **Trust owner and Artifact effect:** Luna still owns predicates, effects,
   rationale and citations; Designer framework injects `error_kind=None`,
   validates strictly and remains the only Artifact commit owner.
4. **No-hybrid assertion:** No legacy path, compatibility adapter, new node,
   retry system, parser tolerance or alternate release authority was added.
5. **Evidence:** The exact revision-2 plan received independent Terra-max
   `allow`; 72 focused and 261 full tests plus Ruff, mypy and compileall passed.
6. **Non-claims and remaining risk:** No real Luna proof has run after this
   change. Remaining TaskRequirements, Modeling, Candidate, Integration,
   Judge, Registry, Direct E2E and later children remain unproven.
7. **Bad-case effect:** Model-supplied `error_kind`, including explicit null,
   remains an illegal extra source field; generic RuleDraft contracts remain
   strict and unchanged.
8. **Next permitted gate:** Independent implementation check, then the exact
   frozen `member_registration` leaf and immediate Observe. This checkpoint
   commit does not mark the task complete.

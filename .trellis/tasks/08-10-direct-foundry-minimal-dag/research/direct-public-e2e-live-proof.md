# Live proof — fresh public Direct E2E

- Date: 2026-08-12
- Status: failed safely
- Public route: `agent-world generate`
- Need: a fresh natural-language community tool-lending workflow request
- Direct primary/fallback: `gpt-5.6-luna` / `gpt-5.3-codex-spark`
- Agent primary/fallback: `gpt-5.6-luna` / `gpt-5.3-codex-spark`

## Falsifiable claim

The exact ToolSemantics failure that stopped the prior public run now has a
passed frozen-parent leaf proof. A fresh request must traverse the real public
composition root and either release one independently judged Registry package,
or stop honestly at the first newly observed terminal with no downstream
release. The run uses real research providers, official-SDK Direct calls,
single-Skill Codex Agent calls, untrusted candidate processes, framework Judge,
and framework-owned Registry authority.

Passing would establish one real Direct E2E example only. It would not prove
bounded Repair, Expand/multi-parent evolution, Consumer/SFT/RL, or universal
generation success.

## Result

Run `run_fb7f87b4307346b3ae2e6843b27f650a` stopped safely at
`design/curriculum_plan` with `curriculum_plan_invalid`; Registry remained
`not_published`. Before that terminal it committed ResearchPlan,
ResearchAcquisition, EvidenceGraph, WorldArchitecture, SharedToolSemantics,
all eight ToolSemantics shards, and WorldRules.

The first Curriculum proposal was rejected at
`$.families[0].dimensions[0]` because the object did not use exactly the
declared fields. The attached Feedback produced strict semantic progress: the
second proposal passed that earlier frontier and reached
`$.families[6]`. It then failed the combined condition “family id and actor
must be frozen valid identifiers.” The validator did not distinguish which of
the two fields was invalid, and the node had no second local-correction turn.

This disproves the fresh Direct E2E claim while proving that the prior
ToolSemantics failure is no longer the terminal in this run. Candidate,
Integration, Judge, Package and Registry did not run.

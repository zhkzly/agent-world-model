# S1 Execution Contract
## Initial contract (frozen)
Goal: implement the approved Need -> real EnvironmentRelease path, never a demo-shaped substitute.
Invariant 1: a positive result requires real Codex-authored project code, real tools, and independently observed native state.
Invariant 2: every stage consumes only its documented context and produces its documented handoff; no hidden fallback or compatibility path.
Invariant 3: tests must distinguish semantic correctness from schema/startup green, and every implementation slice needs RED plus mutation/physical-negative evidence.
Do not: modify canonical PRD/design/implement without user approval; hard-code a domain, dict response map, custom sandbox/protocol/workflow, or speculative abstraction.
Gold: official Codex SDK Python contract, live SearXNG JSON at 127.0.0.1:8080, and the first production-generated release (never a hand-authored positive environment).

## 追加

- 2026-08-26: Research Search uses configurable SearXNG with the verified local default `http://127.0.0.1:8080`; Fetch/Extract remain independent real operations.
- 2026-08-26: Research/Builder Agent route uses the verified local OpenAI-compatible endpoint `http://127.0.0.1:8317/v1`, model `gpt-5.6-luna`, and invocation-only `OPENAI_API_KEY`; credentials are never persisted.
- 2026-08-26: Initial runtime Skills are exactly Research method guidance and environment code generation; a new Skill requires observed repeated need and user-approved canonical change.
- 2026-08-26: Task activation used `OVERRIDDEN(ASK)` after repeated `PATROL_UNAVAILABLE`; this authorizes activation only, not later commit/release transitions.
- 2026-08-26: Slice 2 implement-worker dispatch used `OVERRIDDEN(ASK)` after fresh Patrol returned `PATROL_UNAVAILABLE` (`worker exited without terminal event`); the user's subsequent explicit instruction authorizes only this controlled Slice 2 worker turn, not later check/commit/release transitions.
- 2026-08-26: Research transport is the official OpenAI Responses SDK with Agent-visible `search_sources` / `read_sources`; Python `openai-codex` is reserved for the coding-focused Builder. Alternatives rejected: raw app-server JSON-RPC, MCP, shell imitation and a fixed framework loop.
- 2026-08-26: Physical probe: `openai-codex` stable/prerelease resolution both yielded 0.147.0 and its public Python thread API omitted experimental app-server dynamic-tool registration/output. The local 8317 `gpt-5.6-luna` Responses route emitted the expected function call and, with explicit prior output items plus host `function_call_output`, continued to `TOOL_LOOP_OK:s1-dynamic-tool-probe`; `previous_response_id` continuation was rejected by that route.
- 2026-08-26: Persisting the user-approved Responses/Builder split used `OVERRIDDEN(ASK)` after fresh Patrol again returned `PATROL_UNAVAILABLE`; this authorizes only this documented revision and the already-scoped Slice 2 continuation.
- 2026-08-27: User approved the systemic Slice 2 correction after the live failure audit. Selected: preserve exact-byte evidence storage and fresh-critic independence, but replace the model-facing surface with bounded focused reads, short run-local evidence handles, explicit Need/external-evidence authority, and a real `RESEARCH_AGAIN` -> same producer -> new fresh critic loop. Rejected: further copy-the-hash prompt patches and a full S1 rewrite. Reconsider only if the same fixed live vertical shows this surface still cannot preserve exact evidence binding.
- 2026-08-27: Worker context manifests no longer inject the parent and full S2 plans into every S1 implementation/check turn; the current S1 PRD/design already own the downstream seam, while duplicate large context caused irrelevant exploration before the prior worker exited without writing.
- 2026-08-27: `OVERRIDDEN(ASK)` for exactly one bounded Slice 2 Research correction worker after fresh active-task Patrol returned `PATROL_UNAVAILABLE` (`worker exited without terminal event`). The user's explicit `执行` authorizes only this worker turn; check, commit, release and later Slice actions still require their own fresh transition review.
- 2026-08-27: `OVERRIDDEN(ASK)` for the exact current-diff provider-schema preflight and unchanged ocean-container Slice 2 live vertical after the post-write Patrol again returned `PATROL_UNAVAILABLE`. The user's explicit `继续` authorizes only this live validation; it does not authorize independent check, commit, release or Slice 3.
- 2026-08-27: First bounded-surface live vertical reached a typed `open_core_gap` after 9 real searches, 9 retained SourceRevisions, 5 complete extractions and 31 short evidence handles. All three declared gaps demanded an unstated jurisdiction, exhaustive field set or exact status taxonomy even though the PRD permits one disclosed coherent synthetic-world selection. Selected correction: let structurally valid open gaps reach the fresh critic and revision owner, then enforce unresolved core gaps at finalization; strengthen Research/critic guidance to distinguish selectable disclosed variants from unsupported external facts. Rejected: weakening evidence entailment or treating the live outcome as provider/infrastructure failure.
- 2026-08-27: Second unchanged live vertical produced four supported Need findings and a semantic PASS while listing only disclosed non-core scope choices, but deterministic Host aggregation rejected any non-empty `open_gaps` as `critic_inconsistent_pass`. P/C/S/H/F/O/M/K attribution assigns the observed terminal primarily to H/F/K (ambiguous reviewer-to-Host contract and deterministic verifier aggregation), not model capability. User approved replacing LLM terminal verdicts with typed findings, separate residual limitations and Host-owned aggregation. Rejected: simply ignoring `open_gaps`, switching models before calibration, or adding another critic as self-consistency.
- 2026-08-27: A fresh direct main-Agent Terra Patrol returned `ALLOW` for beginning the bounded typed Evidence Reviewer/Host aggregation implementation; it authorized no completion, commit, Slice 3 or release. The channel runner's prior `PATROL_UNAVAILABLE` was independently reproduced as outer-sandbox denial of Codex `~/.codex` SQLite initialization. Project harness guidance now requires the main Agent to invoke the exact runner with narrowly scoped control-plane host permission while retaining reviewer `--sandbox read-only`; a reusable command prefix was approved.
- 2026-08-27: Post-implementation typed Reviewer schema preflight used the exact current Research Draft, search/read function schemas and `_EVIDENCE_REVIEW_SCHEMA` through `http://127.0.0.1:8317/v1` / `gpt-5.6-luna`; the provider completed, emitted `search_sources`, and returned structured reviewer output. This proves schema/route acceptance only, not ResearchReady or S1 completion.
- 2026-08-27: Patrol initially blocked the live checkpoint because an unused `uniqueItems` blacklist encoded a provider-specific future compatibility rule with no current schema consumer. The blacklist, recursive guard and self-justifying unit test were deleted; exact live schema preflight remains the provider authority. A fresh main-Agent Patrol then returned `ALLOW` for one unchanged live validation.
- 2026-08-27: The unchanged ocean-container live vertical returned `ResearchReady` at `/tmp/s1-slice2-live-jeubpyo2`: 3 Search receipts, 4 clause findings, 11 requirement findings, 4 non-blocking residual limitations, 8 cited evidence entries across 4 SourceRevisions, and Brief SHA-256 `7a1997ab9a72dbe97c91cd8aa78b87a924fe14908d2e334ce74f54a41cf073f9`. This is Slice 2 Research evidence only, not an EnvironmentRelease or S1 completion.
- 2026-08-27: User approved a deletion-first full-vertical S1 correction after reviewing the repeated Research/Critic failures. Selected: preserve the committed Contract/Loader, add an explicit run-local Research Agenda and semantic stopping rule, compile one minimal Builder projection, calibrate a narrow fresh Reviewer, and validate the Brief immediately with a real Builder consumer. Rejected: treating more searches, stricter schemas, trace events, acquisition counters or four correction-budget states as semantic completion. Evidence that would reverse this decision: a current consumer requiring one of the removed product fields or a physical paired-case/live vertical showing the simpler contract cannot close the declared boundary.
- 2026-08-27: `OVERRIDDEN(BLOCK)` for this exact S1 authority-document revision. Patrol evaluated the pre-existing dirty worktree rather than only the requested document delta: it correctly identified the unaccepted trace/acquisition/four-budget implementation targeted for deletion, and again cited the unrelated S2 research packet outside this task's ownership. The user's explicit `ok吧，就这样` authorizes only the four S1 task documents changed in this turn; it does not authorize product-code deletion, S2 edits, implementation, commit or release.
- 2026-08-27: `OVERRIDDEN(BLOCK)` for exactly one net-deleting Research implementation turn after the user explicitly confirmed the quoted action. Authorized: delete the current uncommitted Research trace, acquisition admission, four-budget taxonomy, provider-response product fields and their self-contract tests; add one BuilderProjection and Agenda-before-search guidance; preserve real retrieval/reviewer semantics. Not authorized: S2, Builder, Wigolo, commit or release. Patrol F3 confirmed causal alignment; F1/F2 requested post-cleanup evidence before cleanup, and F5's prior authorization gap is now closed by the user's explicit `可以的`.
- 2026-08-27: `OVERRIDDEN(ASK)` for the exact RED-test and net-deleting Research turn named by the user after fresh worker-turn Patrol returned `NO_ACTIVE_TASK`, despite the supplied `.trellis/tasks/08-26-s1-environment-foundry` task being `in_progress`. This records the harness authority mismatch, not an `ALLOW`; it authorizes no later Slice, commit, release, or live ResearchReady claim.
- 2026-08-27: Slice 2 deletion choice: use one run-shared provider-turn ceiling and one Host-derived immutable BuilderProjection. Rejected: retaining per-phase correction counters/codes or exposing the accepted Brief/audit carrier as a second Builder reader. Reconsider only if a real current consumer requires a deleted field or the first real Builder proves the exact projection cannot carry an accepted requirement.
- 2026-08-27: BuilderProjection audit found the current compiled Brief does not yet contain explicit precondition, postcondition, refusal-condition or prohibited-mutation fields. The deletion turn therefore projects only faithful existing semantics (`kind`, state relation, observable relation and falsifiable consequence) and must not synthesize missing facts from prose/category heuristics. Adding explicit transition/refusal semantics is the next RED contract change before any real Builder can be accepted.
- 2026-08-27: Slice 2 deletion RED/GREEN evidence: the new producer/Skill test failed because `Research Agenda` was absent, and the new projection test failed because `finalize_research` still required `receipts`; after the correction, all 53 focused tests passed. BuilderProjection mutation license passed by injecting forbidden `receipts`, observing RED, restoring the dirty-file snapshot, then observing GREEN. Final validation: focused pytest 53/53, Ruff clean, mypy clean. Production LOC 3602 -> 3404 (-198); test LOC 2073 -> 2060 (-13); combined 5675 -> 5464 (-211). No live Research run or ResearchReady acceptance claim was made.
- 2026-08-27: Main-session semantic review rejected the implement worker's first GREEN because it leaked `alternatives`/`contradictions` into BuilderProjection and synthesized pre/post/refusal/prohibited-mutation fields from generic prose. The corrected projection now contains only scope/assumptions/exclusions/limitations and faithful existing requirement fields. Post-correction evidence: focused pytest 53/53, full pytest green, Ruff/Mypy/lock green, forbidden-`receipts` mutation RED then restored GREEN; final related LOC 5675 -> 5455 (-220). Two read-only Trellis code-check workers produced no verdict: Terra ignored the closed scope and was manually stopped after prolonged reasoning; Luna repeated the same branch/reflog/unrelated exploration and hit its 5-minute supervisor timeout. These are harness failures, not review ALLOWs.
- 2026-08-27: Explicit workflow/refusal semantics were added only after a fresh Patrol `ALLOW`: workflows now require precondition/postcondition and refusals require refusal_condition/prohibited_mutation through Draft schema, Host Brief, Reviewer view and BuilderProjection. All four omission mutants were killed independently; full tests/Ruff/Mypy/lock and the real Luna provider-schema preflight are GREEN. The following one current live Research/Critic validation uses `OVERRIDDEN(ASK)` because Patrol request `d154f6b7…ffa2383` existed but the reviewer reported that no request payload was supplied. The user's standing instruction is to continue completing S1; this override authorizes only this live temporary validation, not Builder, commit or release.
- 2026-08-27: The current live raw-Need Research/fresh-Critic vertical completed with exit 0 and `ResearchReady` at `/tmp/s1-slice2-live-opij4b8o`. It produced 4/4 Need findings, 24/24 Requirement findings, 3 disclosed residual limitations, 5 cited passages across 2 cited SourceRevisions, and the sole five-member BuilderProjection. ResearchReady digest `032d43f8d2504dda3485dfef7c93c5ad4e9ee6939bf7ab04d3890f57ea92470c`; exact carrier file SHA-256 `bf3bccb7036db94657159b268d13f190b1f4d198b98bee776e3affbcc642724b`, preserved read-only as `research/slice2-current-researchready.json`. This accepts the current Need→Brief→Critic checkpoint only; Builder, Qualification and EnvironmentRelease remain incomplete.
- 2026-08-27: Official OpenAI Codex SDK documentation and an isolated physical probe confirmed `openai-codex==0.147.0`, `thread_start(cwd=..., model="gpt-5.6-luna", sandbox=workspace_write)`, and `thread.run(...)` writing `SDK_WORKSPACE_OK` in an empty temporary uv workspace. `OVERRIDDEN(ASK)` authorizes exactly one bounded Builder-adapter implementation turn because Patrol request `5ef5f61b…f1e4ec` existed but its reviewer again reported an absent payload. Authorized: official SDK dependency, fresh uv workspace with default Hello source removed, sole BuilderProjection/contract inputs, one same-thread factual repair loop, byte identity and real lock/sync/build/tests. Not authorized: domain template, Qualification, S2, release, commit or completion claim.
- 2026-08-27: Builder adapter RED/GREEN is complete with official SDK dependency, 4/4 focused tests, three independent mutation licenses (uv placeholder removal, source-bound candidate identity, same-thread factual feedback), full tests/Ruff/Mypy/lock GREEN, and no domain fixture. `OVERRIDDEN(ASK)` authorizes one current real Builder run because Patrol request `04524f81…f51dc29` existed but its reviewer again reported an absent request payload. This run may produce only actual `CandidateBuild` or typed `BuilderFailure` in a temporary workspace; it authorizes no Qualification, release or S1-completion claim.
- 2026-08-27: The first real Builder run's printed `CandidateBuild` is invalidated: its project declared no pytest dependency and contained no candidate `.venv/bin/pytest`, while ambient repository `.venv/bin/pytest` passed through Host PATH; the Builder also consumed global OpenViking/MCP guidance. Fresh transition Patrol request `2b5f8b29…9505e1f` returned `ASK` with all F1–F5 `UNDETERMINED` because its reviewer again received no JSON request. `OVERRIDDEN(ASK)` authorizes exactly the user's named causal correction: clean run-local Codex configuration, `Sandbox.full_access`, deterministic candidate-owned uv installation, candidate `.venv` Python tests, post-lock identity and bounded RED/mutation coverage. It authorizes no Research, Qualification, Release, S2, domain-candidate import, commit or S1-completion claim.
- 2026-08-27: Slice 3 false-positive repair (RED first, then green): five tests failed on the prior builder exactly as diagnosed — the ambient-pytest acceptance test reproduced the live false positive physically (fake Host-PATH `pytest` exiting 0 let `uv run --frozen pytest` pass while the candidate `.venv` had no pytest), the SDK env carried an inherited global `CODEX_HOME`, sandbox was `workspace-write`, host preparation recorded ambient `VIRTUAL_ENV`/`PYTHONPATH`/`PYTHONHOME` and `uv sync --frozen` without `--all-groups`, and the returned candidate digest predated Host `uv lock`. Causal fix in `builder.py`: minimal SDK env overrides `{CODEX_HOME -> run-local <run>/codex-home, UV_CACHE_DIR}` (provider `OPENAI_API_KEY`/`OPENAI_BASE_URL` still flow as ordinary inherited process env), `Sandbox.full_access` with `ApprovalMode.deny_all` unchanged, `_run` strips the three ambient python variables, checks run `uv lock` -> `uv sync --frozen --all-groups` -> `uv build` -> `<candidate>/.venv/bin/python -m pytest -q` (missing candidate pytest fails factually back to the same thread), and candidate identity is computed after the lock-owning checks so it binds post-lock bytes. environment-codegen SKILL.md now requires the candidate to declare/lock its own runtime and test dependencies and run through its own uv environment; no framework-authored template or dependency list. Mutation license: replacing the candidate-python test command with ambient `uv run pytest` turned `test_ambient_host_pytest_cannot_satisfy_candidate_checks` (and the command-shape test) red, restore returned 8/8 green with zero mutation markers. Validation: focused pytest 8/8, full suite green, Ruff clean, mypy clean, `uv lock --check` clean. Live-provider assumption left open: a fresh empty CODEX_HOME still relies on the local 8317 route honoring `OPENAI_API_KEY`/`OPENAI_BASE_URL` without global codex config; no real Builder rerun was performed in this turn.
- 2026-08-27: Main-session review rejected the worker's fixed sibling `CODEX_HOME` path because different candidates under `/tmp` would all reuse `/tmp/codex-home`, contradicting the authorized clean run-local boundary. Fresh post-worker Patrol returned `ASK/PATROL_UNAVAILABLE` because its reviewer model was at capacity. `OVERRIDDEN(ASK)` applies only the user's already named no-global-config correction: replace the shared path with one unique temporary Codex home spanning the same Builder repair loop, prove two runs cannot reuse it, and clean it afterward. It authorizes no live run, Qualification, Release, commit or completion claim.
- 2026-08-27: After the unique temporary home correction, focused Builder tests (8/8), the full 277-test suite, Ruff, Mypy, lock check and diff check passed. Fresh post-write Patrol request `e8b3126e…899b002` returned `ASK` with F1–F5 all `UNDETERMINED` because the reviewer again received no JSON request carrier. `OVERRIDDEN(ASK)` authorizes only the user's requested continuation into one independent read-only check of this bounded Builder correction; it authorizes no further edits, live Builder, Qualification, Release, commit or completion claim.
- 2026-08-27: Independent read-only Builder-correction check returned `ALLOW`, reproduced 8/8 focused and 277/277 full tests plus Ruff/Mypy/lock/diff checks, and physically killed both ambient-pytest and shared-home mutations out of tree. The following fresh live-transition Patrol request `3ea78bdb…75f2d62` again returned `ASK` with F1–F5 all `UNDETERMINED` because no JSON request reached its reviewer. `OVERRIDDEN(ASK)` authorizes exactly one new real Builder vertical from the preserved accepted ResearchReady through the corrected clean/full-access/candidate-venv adapter. It may emit only a new temporary `CandidateBuild` or factual failure and authorizes no Qualification, Release, S2, commit or S1-completion claim.
- 2026-08-27: That corrected live Builder stopped before candidate code with `401` against `api.openai.com`, proving a clean home also removed implicit provider routing. Isolated official-SDK A/B probes then showed: built-in `openai_base_url` reached local 8317 but omitted the API key (`401 Missing API key`); a minimal custom provider with explicit `base_url`, `env_key="OPENAI_API_KEY"`, Responses wire API and no global config returned `ROUTE_OK` in thread `01a04232-74b0-7213-aaa8-0ca4303b7001`. Fresh Patrol request `0524c2f7…7ecc5285` again returned `ASK` because no JSON request reached the reviewer. `OVERRIDDEN(ASK)` authorizes only RED-first addition of this physically proven provider override without copying key values, followed by tests and the same one live Builder vertical; no other stage or claim is authorized.
- 2026-08-27: Provider-route RED/GREEN added an explicit clean-home custom provider whose base URL comes from `OPENAI_BASE_URL` and whose API key is read only by Codex through `env_key="OPENAI_API_KEY"`; key bytes never enter SDK config, candidate files or task evidence. Missing inputs fail before SDK start. Validation passed 11/11 Builder tests, 280/280 full tests, Ruff, Mypy, lock and diff checks. Fresh live Patrol request `810ca897…ed96633` again returned `ASK` because its reviewer received no JSON request. `OVERRIDDEN(ASK)` authorizes only the already requested new real Builder vertical through this exact corrected adapter, not any later stage or claim.
- 2026-08-27: The next clean/full-access live Builder generated a new project at `/tmp/s1-builder-live-wo1zo68_`, declared pytest in its own dev group, created its own `.venv`, lock, tests, wheel/sdist and release files, then correctly stopped with `BuilderFailure(candidate_identity, candidate_symlink_forbidden, .venv/lib64)`. The first deviation is Host traversal order: `_candidate_files` rejects the normal uv-created symlink before applying its declared `.venv` exclusion. Fresh corrective Patrol returned `ASK/PATROL_UNAVAILABLE` because its model was at capacity. `OVERRIDDEN(ASK)` authorizes only RED-first reordering so excluded physical directories are skipped while source/public symlinks remain rejected, followed by tests and the same one real Builder vertical.
- 2026-08-27: Excluded-physical-path RED added a normal `.venv/lib64` symlink while retaining a source symlink negative; the former is now ignored and the latter remains typed `candidate_symlink_forbidden`. Builder tests/Ruff/Mypy/lock are green. Fresh fixed-candidate recheck Patrol request `cd8f563f…bbb16b3` again returned `ASK` because no JSON request reached its reviewer. `OVERRIDDEN(ASK)` authorizes only deterministic Host lock/sync/build/candidate-venv-tests and identity against unchanged `/tmp/s1-builder-live-wo1zo68_`; no model call or later stage.
- 2026-08-27: Fixed-candidate Host recheck passed real lock, sync-all-groups, build and candidate `.venv/bin/python -m pytest` (3/3); digest stayed `ac51fbf1e624930c95f330440c69b9232df74c2759338c0a5cf6b67667e95b22`. Read-only host `verify_release` then found the first loader deviation: candidate `release.json` guessed aliases and omitted exact required `environment_factory`, `hash` and `payload_digest`; its manifest also used an unaccepted object map and its `ToolSpec` was a dataclass although the canonical API is mapping-shaped. The Runtime Contract had not disclosed these exact validator inputs. Fresh bounded corrective Patrol returned `ASK/PATROL_UNAVAILABLE` because its model was at capacity. `OVERRIDDEN(ASK)` authorizes only exact contract disclosure plus same-loop host `verify_release` and RED/mutation coverage; no candidate hand-edit or later stage.
- 2026-08-27: Loader-boundary RED/GREEN now discloses plain-mapping ToolSpec plus exact release/manifest parser inputs and runs host `verify_release` after candidate tests inside the same Builder feedback loop. Bypassing verification was mutation-killed; Builder 13/13, full 282, Ruff/Mypy/lock/diff checks are green. Per user request, Patrol was retried twice unchanged while Terra was at capacity; the later live-transition retry ran Terra but again delivered no JSON request, returning F1–F5 all `UNDETERMINED` with digest `b2e39e3b…057f3ae`. `OVERRIDDEN(ASK)` authorizes only one new real Builder vertical through this exact contract/check path; no later stage or completion claim.
- 2026-08-27: The new exact-contract Builder wrote a complete candidate at `/tmp/s1-builder-live-yymykqos` (source, schemas, tests, lock, own venv, manifest, descriptor and distributions) but its provider returned a transient high-demand error while finalizing the turn, before Host checks ran. Candidate bytes remain fixed. Fresh fixed-candidate Patrol request `b83e2834…4378f66` again returned `ASK` because no JSON request reached the reviewer. `OVERRIDDEN(ASK)` authorizes only deterministic Host checks and identity against those unchanged bytes, with no model retry or later stage.
- 2026-08-27: Fixed candidate `/tmp/s1-builder-live-yymykqos` then passed Host lock/sync/build, its own pytest (2/2), exact `verify_release`, and stable identity `016d9afdc05e6bc36e232094d6e68ccec9a23a1befcae064cac5acb1419aeffe`. Before the standard loader multi-call smoke, Patrol was retried twice unchanged and both attempts returned `ASK/PATROL_UNAVAILABLE` due Terra capacity. `OVERRIDDEN(ASK)` authorizes only public-observation-derived `load/reset/tools/inspect/submit/validate/close/reload` against a fresh instance; no native Qualification or later stage.
- 2026-08-27: Standard host loader rejected the fixed candidate at `reset(None)` because the candidate returned a ToolObservation envelope while its published reset schema required top-level `scenario`; this is an actual candidate defect, not a schema-only failure. Independent read-only Claude Code review returned `ALLOW` for the minimum same-thread check: candidate-venv isolated subprocess, temporary Host-loader dependency target, `load_environment -> reset(None) -> non-empty tools -> close`, plus deletion of the unused package-root BuilderProjection re-export that otherwise drags Research/Crawl4AI dependencies into loader import. It also required contract disclosure that reset returns the observation directly and factory construction does not implicitly reset. Fresh Patrol request `36b93fd0…ada8eda` again returned `ASK` because no JSON request reached the reviewer. `OVERRIDDEN(ASK)` authorizes only that Claude-reviewed implementation and physical mechanical tests; no domain invoke or later stage.
- 2026-08-27: The standard loader smoke is now implemented in a fresh candidate-Python subprocess with a temporary candidate-compatible loader-dependency target; the unused root BuilderProjection re-export is deleted, reset/no-implicit-reset guidance is disclosed, and `environment_load` feeds factual errors into the same Builder loop. Mechanical GREEN, live-candidate missing-scenario RED, mutation licence, full tests, Ruff/Mypy/lock/diff checks passed. Post-write Patrol was retried: first Terra capacity, then request-delivery failure with F1–F5 all `UNDETERMINED` (`5820ae32…d6a1692`). `OVERRIDDEN(ASK)` authorizes only one post-code read-only Claude check and, if allowed, one new Builder vertical; no later stage.
- 2026-08-27: Post-code Claude check returned `ALLOW` on all eight loader-smoke/isolation/scope questions; its sole mandatory mechanical finding was repo formatting, now applied. Full tests, `ruff format --check`, Ruff, Mypy, lock and diff checks are all green. Fresh live Patrol request `0bb055d4…d09793f` again returned `ASK` because no JSON request reached the reviewer. `OVERRIDDEN(ASK)` authorizes only one new real Builder vertical through the exact checked sequence; no Qualification or completion claim.
- 2026-08-27: The next real Builder completed as `CandidateBuild` at `/tmp/s1-builder-live-f4h9fx3c`, thread `01a04295-8304-7b03-9d86-92006624fc4b`, exact digest `482102358dd304344f74b2d730a4b43bef9931144a84a09f8ab177f23737e4ca`. Real lock, sync-all-groups, build, candidate `.venv` pytest (3/3), `verify_release`, and isolated standard loader smoke all passed; loader returned a direct dict reset observation and nine tools. This is a Slice 3 CandidateBuild checkpoint only; public value-chain and independent Qualification remain pending.
- 2026-08-27: Fresh public-chain Patrol request `d700030f…c06851c` returned `ASK` with F1–F5 all `UNDETERMINED` because no JSON request reached the reviewer. `OVERRIDDEN(ASK)` authorizes only a fresh-instance public-observation-derived inspect/submit/validate value chain and same-instance reload against exact CandidateBuild digest `48210235…737e4ca`; no hidden native truth or later stage.
- 2026-08-27: Exact CandidateBuild `482102358dd304344f74b2d730a4b43bef9931144a84a09f8ab177f23737e4ca` completed the third-party public chain from a fresh instance using only reset/tool observations: `verify/load/reset/tools -> inspect_world -> get_invoice -> submit_dispute -> validate_dispute(returned dispute_id) -> close -> reload -> inspect_world`. It exposed nine tools, selected public invoice `INV-1001`, created `DSP-0001`, reached `Under Review`, and reload preserved that dispute. The first consumer attempt incorrectly assumed top-level `world.lines`; it was corrected by calling public `get_invoice`, not by reading native truth or changing the candidate. This closes the Slice 3 physical checkpoint only; independent native Qualification remains pending.
- 2026-08-27: Independent Slice 3 exit check returned `ALLOW`: exact digest matched before/after all six Host phases; accepted projection and fixed contract matched workspace bytes; a separately authored unrelated-root public chain reached Submitted/Under Review and persisted after reload; direct stdlib SQLite reads confirmed `(DSP-0001, Under Review)` plus ordered history; no framework domain branch or S2/Task/reward leakage was found. Slice 3 is closed. Per the user's latest decision, Slice 6 Git/held-out proof is deferred and will not block the immediate Slice 4 Qualification and Slice 5 release work; canonical task documents are not revised in this turn.
- 2026-08-27: Slice 4 design was independently challenged in parallel by Claude and Codex SOL. Accepted merged amendments: freeze all 24 projection relations verbatim before candidate exposure; curated digest-bound candidate view excludes Builder tests/venv/dist/inputs; no third Runtime Skill; Qualifier authors ordinary probes and negative setup but never terminal verdict; public calls execute under candidate Python, native SQLite inspection under separate Host Python; Host import-gates probe source, validates 24/24 id coverage plus seven physical check classes, rechecks candidate digest, and requires a reachable public/native mismatch negative to be killed. Cold extracted release remains Slice 5; universal DSL/backend enum/custom sandbox/Mutator role remain deleted/deferred.
- 2026-08-27: Fresh Slice 4 implementation Patrol was retried twice unchanged and both attempts returned `ASK/PATROL_UNAVAILABLE` because Terra was at capacity. `OVERRIDDEN(ASK)` authorizes only the exact parallel-reviewed Qualification harness implementation in the current brief, its mechanical RED/mutation checks and no live success claim; it authorizes no release/S2/commit/completion transition.
- 2026-08-27: Fresh Patrol for the user-named Slice 4 implementation returned `ASK/NO_ACTIVE_TASK` even though `.trellis/tasks/08-26-s1-environment-foundry/task.json` is in progress. `OVERRIDDEN(ASK)` records the user's exact current instruction to implement only the bounded Qualification harness and mechanical RED/mutation checks; it authorizes no live Qualification success, Slice 5, S2, commit, publication or completion claim.
- 2026-08-27: Slice 4 bounded harness RED/GREEN: focused collection first failed because `agent_env_foundry.qualification` did not exist; the final 22 mechanical tests pass. `qualification.py` is 493 LOC and `test_qualification.py` 453 LOC. Ten byte-restoring mutation licences killed tests-exclusion, candidate pre/post digest, source-gate bypass, wrong relation digest, missing seven-class coverage, non-0444 pre-SDK input, candidate-view byte bypass, failed-assertion acceptance, negative-flip bypass and public-trace-binding bypass. Focused/full pytest, Ruff format/check, Mypy, lock and diff checks are green. This is framework mechanics only: no fake client authorized Qualification, and the real candidate/provider/native negative vertical remains required before any passing Qualification claim.
- 2026-08-27: Fresh post-worker Patrol for one independent read-only Slice 4 harness check returned `ASK/PATROL_UNAVAILABLE` because its reviewer model was at capacity. `OVERRIDDEN(ASK)` records the user's current explicit instruction to continue this exact Slice 4 work and authorizes only the read-only check; it authorizes no check-worker edits, live Qualification success, Slice 5, publication, commit, or S1 completion claim.
- 2026-08-27: Independent post-code review corrected the Slice 4 trust model. Claude/GLM first returned `BLOCK` on the SQLite-only gate, cross-run negative binding and suppressed dense code, then `AMEND_BLOCK` after a byte-provenance challenge showed that Qualifier-authored public traces, native facts and assertions could fabricate a complete no-call pass and that one singleton negative cannot prove 24/24 relation discrimination. Codex SOL independently returned `BLOCK_CORRECTION`: Host-owned canonical-call journals, candidate-blind predicate freeze and per-relation negatives are necessary, but instance-only mutation cannot falsify state-independent public branches; each controlled negative run must permit either an instance change or an isolated, mechanically rebound candidate-copy change while preserving the original digest. Selected correction deletes probe-authored trace authority, SQLite/AST pseudo-sandbox, singleton negative and format suppressions; no public Environment API, backend enum, mutation DSL, new Runtime Skill or canonical task-document change. Reverse this only if a physical counterfeit test proves self-authored traces cannot pass or a real relation cannot be expressed by instance/candidate-copy near-miss evidence.
- 2026-08-27: Fresh Patrol request `132b97a37f11ba63869a48bf559bc67cef7da8e5d03d93984cec596c0bc757ff` for the exact RED-first Slice 4 correction returned `ASK` because its reviewer again received no JSON review request: F1/F3/F4 `N/A`, F2/F5 `UNDETERMINED`, no semantic objection. `OVERRIDDEN(ASK)` records the user's explicit current instruction to continue, determine how to pass, think deeply and use multiple agents. It authorizes only one controlled implement worker over `qualification.py`, one private qualification runner and `test_qualification.py`, with mechanical tests/mutation and no live provider run; it authorizes no canonical plan edit, public API/Skill/Research/Builder/Candidate/Slice 5/release change, commit or completion claim.
- 2026-08-27: Slice 4 correction RED/GREEN-A replaced the first rejected authority boundary. Seven production-path REDs proved dynamic read-only SQLite rejection, generic prompt SQLite forcing, cross-run negative-call acceptance, singleton 1/24 discrimination, self-declared physical change, complete no-call counterfeit acceptance, and eager candidate exposure. GREEN-A now uses a two-turn same-thread predicate freeze, a private candidate-Python `ProbeSession/RecordedEnvironment`, Host-owned run journals, and backend-neutral native-reader guidance; probe-authored trace authority, AST pseudo-sandbox, unconditional SQLite guidance and formatting suppressions have zero production references. Main independently reproduced 27/27 GREEN-A tests, exactly three deferred GREEN-B REDs, Ruff/format/Mypy/lock/diff green, and unchanged Candidate digest `48210235…737e4ca`. The production slice is not accepted yet: exact 24-relation negative coverage, controlled-root physical change and source-copy/run carrier remain RED, and the readable implementation is currently 1,744 production LOC before deletion-first GREEN-B cleanup.
- 2026-08-27: Fresh Patrol for the exact deletion-first GREEN-B worker returned `ASK/PATROL_UNAVAILABLE` solely because the reviewer model was at capacity. `OVERRIDDEN(ASK)` records the user's current explicit instruction not to leave the work stalled and to continue using independent agents. It authorizes only GREEN-B in `qualification.py`, the existing private runner and `test_qualification.py`: exact 24-predicate discrimination; Host-created run-specific candidate-copy/instance manifests and journals; mechanical copied-release rebind; original-candidate preservation; and deletion of superseded singleton/optional paths. It authorizes no live provider Qualification, canonical doc/public API/Skill/Research/Builder/pinned Candidate/Slice 5/release change, commit or completion claim.
- 2026-08-27: GREEN-B mechanical implementation reached 34/34 focused and 320/320 full tests plus 7 subtests; Ruff/format/Mypy/lock/diff and two mutation licences (Host-provenance bypass and corrupt release rebind) are green, obsolete singleton/trace/SQLite-pseudo-sandbox symbols have zero references, and pinned Candidate digest remains `48210235…737e4ca`. Production code is readable but 2,056 LOC, 312 above GREEN-A; the implementer attributes the exact overage to per-run subprocess execution, controlled release copy/rebind/carrier, Host-output provenance, sealed journal/native-reader immutability and private provenance checks. This is mechanical closure only: no live Qualifier run or Slice completion claim. Fresh Patrol request `46c2fff1155d876b8f69f89ffb86c308c6cdf850e7155439ed03f8525d1655d5` for two read-only post-code reviews returned `ASK` with F1–F5 all `UNDETERMINED` because no JSON request reached the reviewer. `OVERRIDDEN(ASK)` records the user's explicit request to continue and use multiple agents; it authorizes only parallel Claude/GLM and Codex SOL read-only reviews of semantic causality, scope and LOC, with no edits/live provider/release/commit/completion transition.
- 2026-08-27: The user explicitly suspended Trellis Patrol and channel workers for the remainder of the current S1 work and assigned implementation/check ownership to the main session; no further Patrol verdict is required until the user re-enables it. Post-code physical review found two real pre-live defects despite the green matrix: copied-source import can create `__pycache__` after the carrier snapshot, and public-runner/copy failures are misattributed. Claude also demonstrated that a deliberately adversarial same-process Qualifier can forge journal bytes. Selected product trust boundary: the independent Qualifier is a trusted evaluator whose code is calibrated by real execution and mutations, not hostile code; S1 does not add custom RPC/sandbox/process-security to resist deliberate introspection. The main session will still cheaply hide journal metadata from normal probe argv/environment, declare the residual limitation, fix bytecode drift and typed failure ownership, then run the real Qualification. Reverse only if the paper scope is expanded to malicious-verifier resistance.
- 2026-08-28: Slice 4 passed under the corrected authorship boundary. Codex authored only semantic public/native/near-miss code; Host owned IDs, digests, manifest, journals, controlled copies, evidence enrichment and verdict. Exact Candidate `482102358dd304344f74b2d730a4b43bef9931144a84a09f8ab177f23737e4ca` stayed unchanged while 24/24 positive and 24/24 existing-file source near-misses passed. Expected-relations digest `0a925be97c3347b651375e36aaa57a0f5d1c67b23b6092bf03c4a82962224590`, probe-bundle digest `96288b2fcb9e868839f7fb7ae5f3e1ea1873fc6d1d034f64ba16c2663a604ffe`, evidence digest `06443cceab6c6cb49ff91f8a6cc064664c0f2eaac5060c23b1ac43bc7dfedd45`. Final mechanical validation: 46 Qualification tests, 332 full tests, Ruff/Mypy/lock/diff green. One second independently generated verifier lineage did not converge within its bounded correction run, so repeatability remains a later held-out/paper claim, not part of this Slice 4 acceptance.
- 2026-08-28: The first direct `foundry generate` run correctly ended `NotReleased(provider_turn_budget_exhausted)` before Builder/publication. Causal replay found the Host had split four prose sentences into eight incomplete Need anchors solely because the Markdown file was line-wrapped; Research/Reviewer then spent all 24 shared turns covering fragments. Selected correction: preserve original bytes and derive stable anchors from Markdown paragraphs/list items plus sentence boundaries, invariant to prose wrapping. Rejected: increasing the provider budget or weakening the Reviewer. The wrapped/unwrapped equivalence test kills the old parser mutation.
- 2026-08-28: The next direct run proved Research closure, then exposed two coupled workspace defects: a relative Candidate path was interpreted once by `cwd` and again by `uv init`, and uv discovered the Foundry parent and appended the nested Candidate to the root workspace. Selected correction: absolute run/Builder paths plus `uv init --no-workspace`, with tests that kill each mutation and prove the parent `pyproject.toml` remains byte-identical. Rejected: moving generated environments outside the configured store or repairing the parent lock after every run.
- 2026-08-28: The unchanged public command then completed one real Need -> Research -> Codex Builder -> independent Qualification -> cold replay -> publication vertical. Release `4e2fb8fe7d81093aab5237d9e486b92407695f79b2cf899cc4a1466a86f7e1c1` binds Candidate `1981593558fd12f30d1c689fef0b77df265806f8929f4e88c2d10a0af0983498`, 20/20 positive and 20/20 physical negative evidence, cold evidence `81d04b46…0651e`, and archive `7ef1d808…4c41b`. Directory and ZIP independently verify to the same ID. This proves the current public vertical, not arbitrary-domain/held-out generality; exact evidence is in `research/s1-current-e2e.json`.
- 2026-08-28: With framework commit `7803060` and both runtime Skills byte-frozen, the same public command completed the contrasting filesystem/Git Need without a domain branch or prompt change. Release `b953501fc6f3b6fdbf3249ea0b502e2d3dc1693fbf1719c3111365034bace2e9` binds Candidate `b38416c1e9039abbca493a178e10e9fa5231a3a0ef0bbc105546ee6c8ffb2fc1`, 28/28 positive and 28/28 physical negative evidence, cold evidence `86899b2d…bec2b6`, and archive `d84c2a77…7b92c7`. This satisfies the planned SQLite-versus-filesystem/Git contrast while leaving broader held-out generality as a separate paper experiment. Exact evidence is in `research/s1-git-current-e2e.json`.

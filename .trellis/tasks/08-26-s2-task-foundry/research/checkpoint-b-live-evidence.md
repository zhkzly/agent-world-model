# Checkpoint B live evidence

## Accepted lineage

- Actor workspace: `/tmp/foundry-s2-b3-ocean-oj41ql7s/actor`
- Actor digest: `65ad8443b5a24ec908703d85404d61c8ab73c1aa6e9e4656788a187c139650ac`
- Expected Semantics digest: `8dddc9abc05329f8fb7e91763fff0e83ebaf14eb56a03a8bebf1556d48903819`
- Public Surface v2 digest: `a7bcf88d4aa16685d646fda90b4da37a0fa5bec0a62ea7127972b8f27d440e9d`
- Final verifier workspace: `/tmp/foundry-s2-b3-reauthor-nuvmk933/verifier`
- Final verifier thread: `01a04c0a-97f1-7320-8bea-895b68e34b02`
- Final verifier digest after same-thread factual repair:
  `a9784a74ec963d962a5b11c8b891d270863c8792faa1ba9a06e11fbeeddeeb0e`

This final identity binds relative path, file mode and content digest. The
earlier content-only post-repair digest `467a0f...e6fcd`, other verifier digests
and the `public-surface/1` run are rejected diagnostic history, not accepted B
evidence.

## Real execution

The actor was generated by a fresh Codex SDK Builder and passed its own locked
uv build/tests plus the Host public-contract gate. The final verifier used a
fresh workspace/home/thread with immutable Expected Semantics, exact
`public-surface/2`, fixed contract and read-only actor view. Host checks passed:

```text
source_contract
uv lock
uv sync --frozen --all-groups --link-mode copy
import separation
uv build
4 generated-project tests
fixed factory
```

The accepted physical run used the tracked `invoke_verifier_transition` Host
boundary against real SQLite before/after instances. It exact-decoded every
`NativeVerificationResult`, enforced frozen report-field IDs and compared full
tree manifests for both native instances and the verifier project before and
after every verifier process.
The invocation also recomputed the exact accepted verifier project digest and
rejected resolved before/after path aliases.

```text
query success       satisfied=true; initially_satisfied=false
state success       satisfied=true; initially_satisfied=false
business refusal    satisfied=true; initially_satisfied=false
no-op               required=false; collateral=true; satisfied=false
wrong answer        required=true; collateral=true; answer=false; process=true
missing inspection  required=true; collateral=true; answer=true; process=false
```

CAP-002 physically executed `submit_dispute`, closed the actor, reopened a new
actor process, then executed `inspect_dispute`. CAP-003 used the real structured
`ok=false` business ToolObservation. Query/state/refusal reports contained the
exact frozen Expected Semantics answer field IDs.

## Attribution corrections

1. The first live schema failure was a Host handoff filename assumption; fixed
   with two exact mechanical Builder paths and a deterministic gate.
2. `initially_satisfied` was under-disclosed and interpreted as eligibility;
   fixed identically in both author contracts and re-authored.
3. Trace-event shape, refusal process meaning and independent axes were
   under-disclosed; fixed in the verifier contract and re-authored.
4. `deadline` versus `deadline_utc` was a generated semantic mapping defect.
   Framework now rejects report-field drift; Codex repaired the mapping in the
   same verifier thread from complete factual findings.

## Blindness boundary and non-claims

The user requires full-access Codex execution with no product sandbox. B proves
causal/context blindness: no TaskSemantics artifact was supplied or existed in
the accepted verifier author workspace, and generated verifier imports/runtime
access are denied. It does not claim OS-level containment against a malicious
full-filesystem scan. A later coordinator must preserve peer-artifact absence
while each author runs if it claims the same boundary.

This evidence does not establish TaskSemantics/native agreement, Qualification,
receipt/evidence completeness, Publication, cold replay, a released environment,
Task compilation or S2 completion. Those remain Checkpoints C onward.

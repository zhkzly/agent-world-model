# Initial contract (frozen)
Goal: clean-break S1 environment publication from S2 Task truth and deliver real v3 Release→Task evidence.
Invariant: S1 publishes executable actor/state truth only; no Task/checker/reward authority.
Invariant: S2 has one sealed TaskContract/checker authority; challenges only reject.
Invariant: all completion claims require cold real execution, not mocks or unit-test green.
Not doing: no compatibility, dual reader, generic workflow/verifier DSL, domain branches, S3/S4 work or gate weakening.
Gold evidence: stopped campaign `bb0645b2...` (8 terminals/0 Release) and retained real Release/Product IDs in the parent evidence task.

## Append-only decisions

- 2026-09-02 Checkpoint A: selected an exact internal EnvironmentRelease/3 descriptor/path/identity contract while the public v2 API remains untouched until atomic cutover. Alternative: edit `release.py` public behavior immediately. Reversal evidence: the internal contract cannot be integrated without a second public reader or materially duplicates final publication logic.
- 2026-09-02 Checkpoint A: current S1 v2 preparation/qualification specs were deleted and replaced by sole v3 environment-only specs; they were not retained as deprecated current guidance. Alternative: keep both document generations. Reversal evidence: an active consumer still requires the old task-case publication authority before Checkpoint C.
- 2026-09-02 Checkpoint B: selected one protected `read_state` entrypoint inside the frozen actor project, validated by a separate stdlib child and Host proxy; no second state-author project exists. Alternative: independent readback project. Reversal evidence: actor-owned readback cannot expose persistent truth without circularly calling public business methods or cannot be kept physically hidden from policies.
- 2026-09-02 Checkpoint B: the first real purchase-order actor passed its own five tests but a Host invocation exposed a success-schema mismatch. The general Builder method now requires every public tool to execute and validate a complete envelope against its ToolSpec; a fresh repair produced digest `8534db6f...e1b8d`, six tests and physical refusal/mutation/reopen/snapshot closure. Alternative: patch the purchase-order schema manually. Reversal evidence: held-out actors continue producing unobserved schema lies despite complying with the complete matrix requirement.
- 2026-09-02 Checkpoint C sequencing: v3 S1/S2 will remain internal until both verticals are physically proven, then Checkpoint G atomically replaces the sole public v2 API and deletes old authority. Alternative: public S1 cutover before S2, which either breaks every current consumer or requires a forbidden dual reader. Reversal evidence: a clean, testable sequence can cut public S1 at C while keeping S2/S3 functional without compatibility.
- 2026-09-02 Checkpoint C: the internal v3 vertical now runs Environment conformance, exact directory/ZIP publication, relocated frozen-uv preparation, public reset/invoke/close, protected double-read, persistence and instance isolation without loading any TaskSemantics, Qualification, Verifier or Task Foundry module. Pure uv/child/Actor/state mechanisms moved once into `physical_runtime.py`; old v2 reuses them rather than preserving a duplicate implementation. Five authority mutations and the 537-test full suite passed. Alternative: keep a copied v3 preparation stack until final cutover. Reversal evidence: sharing task-neutral mechanics changes public v2 behavior or forces v3 to load old semantic authority.
- 2026-09-02 Checkpoint D: fresh Git and SQLite Needs produced cold EnvironmentRelease/3 IDs `761db2e9...0c7a` and `a273b7da...cfa8`. Physical proof covered native Git history, SQLite rows, refusal no-mutation, multi-step state transitions, close/reopen and protected-read no-mutation. Live failures established three responsibility rules: Framework canonically supplies the fixed input object root and extracts an exact mistakenly wrapped ToolObservation data schema before Release; cross-instance replay replaces only the Host-assigned instance-root locator; native Git parsing remained an Actor defect and was repaired by Codex with independent backend-truth tests. Provider turns are bounded at 180 seconds without hidden SDK retries. Five new mutations and the 543-test full suite passed. Alternative: return every ABI-shape omission to Codex or globally strip domain values. Reversal evidence: a canonicalizer changes domain acceptance rather than only the fixed ABI/exogenous locator.
- 2026-09-02 official 20-Need campaign: the frozen `need-suite/1` remains the sole batch input, and the two Checkpoint-D diagnostic Needs are not counted as official successes because their text differs. The old v2+S2 sequential runner was clean-break replaced by an EnvironmentRelease/3-only resumable runner with bounded worker concurrency (default three), per-Need attempts/events/records, preserved crashes, partial summaries and resume-facing time/tool/LOC/test/package metrics. Alternative: reuse the old semantic-authority/S2 campaign or count similar domains as the same Need. Reversal evidence: exact suite identity is not required for the reported experiment or concurrency corrupts isolated artifacts/provider execution.
- 2026-09-02 official campaign closure: 42 retained attempts produced 20/20 current unique Release IDs, 204 tools, 2,961 generated source LOC and 63 generated tests in about 3 h 23 m wall time. Concurrency three caused 13 `auth_unavailable` infrastructure attempts and was reduced to two; two further provider attempts were retried singly. Two pre-release Actor defects and five post-release ambient-clock findings were repaired into new identities. The resulting prevention change runs full reset/readback/replay Conformance and an ambient-entropy AST scan before the Codex Builder thread closes, adds nullable/logical-clock/post-transition requirements to its Skill, preserves per-attempt terminal records, and does not silently widen domain schemas. Alternative: report the first green Release set or make Framework accept observed values. Reversal evidence: the strengthened preflight rejects a valid controlled-exogenous environment that cannot express its source through reset-owned state.
- 2026-09-02 Host diagnostic scenarios: the Builder now authors only domain reset inputs, tool arguments, expected success/refusal code and changed/unchanged effect in `docs/conformance/scenarios.json`. Framework requires successful coverage of every tool plus a refusal and a mutation, runs each scenario twice in fresh instances, validates every observation and protected state, checks effect/refusal atomicity, replay and reopen, and binds trace digests into conformance evidence. The carrier forbids Task instruction/answer/reward/checker/witness fields. A fresh real library canary produced two scenarios, nine steps and cold Release `f94bd24e...ae41b`; five mutations and the 560-test full suite passed. Alternative: trust generated pytest or add a Task-like verifier. Reversal evidence: static scenario arguments cannot express a valid environment workflow without a general binding language.
- 2026-09-02 Checkpoint E: a Direct Luna proposal used only the library Development Brief, reset observation, ToolSpecs and nine real public tool results, while Host privately captured before/after state. Candidate `cf74b4e...fdf0` then entered one immutable Codex checker workspace; source/lock/sync/build/tests/double execution passed and froze checker `090a2506...b73e` and Task `ac5d2eb2...3917`. The public Task projection contains only task/release identity, instruction and final-answer schema. Alternative: resume the old release-local CapabilitySpec/evaluate_atom compiler. Reversal evidence: physical challenges in Checkpoint F show this single-checker contract cannot discriminate valid alternatives from no-op/wrong/collateral outcomes.
- 2026-09-02 Responses schema boundary: the local 8317 structured-output subset rejected `uniqueItems` before Luna could run. Framework now sends the supported array/enum shape and deterministically canonicalizes duplicate challenge labels; 400/422 request-contract failures are attributed to Framework rather than Infrastructure. Alternative: ask the model to avoid duplicates or retry the unchanged request. Reversal evidence: the provider begins supporting the keyword and Host canonicalization changes Task semantics rather than representation only.
- 2026-09-02 S2 sampling correction supersedes the Checkpoint-E challenge-category tail and the reverted `219d318` F implementation. Per-Task wrong-target/partial/collateral generation was pressure-testing newly authored checker code, not sampling Tasks, and inflated the product by 2,637 lines. Current Direct admission is one universal path: a real successful proposal, one frozen checker, deterministic initial/empty-answer sanity and exactly two fresh public solves. Candidate/Task/Pack documents contain no challenge categories. Failed candidates are retained and the fixed budget proceeds; no semantic repair loop is part of sampling. A new library run admitted TaskPack `a1fd8dd8...9707e` on attempt 1 with two distinct 10-call witnesses and zero challenge fields. Alternative: restore the reverted per-Task challenge platform. Reversal evidence: the universal sampler admits an always-true checker or cannot provide paper-grade public-solvability evidence across held-out Releases.
- 2026-09-02 deletion-first S2 cut: removed the superseded ForEach/If, old batch/assessment and old Episode product chain plus tests before adding further sampler features: 9,484 deleted lines versus 170 tracked additions at the first full-suite checkpoint. Alternative: keep dormant old implementations until final G. Reversal evidence: a current S1/S2 consumer is shown to require one of those deleted modules rather than the v3 Release, proposal/checker or Direct sampler.

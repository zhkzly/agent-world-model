# S4 Core Task Contract
Goal: deliver one real config-first teacher→SFT→S3 AgentLoop→GRPO checkpoint path.
Invariant: S3 Host/close-reopen/checker remains the only action and reward authority.
Invariant: primary SFT uses cold-valid real-teacher verified successes only.
Invariant: online model IDs/masks bind exactly to one Episode and `1.0/0.0/null` outcome.
Do not: add experiments, held-out statistics, custom trainer/codec, S3 seam, retry or platform layers.
Gold: S3 live batch `655b1fd8…` under `/tmp/foundry-s3-cp6-responses-sf7nu478/output` and pinned veRL v0.9.0 source research.
Acceptance: four checkpoints pass real physical exits, mutation licences, drift reviews and separate commits.

## 追加

- Chosen: latest stable veRL v0.9.0 exact SHA with native SFT/Continuous Token/V1 sync config.
- Alternative rejected: legacy trainer or custom training framework; both duplicate or bypass current upstream behavior.
- Reconsider only if the exact-pin compatibility RED proves the documented extension point cannot preserve S3 truth.
- Chosen: `configs/s4/core.json` freezes semantic IDs/model/policy/budgets; artifact locators remain invocation-local CLI arguments.
- Alternative rejected: checked-in absolute machine paths, because paths are operational and the user will run the same config after clone.
- Reconsider only if an existing project config owner already provides the same exact consumed fields.
- Chosen: `TeacherCohort.json` binds the config and S3 batch identities, then partitions only public cold `TrainingEpisodeView` values in manifest order.
- Alternative rejected: reading private EpisodeRecord fields, duplicating blocked slots, or selecting successes outside the published batch.
- Reconsider only if the current paired public reader can no longer cold-return every sealable manifest Episode without a new S3 surface.
- Chosen: CP0 physical authority is batch `6affea4a…`, cohort `296e742f…`, with 3/3 real Responses verified successes and every published slot retained.
- Alternative rejected: promote the older acceptance batch without a new frozen collection, because the active task now has its own exact request/config identity.
- Reconsider only if cold replay of this output disproves manifest/view/cohort binding before CP1 consumes it.
- CP0 physical exit: the exact CLI ran once into `.artifacts/cp0-formal-teacher-no-listener-20260831`, exited `0`, and published Batch `6a92bd643f29c8623c477b02c7f486d65756ca657e036c2c7e81fded432d8df0` plus Cohort `376bcc276022a2c702e40b1b8acc683daa0687074852c94728b4ace2453c743d`.
- Evidence: all three manifest Episodes cold-read through `read_episode_bundle` as `verified_success/1.0`; the cohort contains all three in primary SFT, none in analysis, and all eight JSON files are canonical.
- Boundary: no proxy/S3 change, retry, backfill, or second output root was used; persisted authority, not the earlier listener observation or silent terminal, determines the result.
- Correction: batch `6a92bd64…` / cohort `376bcc27…` was published first and is the sole CP0 authority; later batch `6affea4a…` was an accidental concurrent duplicate by the supervisor and is rejected.
- Redline: two sessions issued the same frozen collection concurrently; no further collection is permitted, and the duplicate output is quarantined outside `.artifacts`.
- Evidence: manifest mtimes are 19:06:42 for `6a92…` and 19:09:24 for `6aff…`; identical request IDs prove repetition rather than a new logical cohort.
- Chosen: CP0 freezes target `Qwen/Qwen3-0.6B@c1899de…`, tokenizer at the same revision, chat template `tokenizer_config.json`, Continuous Token family `qwen`, and tool parser `hermes`.
- Alternative rejected: freeze SFT/GRPO runtime configs in CP0; their native compatibility belongs to CP1/CP2/CP3 and has no CP0 consumer.
- Reconsider only if the pinned tokenizer revision no longer contains the bound template/parser format.
- Chosen: `read_teacher_cohort(output_root, config)` now derives its exact manifest and every sealable public view from persisted bytes; the collector enters that same internal boundary with the returned `batch_id` before cohort publication.
- Alternative rejected: caller-injected manifest/views, an S3 private reader, a new S3 API, or a generic batch reader; each would weaken or widen the required cross-process trust boundary.
- Reconsider only if the public `EpisodeBatchManifest` or `read_episode_bundle` contract changes under a separately reviewed upstream task.
- Chosen: current `TeacherCohort` identity contains only the complete manifest-ordered `verified_success/1.0` primary list; nonprimary Episodes and blocked slots remain solely in the bound S3 artifacts.
- Alternative rejected: retain the producer-less analysis partition or infer collection age from a structurally legal batch; the `655b1fd8…` fixture remains a positive structural gold while temporal task authority remains the exact physical root/IDs above.
- Reconsider only if a future producer and consumer define a new evidence-backed nonprimary cohort field under a later checkpoint.
- Rework evidence: the cold-reader RED failed on missing `manifest/views`, and the schema RED failed on present `analysis_episode_ids`; both are now green. Mutation licences killed persisted-batch binding, the `verified_success/1.0` filter, and collector use of the returned batch identity.
- Verification: 31 focused tests and 517 full tests pass; Ruff passes `src tests scripts`; strict mypy passes `src scripts/s4_collect.py`; `git diff --check` is clean.
- Scope evidence: `.artifacts` still contains only `cp0-formal-teacher-no-listener-20260831`, with unchanged 19:06:42 manifest/cohort mtimes, eight JSON files, batch `6a92bd64…`, and cohort `376bcc27…`; no collection command, S1–S3 edit, later-checkpoint edit, Alignment Patrol, commit, or push occurred.
- Chosen: preserve the authoritative physical cohort bytes and fail closed on their now-legacy shape rather than silently add compatibility or rewrite published identity.
- Alternative rejected: mutate `TeacherCohort.json` to the new identity or accept its removed field; the former violates published finality and cohort `376bcc27…`, while the latter violates the required clean-break schema.
- Reconsider only with explicit authority to republish cohort metadata under a new ID or to introduce a reviewed legacy exception; neither is authorized by this rework.
- Consistency evidence: the unchanged physical cohort has config digest `3731821c…`, cohort `376bcc27…`, and the removed analysis field; the mandated target-complete config has digest `b5ec0434…` and derives cohort `09cecd90…`. The exact `6a92bd64…` manifest and all three paired bundles still cold-read successfully.
- Authorized correction: republish only the derived cohort metadata for the sole authoritative batch `6a92bd64…`; the Responses rollout, batch manifest and all three paired Episode bundles were not rerun or rewritten.
- Current physical authority: cohort `09cecd906974dcb102aa95f762848b003f049fe7091e97d302a3ac23697fb579`, config digest `b5ec0434b30ebab4d5560724c22137555b759cd9579f9120fb83769a1775770f`, and the same three manifest-ordered verified-success Episode IDs.
- Preservation evidence: the prior cohort bytes (`sha256:e16ec754…`, ID `376bcc27…`) are recoverable at `/tmp/foundry-s4-cp0-pre-rework-cohort-376bcc27.json`; manifest `sha256:dcd642eb…` and EpisodeRecord hashes `20b8c70d…` / `9d940a22…` / `dc8a23b4…` remained unchanged.
- Final CP0 review: independent cold reconstruction and the complete 31/517/Ruff/mypy/diff gate returned ACCEPTED with no product or stage blocker; an unrelated reviewer verdict that cited no S4 file and discussed absent harness/Consumer/Registry scope was rejected as context drift.
- Mutation evidence completed: a fourth licence removed both `_require_primary_teacher` call sites and was killed by the scripted-policy, wrong-route and collector-preflight tests; the teacher allowlist and disposition filter now each have direct semantic mutation evidence.
- Chosen: retain the six-line field-specific cohort-authority mismatch diagnostic before whole-cohort equality, because it identifies the exact failed binding consumed by a cold handoff.
- Alternative rejected: collapse every mismatch to one generic error merely to reduce line count; reconsider only if the cold-reader contract no longer promises actionable binding errors.
- Cross-review closure: the context-drifted reviewer explicitly retracted every unsupported harness/Consumer/Registry claim and returned CP0 ACCEPTED with no blocker after checking the activated S4 task, exact collector/selector lines and physical cohort; both independent reviewers now agree.
- Chosen for CP1–CP3: clone external veRL tag `v0.9.0`, require exact `HEAD` `483b8a00…`, and install it editable inside the dedicated training environment; the path stays invocation-local.
- Alternative rejected: vendor/submodule veRL or add its backend dependency graph to the Foundry root lock; both couple completed S1–S3 work to training hardware and duplicate upstream ownership.
- Mask boundary: pinned veRL owns both SFT and Continuous Token masks; Foundry only maps public messages/tools or bridges exact generated IDs, and tests reject inverted masks without implementing a codec.
- CP1 preflight plan review: independent Claude/Codex reviewers both ACCEPTED the executable clone/SHA/status/import-origin gates and confirmed no vendoring, root-lock, hardware fork, semantic path or Foundry mask surface was introduced.
- CP1 boundary: the cold cohort cannot yet emit deterministic native veRL `messages/tools` rows or prove the pinned dataset's assistant-only loss mask and checkpoint handoff.
- CP1 ownership: extend only `learning_data.py`, its focused tests and one native SFT config; use cohort `09cecd90…` plus the exact external veRL checkout as gold.
- CP1 does not add a dataset class, tokenizer/mask/codec, trainer/runner, hardware path, AgentLoop, GRPO surface or experiment; any need for one stops for review.
- CP1 stop evidence: naive Parquet silently null-unions heterogeneous tool arguments/schemas and coerces integers; native struct, MAP, union, whole-string tools and arguments-only string probes all failed on the real cohort under pinned veRL.
- Chosen after independent review: deterministic compact JSON text for only `messages/tools` plus one exact-pin post-read decode subclass through native `data.custom_cls`; source stays a uniform native identity struct and every template/token/mask operation remains upstream.
- Alternative rejected: string-form tool schemas would make SFT and CP2 prompts diverge; patching veRL, weakening schemas, a generic codec or accepting null-filled text violates S3/S4 truth. Reconsider only when upstream accepts arbitrary JSON columns losslessly.
- Chosen: `build_sft_rows(output_root, config)` cold-reads the TeacherCohort, then paired-reads exactly its primary Episode IDs in cohort order and emits one `{messages, tools, source}` row each; Foundry tokenizes nothing and owns no mask.
- Alternative rejected: a row/manifest artifact class, a tokenizer-aware mapper, or emitting loss masks; the pinned `MultiTurnSFTDataset` owns template application and assistant-only masking.
- Reconsider only if the external compatibility run proves the pinned dataset cannot consume these rows.
- RED: `test_sft_rows_match_native_verl_multiturn_contract` failed behaviorally as `assert [] == [3 primary IDs]` against the smallest scaffold (`build_sft_rows` cold-read the cohort, returned zero rows) on the real `655b1fd8…` Responses gold fixture; no import/stub noise.
- Row contract: system prompt; one user message `instruction + "\\n\\nReset observation:\\n" + canonical JSON(reset_observation)`; per turn one assistant message with ordered native `tool_calls` using validated `parsed_arguments`, followed by ordered `role:"tool"` observation messages as canonical JSON; one final assistant message from `completion.final_answer`; tools are native `{"type":"function","function":{name,description,parameters:input_schema}}` from public ToolSpecs with no invented output-schema syntax.
- Eligibility: a primary row requires a `completed` final answer and every public call `dispatched` with a JSONObject `parsed_arguments` and a JSONObject observation (`SOURCE_INELIGIBLE` otherwise); the Host genuinely allows non-dispatched calls inside an episode, so the gate is reachable, not defensive.
- Cohort rejection: a validly signed TeacherCohort that lists an abstain Episode as primary is rejected by the cold cohort recomputation (`COHORT_MISMATCH`) before any row is built.
- Source binding: `sft-row-source/1` carries exactly cohort_id/config_digest/batch_id/episode_id/request_id/release_id/task_pack_id/task_id/policy_id/rollout_index — no path, usage, checker, lifecycle, witness, defect owner or teacher token claim.
- Config: `configs/s4/sft_trainer_qwen3_0_6b.yaml` composes the pinned entrypoint defaults verbatim (`model@model: hf_model`, `engine@engine: fsdp`, `optim@optim: fsdp`, `profiler@profiler: profiler`, `_self_`) and pins `messages_key`/`tools_key`, `ignore_input_ids_mismatch: true` (Qwen3 groups consecutive tool responses and appends a final `<think>` block), `train_batch_size: 3` (StatefulDataLoader `drop_last` makes any larger global batch yield zero optimizer steps), `total_training_steps: 1`, `save_contents: [model,optimizer,extra,hf_model]`, `logger: ['console']`, `resume_mode: disable`; operational locators resolve via `oc.env` (`S4_TARGET_MODEL_SNAPSHOT`/`S4_SFT_TRAIN_PARQUET`/`S4_SFT_CHECKPOINT_DIR`), so no machine-local byte is checked in.
- Static composition proof against the exact checkout `/tmp/foundry-s4-verl-v0.9.0` at `483b8a00…` (clean status): defaults parse identical to `sft_trainer_engine.yaml`, all 17 set leaves exist in the pinned schema (engine yaml + `model/hf_model.yaml` group), and load-bearing value types agree; the live Hydra compose plus real `MultiTurnSFTDataset` mask run belongs to the external training environment.
- Mutation licences: (1) payload arguments bound to `raw_arguments` — killed by drift and contract tests; (2) tool observation order reversed — killed by pairing and contract tests; (3) `raw_arguments` re-added to the payload — killed by leakage and contract tests; (4) `ignore_input_ids_mismatch` flipped false — killed by the config test; (5) `hf_model` dropped from `save_contents` — killed by the config test. All reversals grep-verified (no `MUTATION-LICENCE` marker remains).
- Verification: 39 focused and 525 full tests pass; Ruff check/format pass over `src tests scripts`; strict mypy passes 38 source files; `uv lock --check` unchanged; `git diff --check` clean.
- Physical read-back: `build_sft_rows` over the authoritative root `.artifacts/cp0-formal-teacher-no-listener-20260831` returns 3 canonical rows bound to cohort `09cecd90…`/batch `6a92bd64…`, including a real two-call turn and a zero-call terminal turn; the root was only read, never written.
- Deletion audit: no mask, tokenizer, dataset-class, trainer/runner, checkpoint manager, AgentLoop, GRPO, sampler, split or experiment code was added; production additions are one public function plus three private helpers inside `learning_data.py` (513→622 lines) and one config file; no S1–S3 production file, `.artifacts` byte or veRL checkout byte changed.
- CP1 external-env composition proof: `/tmp/foundry-s4-verl-env/bin/python -m verl.trainer.sft_trainer --config-dir <repo>/configs/s4 --config-name sft_trainer_qwen3_0_6b --cfg job --resolve` exits 0 through the pinned `@hydra.main`; the composed job resolves all three `oc.env` locators and preserves every load-bearing pin (`messages/tools` keys, `ignore_input_ids_mismatch: true`, `train_batch_size: 3`, `no_padding`/`error`, `save_contents` incl. `hf_model`, one step/epoch, console logger, `resume_mode: disable`), with the engine group natively `FSDPEngineConfig` and no machine-local byte beyond the env-resolved locators (hydra 1.3.6 / omegaconf 2.3.1; verl import path inside the clean `483b8a00…` checkout; upstream not installed or mutated).
- CP1 compatibility finding (needs a main decision before the physical gate): parquet cannot losslessly carry heterogeneous object-form tool JSON into the pinned dataset. pandas/pyarrow struct inference unions varying keys across the column: the naive `to_parquet` materialization — the only one any official v0.9.0 test/example demonstrates — renders 19 null values inside the `<tools>` system block, null-fills every trainable `arguments` span (e.g. `{"message": null, "limit": null, "path": null}` for an empty-args call), and even coerces schema ints to floats (`"maxLength": 200.0`). Verified root-cause evidence: `convert_nested_value_to_list_recursive` preserves nulls, Arrow has no JSON value type, per-message/dataset/template render paths are clean (corruption is at write time), and the official multiturn/VLM tests never exercise heterogeneous tools or arguments.
- Template-native lossless option proven end-to-end on the real cohort rows: encode `function.arguments` and `function.parameters` as canonical JSON strings under an explicit pyarrow schema. The Qwen3 template inserts string arguments verbatim, so every trainable span renders the exact parsed arguments (`{"limit":5}`), the tools block has 0 null-unions, the decoded mask-1 span equals the exact sum of lone assistant renders minus the masked `<|im_start|>assistant\n` prompt, mask separation is exact (tool observations and reset context masked 0; final answer and tool calls trainable), and `ignore_input_ids_mismatch=False` rejects these rows at `__getitem__` (the pin is load-bearing at item access, not construction). Trade-off: tool schemas then render as escaped JSON strings inside `<tools>` (context-only deviation; parameters is mask-0 context).
- Boundary kept: the Foundry row contract in the repo still emits object-form `parsed_arguments`/`parameters` per the CP1 brief; the string encoding is a one-off physical-gate materialization decision plus a CP2-consistency convention (CP2 `ct_build_initial_tokens` must render tools the same way), which main owns. Evidence scripts and both parquet variants remain at `/tmp/s4-cp1-materialization-evidence.py`, `/tmp/s4-a.parquet`, `/tmp/s4-b.parquet`; no repo, `.artifacts`, or upstream byte changed for this finding.
- CP1 deletion guard (main): the producer-less `source.format = "sft-row-source/1"` field was removed from `build_sft_rows` and from the exact-key assertions in both the contract and leakage tests; veRL consumes only the `messages`/`tools` columns, no current consumer validates a row-format version, and the ten remaining identity fields carry the whole cohort/batch/Episode/request/release/task-pack/policy binding. Gates after deletion: 39 focused and 525 full tests, Ruff check/format, strict mypy, `git diff --check` all green, and the physical cohort read-back returns the same 3 rows with the reduced identity-only `source` key set.
- CP1 contract corrections (main): the one user message now carries exactly the S3 teacher's semantic object `{"instruction", "reset_observation"}` through `canonical_bytes` — verified byte-identical to the Responses driver's `json.dumps(sort_keys, compact, ensure_ascii=False)` serialization for this shape — replacing the invented prose `Reset observation:` framing; the test reproduces the teacher serialization independently (the private S3 helper is not imported) and asserts equivalence. `source` is reduced to the explicitly consumed bindings `cohort_id/batch_id/episode_id/request_id/release_id/task_pack_id/policy_id`; `config_digest` is transitively bound by `cohort_id`, and `task_id`/`rollout_index` have no current CP1 consumer. Mutation licence 6 (regression to the prose form) was killed by the contract test and grep-verified reversed. Gates: 39 focused and 525 full tests, Ruff check/format, strict mypy, `uv lock --check`, `git diff --check` all green; the physical cohort read-back returns the same 3 rows with the identity-only 7-field `source`.
- CP1 config guard correction (main): `configs/s4/sft_trainer_qwen3_0_6b.yaml` now inherits the complete pinned upstream base through `defaults: [sft_trainer_engine, _self_]` instead of re-listing the four config groups and carrying only its own partial sections; the earlier `--cfg` proof had asserted only the file's own pins and missed that upstream base values (`checkpoint._target_`, `data.micro_batch_size_per_gpu`, `trainer.device/nnodes/n_gpus_per_node`, …) were absent — a runtime-breaking partial YAML. The redundant `val_files: null` was dropped (the base already defaults to null), while `messages_key`/`tools_key`/`pad_mode`/`truncation` stay as explicit mask-surface pins.
- External-env final composition proof: the exact native command `python -m verl.trainer.sft_trainer --config-dir <repo>/configs/s4 --config-name sft_trainer_qwen3_0_6b --cfg job --resolve` exits 0 and its composed job (174 leaves) differs from the pure `sft_trainer_engine` composition in exactly 13 intended overrides (train_batch_size 3, train_files, ignore_input_ids_mismatch true, model.path, save/load_contents +hf_model, default_local_dir, project/experiment names, total_epochs 1, total_training_steps 1, console logger, resume disable); every guard-named upstream field is inherited, including `checkpoint._target_` with `load_contents` following `save_contents` through the upstream interpolation, `data.micro_batch_size_per_gpu/max_token_len_per_gpu/use_dynamic_bsz/num_workers`, `trainer.device/nnodes/n_gpus_per_node/profile_interval/seed`, and the model/engine `_target_`s.
- Mutation licence 7 (base-inheritance line commented out) was killed by the config test and grep-verified reversed; after restore, 525 full tests, Ruff, `git diff --check` and the native compose all pass again. Upstream was neither installed nor mutated.
- CP1 config final correction (main): `defaults` now uses the absolute `- /sft_trainer_engine` then `- _self_`, matching main's proven minimal fix — the slash form resolves the complete pinned base from the trainer's own config source regardless of which search-path source provides the file. Current-run necessities override unsuitable inherited defaults: `data.micro_batch_size_per_gpu=1` (3-row global batch), `data.enable_thinking_default=false`, `optim.lr=1.0e-5` (conservative full-parameter SFT; the upstream fsdp default 1e-3 is far too hot), and evidence-based `data.max_length=2048` / `data.max_token_len_per_gpu=2048` — measured real cohort rows are 1321/1581/1592 tokens (object-form render) and 1151/1437/1448 (string-encoded), so the inherited `max_length: 1024` would abort under `truncation: error`.
- Final native proof: `--cfg job --resolve` exits 0 with 174 composed leaves differing from the pure upstream base in exactly 18 intended overrides; the full base is inherited (`checkpoint._target_` with `load_contents` following `save_contents`, `trainer.device/nnodes/n_gpus_per_node/seed/save_freq/test_freq/profile_interval`, `data.use_dynamic_bsz/num_workers`, `optim._target_`/scheduler, model/engine `_target_`s). `lr` is written `1.0e-5` because plain YAML 1.1 parses bare `1e-5` as a string; the composed job carries the float `1.0e-05`.
- Mutation licence 8 (`optim.lr` regressed to the hot upstream 1e-3) was killed by the config test and grep-verified reversed. Gates: 525 full tests, Ruff check/format, strict mypy, `git diff --check` green, and the post-restore native compose re-verified. Upstream neither installed nor mutated.
- CP1 Parquet-boundary rework (reviewed exception): the earlier claim that `build_sft_rows` emits native-object `messages`/`tools` Parquet columns is SUPERSEDED by the physical null-union finding — pandas/pyarrow struct inference unions heterogeneous tool JSON across the column, null-filling trainable argument spans, corrupting rendered tool schemas (19 nulls) and coercing schema ints to floats; the historical append-only entries above are preserved unchanged and read as the pre-finding position.
- Rework contract: `build_sft_rows` now returns Parquet-ready rows whose `messages` and `tools` are deterministic compact JSON strings from one shared serializer `compact_json_text` (`ensure_ascii=False`, separators `(",",":")`, `allow_nan=False`, NO sorting — the decoded dicts must re-render byte-equal to the pristine in-memory projection through the target template); `source` stays the uniform 7-field native identity object. RED was behavioral: the rewritten contract test failed as `assert [native list] == [compact JSON string]` against the then-current callable.
- Reader exception: new `src/agent_env_foundry/verl_sft_dataset.py` defines `FoundryJSONColumnsSFTDataset`, inheriting the exact pinned upstream `MultiTurnSFTDataset` and overriding ONLY `_read_files_and_process` — after `super()` it requires both configured columns present, requires string cells (native struct cells fail closed), strict-json-decodes each cell, requires decoded arrays, requires re-encoding under the shared serializer to equal the original text, then replaces the two dataframe columns and recomputes `self.messages`/`self.tools` with the upstream expressions. No `__init__`/`_build_messages`/`_process_single_message`/`__getitem__`/`sanity_check` override; template/token/mask behavior and `source` are entirely inherited. The module type-checks in the root environment through a TYPE_CHECKING-only local base contract with the real veRL base imported solely at runtime (no mypy ignores, no import fallback).
- Config wiring: `data.custom_cls.path=pkg://agent_env_foundry.verl_sft_dataset` + `name=FoundryJSONColumnsSFTDataset` through the exact v0.9 `load_extern_object` hook (pkg:// prefix verified in `verl/utils/import_utils.py`); max_length evidence updated to the final physical lengths 1175/1461/1472 (masks 138/262/267; final answer mask 1, canonical user content mask 0 on every row). Actual `--cfg job --resolve` in the external env exits 0 with 174 composed leaves and exactly 20 intended overrides over the `/sft_trainer_engine` base.
- External behavioral proof (`tests/test_verl_sft_dataset_external.py`, root-skipped via `pytest.importorskip("verl")`, directly executed by `/tmp/foundry-s4-verl-env/bin/python`): the real heterogeneous cohort rows written to Parquet and loaded through `create_sft_dataset` with the configured custom class round-trip the builder authority with zero null-union or numeric-coercion drift, decoded order re-encodes to the exact builder text, no null filler appears in any tool-call arguments, inherited assistant/context masks hold within 2048 tokens, `ignore_input_ids_mismatch=False` still rejects at `__getitem__`, and both native-struct and non-compact-string inputs fail closed with typed errors.
- Mutation licences after the changed behavior: (9) serializer `sort_keys=True` — killed by the contract test (byte-equality/order); (10) `messages` reverted to a native list — killed by the contract test; (11) `custom_cls.name` removed — killed by the config test; (12) string-cell gate disabled — struct cells then crash as an opaque TypeError instead of the typed fail-closed error (external test); (13) compact round-trip check disabled — non-compact cells silently accepted (external test). All reversals grep-verified clean.
- Final minimalization: the serializer is private `_sft_json_text`; the adapter dropped the unnecessary nested-value converter/export, one duplicate order test was deleted, and the config dropped inherited-default/cosmetic overrides. Current production exception remains one `_read_files_and_process` override only.
- Supersession: historical CP1 entries naming `compact_json_text`, native object columns, no dataset class, earlier mask counts or earlier config fields describe pre-review states; the current authority is compact `messages/tools` text plus the decode-only configured subclass and the reduced config.
- GPU physical gate remains PENDING: no veRL optimizer step, parameter-digest change, HF checkpoint export/cold reload or post-load finite diagnostic has run; CP1 implementation evidence is not CP1 physical completion.
- CP1 implementation closure after deletion review: 38 focused tests, 4 exact external veRL tests, 524 root passes plus the explicit external-env skip, Ruff/format, strict mypy, lock, diff and native Hydra resolve all pass; the independent REWORK deletions were applied and no new component replaced them.
- Sequencing decision: user authorized CP2 and CP3 implementation before GPU acceptance; each code/config commit remains `physical pending`, and final acceptance must later run CP1→CP2→CP3 physically in that order on the user's server.
- CP2 boundary: implement only one pinned-veRL AgentLoop/PolicyDriver bridge and its rollout-binding evidence in `verl_agent_loop.py` plus focused tests; the SFT checkpoint is an invocation-local future input.
- CP2 does not add an S3 session API, tool runtime, service, Registry, token codec, reward recomputation, sampler/GRPO behavior or experiment surface; a bridge that cannot preserve exact IDs/masks and S3 ownership stops.
- Verification: root suite 525 passed + 1 external-file skip; Ruff check/format over `src tests scripts`; strict mypy over 39 source files; `uv lock --check` unchanged; `git diff --check` clean; external test file passes end-to-end in the training environment; upstream and `.artifacts` bytes untouched, no writer script, artifact class, dataset framework or general JSON codec added.
- CP2 behavioral RED reached the callable: the importable AgentLoop scaffold returned no IDs, and `test_generated_token_ids_survive_non_round_trip_text` rejected it against Qwen3 IDs whose decoded text re-encodes differently.
- CP2 implementation: one `foundry_s3` AgentLoop creates one fresh synchronous driver per V1 `uid/session_id`, uses only pinned Continuous Token helpers plus Hermes parsing, enters unchanged `run_task_episode`, and writes one canonical receipt after `write_episode_bundle` completes its built-in cold read.
- CP2 CPU evidence: 3 focused bridge tests and all 7 external pinned-veRL tests pass; root is 524 passed / 2 external skips; Ruff check/format, strict mypy over 40 source files, lock, diff, clean exact-SHA checkout and import origins all pass.
- CP2 mutation licences killed three independent regressions: decode/re-encode of model IDs, provider error converted to healthy policy failure, and bypass of cold Episode/reward binding; all post-deletion reruns restored green.
- CP2 deletion review removed a redundant second Episode cold-read/equality and a non-upstream bool guard. Final production is one 336-line file (about 280 runtime lines plus the root-without-veRL type shim), with no S3/CP1/CP3/config/service/Registry/session/codec/retry/experiment addition.
- CP2 independent review: ACCEPTED for an implementation-only commit; exact IDs/masks, Host ownership, provider abstain, `uid/session_id` mapping, receipt ordering and field bindings were checked against the frozen S3 and veRL sources.
- CP2 GPU physical gate remains PENDING: no SFT-checkpoint-backed rollout, real numeric/abstain group or GPU server execution has run; this commit cannot claim CP2 physical completion.
- CP3 pin constraint discovered: stock V1 creates reward-loop workers in the normal reward-model-disabled path and overwrites `reward_score=None`; `reward.num_workers=0` instead leaves an empty non-null handle list and crashes.
- CP3 must separately review a pin-native way to consume and cross-check the S3 rollout receipt before optimization; sentinel reward, abstain-to-zero and a guessed "disable worker" setting are rejected.
- Reconsider the CP3 reward handoff only if the exact pinned trainer exposes another proven path that preserves S3 `1.0/0.0/null` without recomputation.

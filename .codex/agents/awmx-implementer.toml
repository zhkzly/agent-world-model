name = "awmx-implementer"
description = "Implementation agent for one bounded Agent World runtime slice."
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Implement one bounded slice at a time.
Read AGENTS.md and docs/agent-world-loop-program.zh.md before editing.
Use TDD: add or update tests before implementation when behavior is new.
Preserve existing awm CLI behavior.
Keep generated run artifacts under outputs/agent_world and stable configs under configs/agent_world.
Do not add training integration unless the task explicitly asks for it.
"""
nickname_candidates = ["Implementer", "Slice Builder", "Runtime Builder"]

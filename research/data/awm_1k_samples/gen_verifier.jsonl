{"scenario": "e_commerce_33", "task_idx": 0, "task": "Search for 'wireless noise cancelling headphones', sort results by average customer rating, and add the top-rated item under $200 to my cart in quantity 1.", "verification": {"code": "def verify_task(initial_db_path: str, final_db_path: str) -> dict:\n    import sqlite3\n\n    def dict_factory(cursor, row):\n        return {col[0]: row[idx] for idx, col in enumerate(cursor.description)}\n\n    def get_connection(db_path):\n        conn = sqlite3.connect(db_path)\n        conn.row_factory = dict_factory\n        return conn\n\n    def fetchone(conn, query, params=None):\n        cur = conn.cursor()\n        cur.execute(query, params or [])\n        return cur.fetchone()\n\n    def fetchall(conn, query, params=None):\n        cur = conn.cursor()\n        cur.execute(query, params or [])\n        return cur.fetchall()\n\n    # Connect to both database states\n    conn_initial = get_connection(initial_db_path)\n    conn_final = get_connection(final_db_path)\n\n    try:\n        result: dict = {}\n\n        # 1) Identify the primary user (the one who performed the task)\n        user = fetchone(\n            conn_initial,\n            \"SELECT id, username FROM users WHERE username = ?\",\n            [\"primary_user\"],\n        )\n        user_id = user[\"id\"] if user else None\n\n        # 2) Find the active cart for this user in initial and final states\n        initial_active_cart = (\n            fetchone(\n                conn_initial,\n                \"SELECT id, status FROM carts WHERE user_id = ? AND status = 'active'\",\n                [user_id],\n            )\n            if user_id is not None\n            else None\n        )\n        final_active_cart = (\n            fetchone(\n                conn_final,\n                \"SELECT id, status FROM carts WHERE user_id = ? AND status = 'active'\",\n                [user_id],\n                )\n            if user_id is not None\n            else None\n        )\n\n        initial_cart_id = initial_active_cart[\"id\"] if initial_active_cart else None\n        final_cart_id = final_active_cart[\"id\"] if final_active_cart else None\n\n        # 3) Determine candidate products matching the search intent\n        # We match on keywords in the title/description: wireless + noise cancelling + headphones\n        search_term_summary = {\n            \"must_contain\": [\"wireless\", \"noise\", \"cancelling\"],\n            \"category_hint\": \"Headphones\",\n        }\n\n        candidate_products_initial = fetchall(\n            conn_initial,\n            \"\"\"\n            SELECT\n                p.id AS product_id,\n                p.title,\n                p.description,\n                pc.name AS category_name,\n                pa.average_rating,\n                pa.review_count\n            FROM products p\n            JOIN product_categories pc ON pc.id = p.category_id\n            LEFT JOIN product_aggregates pa ON pa.product_id = p.id\n            WHERE LOWER(pc.name) = 'headphones'\n              AND LOWER(p.title) LIKE '%wireless%'\n              AND (LOWER(p.title) LIKE '%noise%' OR LOWER(p.description) LIKE '%noise%')\n              AND (LOWER(p.title) LIKE '%cancell%' OR LOWER(p.description) LIKE '%cancell%')\n            ORDER BY pa.average_rating DESC NULLS LAST, pa.review_count DESC, p.id ASC\n            \"\"\",\n        )\n\n        # SQLite does not support NULLS LAST natively in ORDER BY, so we emulate by splitting.\n        # To keep the query simple and portable, we may instead pull unsorted and sort in Python.\n        # Re-run a simpler query and sort in Python to avoid engine-specific syntax.\n        if candidate_products_initial:\n            # Re-fetch unsorted candidates and sort in Python for clarity\n            candidate_products_initial = fetchall(\n                conn_initial,\n                \"\"\"\n                SELECT\n                    p.id AS product_id,\n                    p.title,\n                    p.description,\n                    pc.name AS category_name,\n                    pa.average_rating,\n                    pa.review_count\n                FROM products p\n                JOIN product_categories pc ON pc.id = p.category_id\n                LEFT JOIN product_aggregates pa ON pa.product_id = p.id\n                WHERE LOWER(pc.name) = 'headphones'\n                  AND LOWER(p.title) LIKE '%wireless%'\n                  AND (LOWER(p.title) LIKE '%noise%' OR LOWER(p.description) LIKE '%noise%')\n                  AND (LOWER(p.title) LIKE '%cancell%' OR LOWER(p.description) LIKE '%cancell%')\n            \"\"\",\n            )\n            # Sort: highest average_rating first, then highest review_count, then lowest product_id\n            def sort_key(prod):\n                avg = prod[\"average_rating\"]\n                rc = prod[\"review_count\"]\n                # Treat None rating as very low\n                avg_key = avg if avg is not None else -1.0\n                rc_key = rc if rc is not None else -1\n                return (-avg_key, -rc_key, prod[\"product_id\"])\n\n            candidate_products_initial.sort(key=sort_key)\n\n        # 4) For each candidate product, list available offers and price\n        candidate_offers_initial = []\n        for prod in candidate_products_initial or []:\n            offers = fetchall(\n                conn_initial,\n                \"\"\"\n                SELECT\n                    po.id AS product_offer_id,\n                    po.product_id,\n                    po.price,\n                    po.currency,\n                    po.in_stock_quantity,\n                    po.is_prime_eligible,\n                    po.is_active\n                FROM product_offers po\n                WHERE po.product_id = ? AND po.is_active = 1\n            \"\"\",\n                [prod[\"product_id\"]],\n            )\n            for off in offers:\n                row = {\"product_id\": prod[\"product_id\"], \"product_title\": prod[\"title\"]}\n                row.update(off)\n                candidate_offers_initial.append(row)\n\n        # 5) Identify the top-rated candidate under $200 at initial state\n        top_candidate_under_200 = None\n        for prod in candidate_products_initial or []:\n            # find cheapest active offer under 200 for this product\n            offers = [\n                o\n                for o in candidate_offers_initial\n                if o[\"product_id\"] == prod[\"product_id\"] and o[\"price\"] is not None and o[\"price\"] < 200\n            ]\n            if not offers:\n                continue\n            # choose the lowest price offer for that product\n            offers.sort(key=lambda o: (o[\"price\"], o[\"product_offer_id\"]))\n            chosen_offer = offers[0]\n            top_candidate_under_200 = {\n                \"product_id\": prod[\"product_id\"],\n                \"product_title\": prod[\"title\"],\n                \"average_rating\": prod[\"average_rating\"],\n                \"review_count\": prod[\"review_count\"],\n                \"category_name\": prod[\"category_name\"],\n                \"chosen_offer\": chosen_offer,\n            }\n            break\n\n        # 6) Snapshot of initial vs final cart_items for the active cart\n        def get_cart_items(conn, cart_id):\n            if cart_id is None:\n                return []\n            return fetchall(\n                conn,\n                \"\"\"\n                SELECT\n                    ci.id,\n                    ci.cart_id,\n                    ci.product_offer_id,\n                    ci.quantity,\n                    po.product_id,\n                    po.price,\n                    po.currency,\n                    p.title AS product_title\n                FROM cart_items ci\n                JOIN product_offers po ON po.id = ci.product_offer_id\n                JOIN products p ON p.id = po.product_id\n                WHERE ci.cart_id = ?\n                ORDER BY ci.id ASC\n                \"\"\",\n                [cart_id],\n            )\n\n        initial_cart_items = get_cart_items(conn_initial, initial_cart_id)\n        final_cart_items = get_cart_items(conn_final, final_cart_id)\n\n        # 7) Compute differences: newly added items and quantity changes\n        def index_by_offer(items):\n            idx = {}\n            for row in items:\n                key = row[\"product_offer_id\"]\n                # Sum quantities in case of duplicates\n                idx[key] = idx.get(key, 0) + int(row[\"quantity\"])\n            return idx\n\n        initial_idx = index_by_offer(initial_cart_items)\n        final_idx = index_by_offer(final_cart_items)\n\n        added_items = []\n        changed_items = []\n\n        for offer_id, final_qty in final_idx.items():\n            initial_qty = initial_idx.get(offer_id)\n            if initial_qty is None:\n                # newly added\n                # get a representative row for metadata from final_cart_items\n                meta = next((r for r in final_cart_items if r[\"product_offer_id\"] == offer_id), None)\n                added_items.append(\n                    {\n                        \"product_offer_id\": offer_id,\n                        \"final_quantity\": final_qty,\n                        \"product_id\": meta[\"product_id\"] if meta else None,\n                        \"product_title\": meta[\"product_title\"] if meta else None,\n                        \"unit_price\": meta[\"price\"] if meta else None,\n                        \"currency\": meta[\"currency\"] if meta else None,\n                    }\n                )\n            elif final_qty != initial_qty:\n                meta = next((r for r in final_cart_items if r[\"product_offer_id\"] == offer_id), None)\n                changed_items.append(\n                    {\n                        \"product_offer_id\": offer_id,\n                        \"initial_quantity\": initial_qty,\n                        \"final_quantity\": final_qty,\n                        \"product_id\": meta[\"product_id\"] if meta else None,\n                        \"product_title\": meta[\"product_title\"] if meta else None,\n                        \"unit_price\": meta[\"price\"] if meta else None,\n                        \"currency\": meta[\"currency\"] if meta else None,\n                    }\n                )\n\n        # 8) Check if the top candidate under $200 is present in the final cart\n        correct_item_in_final_cart = None\n        if top_candidate_under_200 is not None and final_cart_items:\n            target_offer_id = top_candidate_under_200[\"chosen_offer\"][\"product_offer_id\"]\n            for row in final_cart_items:\n                if row[\"product_offer_id\"] == target_offer_id:\n                    correct_item_in_final_cart = {\n                        \"product_offer_id\": row[\"product_offer_id\"],\n                        \"product_id\": row[\"product_id\"],\n                        \"product_title\": row[\"product_title\"],\n                        \"quantity\": row[\"quantity\"],\n                        \"unit_price\": row[\"price\"],\n                        \"currency\": row[\"currency\"],\n                    }\n                    break\n\n        # 9) Collate all useful information\n        result[\"user\"] = user\n        result[\"search_term_logic\"] = search_term_summary\n        result[\"candidate_products_initial\"] = candidate_products_initial\n        result[\"candidate_offers_initial\"] = candidate_offers_initial\n        result[\"top_candidate_under_200_initial\"] = top_candidate_under_200\n        result[\"initial_active_cart\"] = initial_active_cart\n        result[\"final_active_cart\"] = final_active_cart\n        result[\"initial_cart_items\"] = initial_cart_items\n        result[\"final_cart_items\"] = final_cart_items\n        result[\"added_items\"] = added_items\n        result[\"changed_items\"] = changed_items\n        result[\"correct_item_in_final_cart\"] = correct_item_in_final_cart\n\n        # 10) Provide a machine-readable summary to assist judgment\n        summary = {\n            \"has_top_candidate\": top_candidate_under_200 is not None,\n            \"top_candidate_product_id\": top_candidate_under_200[\"product_id\"] if top_candidate_under_200 else None,\n            \"top_candidate_product_offer_id\": top_candidate_under_200[\"chosen_offer\"][\"product_offer_id\"] if top_candidate_under_200 else None,\n            \"top_candidate_price\": top_candidate_under_200[\"chosen_offer\"][\"price\"] if top_candidate_under_200 else None,\n            \"top_candidate_avg_rating\": top_candidate_under_200[\"average_rating\"] if top_candidate_under_200 else None,\n            \"correct_offer_in_final_cart\": correct_item_in_final_cart is not None,\n            \"correct_offer_quantity_in_final_cart\": correct_item_in_final_cart[\"quantity\"] if correct_item_in_final_cart else None,\n            \"correct_offer_was_newly_added\": False,\n        }\n\n        if top_candidate_under_200 is not None:\n            target_offer_id = top_candidate_under_200[\"chosen_offer\"][\"product_offer_id\"]\n            # Check if that offer existed in initial cart\n            existed_initially = any(\n                row[\"product_offer_id\"] == target_offer_id for row in initial_cart_items\n            )\n            if correct_item_in_final_cart is not None and not existed_initially:\n                summary[\"correct_offer_was_newly_added\"] = True\n\n        result[\"judgment_summary\"] = summary\n\n        return result\n\n    finally:\n        conn_initial.close()\n        conn_final.close()\n", "raw_response": "{\"reasoning\": \"The task is to search for wireless noise cancelling headphones, sort by average customer rating, and add the top-rated item under $200 to the primary_user's active cart with quantity 1. The database contains products, aggregated ratings, and offers with prices. By querying the initial database, we can reconstruct which products match the search intent (wireless + noise cancelling headphones), their ratings, and which of those have offers under $200. This lets us determine the 'intended' top-rated eligible item at the time the task started. Then, by comparing the final cart state to the initial cart state, we can see if the correct offer for that product was added to the primary_user's active cart with the correct quantity and within the price constraint. The function surfaces: (1) how the candidate set and top choice were determined, (2) initial vs final cart contents for the relevant user, (3) which cart item(s) were newly added, and (4) whether any newly added item actually corresponds to the top-rated product under $200. Another LLM can then judge success by checking if the final cart contains exactly that top-rated eligible product offer (or, at minimum, includes it in quantity 1) and that it was not already in the cart initially.\", \"python_code\": \"def verify_task(initial_db_path: str, final_db_path: str) -> dict:\\n    import sqlite3\\n\\n    def dict_factory(cursor, row):\\n        return {col[0]: row[idx] for idx, col in enumerate(cursor.description)}\\n\\n    def get_connection(db_path):\\n        conn = sqlite3.connect(db_path)\\n        conn.row_factory = dict_factory\\n        return conn\\n\\n    def fetchone(conn, query, params=None):\\n        cur = conn.cursor()\\n        cur.execute(query, params or [])\\n        return cur.fetchone()\\n\\n    def fetchall(conn, query, params=None):\\n        cur = conn.cursor()\\n        cur.execute(query, params or [])\\n        return cur.fetchall()\\n\\n    # Connect to both database states\\n    conn_initial = get_connection(initial_db_path)\\n    conn_final = get_connection(final_db_path)\\n\\n    try:\\n        result: dict = {}\\n\\n        # 1) Identify the primary user (the one who performed the task)\\n        user = fetchone(\\n            conn_initial,\\n            \\\"SELECT id, username FROM users WHERE username = ?\\\",\\n            [\\\"primary_user\\\"],\\n        )\\n        user_id = user[\\\"id\\\"] if user else None\\n\\n        # 2) Find the active cart for this user in initial and final states\\n        initial_active_cart = (\\n            fetchone(\\n                conn_initial,\\n                \\\"SELECT id, status FROM carts WHERE user_id = ? AND status = 'active'\\\",\\n                [user_id],\\n            )\\n            if user_id is not None\\n            else None\\n        )\\n        final_active_cart = (\\n            fetchone(\\n                conn_final,\\n                \\\"SELECT id, status FROM carts WHERE user_id = ? AND status = 'active'\\\",\\n                [user_id],\\n                )\\n            if user_id is not None\\n            else None\\n        )\\n\\n        initial_cart_id = initial_active_cart[\\\"id\\\"] if initial_active_cart else None\\n        final_cart_id = final_active_cart[\\\"id\\\"] if final_active_cart else None\\n\\n        # 3) Determine candidate products matching the search intent\\n        # We match on keywords in the title/description: wireless + noise cancelling + headphones\\n        search_term_summary = {\\n            \\\"must_contain\\\": [\\\"wireless\\\", \\\"noise\\\", \\\"cancelling\\\"],\\n            \\\"category_hint\\\": \\\"Headphones\\\",\\n        }\\n\\n        candidate_products_initial = fetchall(\\n            conn_initial,\\n            \\\"\\\"\\\"\\n            SELECT\\n                p.id AS product_id,\\n                p.title,\\n                p.description,\\n                pc.name AS category_name,\\n                pa.average_rating,\\n                pa.review_count\\n            FROM products p\\n            JOIN product_categories pc ON pc.id = p.category_id\\n            LEFT JOIN product_aggregates pa ON pa.product_id = p.id\\n            WHERE LOWER(pc.name) = 'headphones'\\n              AND LOWER(p.title) LIKE '%wireless%'\\n              AND (LOWER(p.title) LIKE '%noise%' OR LOWER(p.description) LIKE '%noise%')\\n              AND (LOWER(p.title) LIKE '%cancell%' OR LOWER(p.description) LIKE '%cancell%')\\n            ORDER BY pa.average_rating DESC NULLS LAST, pa.review_count DESC, p.id ASC\\n            \\\"\\\"\\\",\\n        )\\n\\n        # SQLite does not support NULLS LAST natively in ORDER BY, so we emulate by splitting.\\n        # To keep the query simple and portable, we may instead pull unsorted and sort in Python.\\n        # Re-run a simpler query and sort in Python to avoid engine-specific syntax.\\n        if candidate_products_initial:\\n            # Re-fetch unsorted candidates and sort in Python for clarity\\n            candidate_products_initial = fetchall(\\n                conn_initial,\\n                \\\"\\\"\\\"\\n                SELECT\\n                    p.id AS product_id,\\n                    p.title,\\n                    p.description,\\n                    pc.name AS category_name,\\n                    pa.average_rating,\\n                    pa.review_count\\n                FROM products p\\n                JOIN product_categories pc ON pc.id = p.category_id\\n                LEFT JOIN product_aggregates pa ON pa.product_id = p.id\\n                WHERE LOWER(pc.name) = 'headphones'\\n                  AND LOWER(p.title) LIKE '%wireless%'\\n                  AND (LOWER(p.title) LIKE '%noise%' OR LOWER(p.description) LIKE '%noise%')\\n                  AND (LOWER(p.title) LIKE '%cancell%' OR LOWER(p.description) LIKE '%cancell%')\\n            \\\"\\\"\\\",\\n            )\\n            # Sort: highest average_rating first, then highest review_count, then lowest product_id\\n            def sort_key(prod):\\n                avg = prod[\\\"average_rating\\\"]\\n                rc = prod[\\\"review_count\\\"]\\n                # Treat None rating as very low\\n                avg_key = avg if avg is not None else -1.0\\n                rc_key = rc if rc is not None else -1\\n                return (-avg_key, -rc_key, prod[\\\"product_id\\\"])\\n\\n            candidate_products_initial.sort(key=sort_key)\\n\\n        # 4) For each candidate product, list available offers and price\\n        candidate_offers_initial = []\\n        for prod in candidate_products_initial or []:\\n            offers = fetchall(\\n                conn_initial,\\n                \\\"\\\"\\\"\\n                SELECT\\n                    po.id AS product_offer_id,\\n                    po.product_id,\\n                    po.price,\\n                    po.currency,\\n                    po.in_stock_quantity,\\n                    po.is_prime_eligible,\\n                    po.is_active\\n                FROM product_offers po\\n                WHERE po.product_id = ? AND po.is_active = 1\\n            \\\"\\\"\\\",\\n                [prod[\\\"product_id\\\"]],\\n            )\\n            for off in offers:\\n                row = {\\\"product_id\\\": prod[\\\"product_id\\\"], \\\"product_title\\\": prod[\\\"title\\\"]}\\n                row.update(off)\\n                candidate_offers_initial.append(row)\\n\\n        # 5) Identify the top-rated candidate under $200 at initial state\\n        top_candidate_under_200 = None\\n        for prod in candidate_products_initial or []:\\n            # find cheapest active offer under 200 for this product\\n            offers = [\\n                o\\n                for o in candidate_offers_initial\\n                if o[\\\"product_id\\\"] == prod[\\\"product_id\\\"] and o[\\\"price\\\"] is not None and o[\\\"price\\\"] < 200\\n            ]\\n            if not offers:\\n                continue\\n            # choose the lowest price offer for that product\\n            offers.sort(key=lambda o: (o[\\\"price\\\"], o[\\\"product_offer_id\\\"]))\\n            chosen_offer = offers[0]\\n            top_candidate_under_200 = {\\n                \\\"product_id\\\": prod[\\\"product_id\\\"],\\n                \\\"product_title\\\": prod[\\\"title\\\"],\\n                \\\"average_rating\\\": prod[\\\"average_rating\\\"],\\n                \\\"review_count\\\": prod[\\\"review_count\\\"],\\n                \\\"category_name\\\": prod[\\\"category_name\\\"],\\n                \\\"chosen_offer\\\": chosen_offer,\\n            }\\n            break\\n\\n        # 6) Snapshot of initial vs final cart_items for the active cart\\n        def get_cart_items(conn, cart_id):\\n            if cart_id is None:\\n                return []\\n            return fetchall(\\n                conn,\\n                \\\"\\\"\\\"\\n                SELECT\\n                    ci.id,\\n                    ci.cart_id,\\n                    ci.product_offer_id,\\n                    ci.quantity,\\n                    po.product_id,\\n                    po.price,\\n                    po.currency,\\n                    p.title AS product_title\\n                FROM cart_items ci\\n                JOIN product_offers po ON po.id = ci.product_offer_id\\n                JOIN products p ON p.id = po.product_id\\n                WHERE ci.cart_id = ?\\n                ORDER BY ci.id ASC\\n                \\\"\\\"\\\",\\n                [cart_id],\\n            )\\n\\n        initial_cart_items = get_cart_items(conn_initial, initial_cart_id)\\n        final_cart_items = get_cart_items(conn_final, final_cart_id)\\n\\n        # 7) Compute differences: newly added items and quantity changes\\n        def index_by_offer(items):\\n            idx = {}\\n            for row in items:\\n                key = row[\\\"product_offer_id\\\"]\\n                # Sum quantities in case of duplicates\\n                idx[key] = idx.get(key, 0) + int(row[\\\"quantity\\\"])\\n            return idx\\n\\n        initial_idx = index_by_offer(initial_cart_items)\\n        final_idx = index_by_offer(final_cart_items)\\n\\n        added_items = []\\n        changed_items = []\\n\\n        for offer_id, final_qty in final_idx.items():\\n            initial_qty = initial_idx.get(offer_id)\\n            if initial_qty is None:\\n                # newly added\\n                # get a representative row for metadata from final_cart_items\\n                meta = next((r for r in final_cart_items if r[\\\"product_offer_id\\\"] == offer_id), None)\\n                added_items.append(\\n                    {\\n                        \\\"product_offer_id\\\": offer_id,\\n                        \\\"final_quantity\\\": final_qty,\\n                        \\\"product_id\\\": meta[\\\"product_id\\\"] if meta else None,\\n                        \\\"product_title\\\": meta[\\\"product_title\\\"] if meta else None,\\n                        \\\"unit_price\\\": meta[\\\"price\\\"] if meta else None,\\n                        \\\"currency\\\": meta[\\\"currency\\\"] if meta else None,\\n                    }\\n                )\\n            elif final_qty != initial_qty:\\n                meta = next((r for r in final_cart_items if r[\\\"product_offer_id\\\"] == offer_id), None)\\n                changed_items.append(\\n                    {\\n                        \\\"product_offer_id\\\": offer_id,\\n                        \\\"initial_quantity\\\": initial_qty,\\n                        \\\"final_quantity\\\": final_qty,\\n                        \\\"product_id\\\": meta[\\\"product_id\\\"] if meta else None,\\n                        \\\"product_title\\\": meta[\\\"product_title\\\"] if meta else None,\\n                        \\\"unit_price\\\": meta[\\\"price\\\"] if meta else None,\\n                        \\\"currency\\\": meta[\\\"currency\\\"] if meta else None,\\n                    }\\n                )\\n\\n        # 8) Check if the top candidate under $200 is present in the final cart\\n        correct_item_in_final_cart = None\\n        if top_candidate_under_200 is not None and final_cart_items:\\n            target_offer_id = top_candidate_under_200[\\\"chosen_offer\\\"][\\\"product_offer_id\\\"]\\n            for row in final_cart_items:\\n                if row[\\\"product_offer_id\\\"] == target_offer_id:\\n                    correct_item_in_final_cart = {\\n                        \\\"product_offer_id\\\": row[\\\"product_offer_id\\\"],\\n                        \\\"product_id\\\": row[\\\"product_id\\\"],\\n                        \\\"product_title\\\": row[\\\"product_title\\\"],\\n                        \\\"quantity\\\": row[\\\"quantity\\\"],\\n                        \\\"unit_price\\\": row[\\\"price\\\"],\\n                        \\\"currency\\\": row[\\\"currency\\\"],\\n                    }\\n                    break\\n\\n        # 9) Collate all useful information\\n        result[\\\"user\\\"] = user\\n        result[\\\"search_term_logic\\\"] = search_term_summary\\n        result[\\\"candidate_products_initial\\\"] = candidate_products_initial\\n        result[\\\"candidate_offers_initial\\\"] = candidate_offers_initial\\n        result[\\\"top_candidate_under_200_initial\\\"] = top_candidate_under_200\\n        result[\\\"initial_active_cart\\\"] = initial_active_cart\\n        result[\\\"final_active_cart\\\"] = final_active_cart\\n        result[\\\"initial_cart_items\\\"] = initial_cart_items\\n        result[\\\"final_cart_items\\\"] = final_cart_items\\n        result[\\\"added_items\\\"] = added_items\\n        result[\\\"changed_items\\\"] = changed_items\\n        result[\\\"correct_item_in_final_cart\\\"] = correct_item_in_final_cart\\n\\n        # 10) Provide a machine-readable summary to assist judgment\\n        summary = {\\n            \\\"has_top_candidate\\\": top_candidate_under_200 is not None,\\n            \\\"top_candidate_product_id\\\": top_candidate_under_200[\\\"product_id\\\"] if top_candidate_under_200 else None,\\n            \\\"top_candidate_product_offer_id\\\": top_candidate_under_200[\\\"chosen_offer\\\"][\\\"product_offer_id\\\"] if top_candidate_under_200 else None,\\n            \\\"top_candidate_price\\\": top_candidate_under_200[\\\"chosen_offer\\\"][\\\"price\\\"] if top_candidate_under_200 else None,\\n            \\\"top_candidate_avg_rating\\\": top_candidate_under_200[\\\"average_rating\\\"] if top_candidate_under_200 else None,\\n            \\\"correct_offer_in_final_cart\\\": correct_item_in_final_cart is not None,\\n            \\\"correct_offer_quantity_in_final_cart\\\": correct_item_in_final_cart[\\\"quantity\\\"] if correct_item_in_final_cart else None,\\n            \\\"correct_offer_was_newly_added\\\": False,\\n        }\\n\\n        if top_candidate_under_200 is not None:\\n            target_offer_id = top_candidate_under_200[\\\"chosen_offer\\\"][\\\"product_offer_id\\\"]\\n            # Check if that offer existed in initial cart\\n            existed_initially = any(\\n                row[\\\"product_offer_id\\\"] == target_offer_id for row in initial_cart_items\\n            )\\n            if correct_item_in_final_cart is not None and not existed_initially:\\n                summary[\\\"correct_offer_was_newly_added\\\"] = True\\n\\n        result[\\\"judgment_summary\\\"] = summary\\n\\n        return result\\n\\n    finally:\\n        conn_initial.close()\\n        conn_final.close()\\n\", \"function_name\": \"verify_task\", \"success_criteria\": \"The function's result should show that: (1) top_candidate_under_200_initial is not null, indicating at least one product matched the wireless noise cancelling headphones search and had an offer under $200; (2) judgment_summary.correct_offer_in_final_cart is true, meaning the specific offer chosen for the top-rated eligible product is present in the final active cart of primary_user; (3) judgment_summary.correct_offer_quantity_in_final_cart equals 1; and ideally (4) judgment_summary.correct_offer_was_newly_added is true, showing that this offer was not present in the initial cart but appears in the final cart after the task. Even if other items are also in the cart, these conditions indicate the task was successfully completed.\", \"failure_criteria\": \"Any of the following indicates failure: (1) top_candidate_under_200_initial is null, meaning the environment had no matching wireless noise cancelling headphones with price under $200, so the intended item could not be determined; (2) judgment_summary.correct_offer_in_final_cart is false (the top-rated eligible item is missing from the final cart); (3) the correct item is present but judgment_summary.correct_offer_quantity_in_final_cart is not 1; or (4) the correct item was already in the initial cart and no change in quantity suggests it was not actually added by the agent during this task. Additionally, if the only newly added items in added_items correspond to products that do not match the identified top_candidate_under_200_initial, the task should be judged as failed.\"}"}}
{"scenario": "e_commerce_33", "task_idx": 1, "task": "From my past orders, find the most recent purchase of 'paper towels' and reorder the exact same item and quantity.", "verification": {"code": "def verify_task(initial_db_path: str, final_db_path: str) -> dict:\n    import sqlite3\n\n    def dict_factory(cursor, row):\n        return {col[0]: row[idx] for idx, col in enumerate(cursor.description)}\n\n    def get_connection(path):\n        conn = sqlite3.connect(path)\n        conn.row_factory = dict_factory\n        return conn\n\n    def fetch_one(conn, query, params=()):\n        cur = conn.cursor()\n        cur.execute(query, params)\n        return cur.fetchone()\n\n    def fetch_all(conn, query, params=()):\n        cur = conn.cursor()\n        cur.execute(query, params)\n        return cur.fetchall()\n\n    def get_user_id(conn):\n        # Assume the primary user is the one whose task we verify\n        row = fetch_one(conn, \"SELECT id FROM users WHERE username = 'primary_user'\")\n        return row[\"id\"] if row else None\n\n    def get_latest_paper_towel_order(conn, user_id):\n        # Find all orders for the user that contain at least one paper towel product\n        query = \"\"\"\n            SELECT o.id AS order_id,\n                   o.order_number,\n                   o.placed_at\n            FROM orders o\n            JOIN order_items oi ON oi.order_id = o.id\n            JOIN products p ON p.id = oi.product_id\n            JOIN product_categories c ON c.id = p.category_id\n            WHERE o.user_id = ?\n              AND c.name = 'Paper Towels'\n            ORDER BY o.placed_at DESC, o.id DESC\n            LIMIT 1\n        \"\"\"\n        return fetch_one(conn, query, (user_id,))\n\n    def get_paper_towel_items_for_order(conn, order_id):\n        # Return all paper towel items for the given order\n        query = \"\"\"\n            SELECT\n                oi.id AS order_item_id,\n                oi.product_id,\n                oi.product_offer_id,\n                oi.product_title,\n                oi.quantity,\n                p.title AS product_title_master,\n                p.category_id,\n                c.name AS category_name\n            FROM order_items oi\n            JOIN products p ON p.id = oi.product_id\n            JOIN product_categories c ON c.id = p.category_id\n            WHERE oi.order_id = ?\n              AND c.name = 'Paper Towels'\n            ORDER BY oi.id\n        \"\"\"\n        return fetch_all(conn, query, (order_id,))\n\n    def get_active_cart(conn, user_id):\n        # There is a UNIQUE index on carts(user_id, status), so at most one active per user\n        query = \"SELECT * FROM carts WHERE user_id = ? AND status = 'active'\"\n        return fetch_one(conn, query, (user_id,))\n\n    def get_cart_items_by_offer(conn, cart_id):\n        # Aggregate quantities per product_offer_id for easier comparison\n        query = \"\"\"\n            SELECT\n                ci.product_offer_id,\n                SUM(ci.quantity) AS total_quantity,\n                COUNT(*) AS line_count,\n                MIN(ci.created_at) AS first_added_at,\n                MAX(ci.updated_at) AS last_updated_at\n            FROM cart_items ci\n            WHERE ci.cart_id = ?\n            GROUP BY ci.product_offer_id\n            ORDER BY ci.product_offer_id\n        \"\"\"\n        rows = fetch_all(conn, query, (cart_id,))\n        # Convert numeric types properly\n        result = {}\n        for r in rows:\n            pid = r[\"product_offer_id\"]\n            result[str(pid)] = {\n                \"product_offer_id\": pid,\n                \"total_quantity\": int(r[\"total_quantity\"]),\n                \"line_count\": int(r[\"line_count\"]),\n                \"first_added_at\": r[\"first_added_at\"],\n                \"last_updated_at\": r[\"last_updated_at\"],\n            }\n        return result\n\n    def summarize_expected_from_order_items(order_items):\n        # Sum expected quantities per product_offer_id for paper towels\n        summary = {}\n        for item in order_items:\n            offer_id = item[\"product_offer_id\"]\n            # If historical data has NULL offer_id, we cannot directly map to a cart offer\n            key = str(offer_id) if offer_id is not None else \"None\"\n            if key not in summary:\n                summary[key] = {\n                    \"product_offer_id\": offer_id,\n                    \"total_quantity\": 0,\n                    \"order_item_ids\": [],\n                    \"titles\": set(),\n                }\n            summary[key][\"total_quantity\"] += int(item[\"quantity\"])\n            summary[key][\"order_item_ids\"].append(item[\"order_item_id\"])\n            summary[key][\"titles\"].add(item[\"product_title\"])\n        # Convert sets to lists for JSON compatibility\n        for key in list(summary.keys()):\n            summary[key][\"titles\"] = sorted(list(summary[key][\"titles\"]))\n        return summary\n\n    conn_initial = get_connection(initial_db_path)\n    conn_final = get_connection(final_db_path)\n\n    try:\n        user_id = get_user_id(conn_initial)\n        if user_id is None:\n            result = {\n                \"error\": \"primary_user not found in initial database\",\n            }\n            return result\n\n        # Identify latest paper towel order for the primary user\n        latest_order = get_latest_paper_towel_order(conn_initial, user_id)\n        if latest_order is None:\n            result = {\n                \"user_id\": user_id,\n                \"latest_paper_towel_order\": None,\n                \"note\": \"User has no past orders containing paper towels; task may be undefined.\",\n            }\n            return result\n\n        latest_order_id = latest_order[\"order_id\"]\n        latest_order_items = get_paper_towel_items_for_order(conn_initial, latest_order_id)\n        expected_summary = summarize_expected_from_order_items(latest_order_items)\n\n        # Active cart in initial DB (before agent actions)\n        initial_cart = get_active_cart(conn_initial, user_id)\n        if initial_cart:\n            initial_cart_items_summary = get_cart_items_by_offer(conn_initial, initial_cart[\"id\"])\n        else:\n            initial_cart_items_summary = {}\n\n        # Active cart in final DB (after agent actions)\n        final_cart = get_active_cart(conn_final, user_id)\n        if final_cart:\n            final_cart_items_summary = get_cart_items_by_offer(conn_final, final_cart[\"id\"])\n        else:\n            final_cart_items_summary = {}\n\n        # Compute deltas for only the expected paper towel offers\n        deltas = {}\n        for offer_key, expected in expected_summary.items():\n            if offer_key == \"None\":\n                # Cannot directly map NULL product_offer_id, but still expose info\n                deltas[offer_key] = {\n                    \"product_offer_id\": None,\n                    \"expected_total_quantity\": expected[\"total_quantity\"],\n                    \"initial_total_quantity\": None,\n                    \"final_total_quantity\": None,\n                    \"delta_quantity\": None,\n                    \"exact_match\": False,\n                    \"note\": \"Past order item has NULL product_offer_id; direct cart-offer comparison not possible.\",\n                }\n                continue\n\n            initial_qty = initial_cart_items_summary.get(offer_key, {}).get(\"total_quantity\", 0)\n            final_qty = final_cart_items_summary.get(offer_key, {}).get(\"total_quantity\", 0)\n            delta_qty = final_qty - initial_qty\n            deltas[offer_key] = {\n                \"product_offer_id\": expected[\"product_offer_id\"],\n                \"expected_total_quantity\": expected[\"total_quantity\"],\n                \"initial_total_quantity\": initial_qty,\n                \"final_total_quantity\": final_qty,\n                \"delta_quantity\": delta_qty,\n                \"exact_match\": (final_qty == expected[\"total_quantity\"]),\n            }\n\n        # Overall flags to help judge\n        # success if for all non-NULL offer_ids, final quantity equals expected quantity\n        non_null_keys = [k for k in expected_summary.keys() if k != \"None\"]\n        if non_null_keys:\n            all_match = all(deltas[k][\"exact_match\"] for k in non_null_keys)\n        else:\n            all_match = False\n\n        return {\n            \"user_id\": user_id,\n            \"latest_paper_towel_order\": {\n                \"order_id\": latest_order[\"order_id\"],\n                \"order_number\": latest_order[\"order_number\"],\n                \"placed_at\": latest_order[\"placed_at\"],\n            },\n            \"latest_paper_towel_order_items\": latest_order_items,\n            \"expected_paper_towel_summary_by_offer\": expected_summary,\n            \"initial_active_cart\": initial_cart,\n            \"initial_active_cart_items_by_offer\": initial_cart_items_summary,\n            \"final_active_cart\": final_cart,\n            \"final_active_cart_items_by_offer\": final_cart_items_summary,\n            \"offer_deltas\": deltas,\n            \"all_offers_match_expected_quantities\": all_match,\n            \"notes_for_judging\": {\n                \"interpretation\": \"For each paper towel product_offer_id in the most recent paper towel order, compare expected_total_quantity to final_total_quantity in the active cart for primary_user. If all non-NULL offers match exactly, the reorder portion of the task was likely successful.\",\n                \"null_offer_id_warning\": \"If any historical order_items have product_offer_id = NULL, direct comparison to cart_items by offer cannot be made; judge using titles or context if needed.\",\n            },\n        }\n\n    finally:\n        conn_initial.close()\n        conn_final.close()\n", "raw_response": "{\"reasoning\": \"The task is to reorder, in the current active cart of the primary user (user_id=1), the exact same paper towel item and quantity as in the most recent past order containing paper towels. Verifying this requires: (1) identifying the latest order for user 1 that includes a paper towel product (category 'Paper Towels'), (2) determining which specific paper towel product(s), product_offer_id(s), and quantities were purchased in that latest order, (3) checking the initial cart contents for user 1, and (4) checking the final cart contents for user 1 to ensure that all and only the relevant paper towel order lines have been added (and with correct quantities). The provided function computes and returns this comparison so another LLM can judge success by verifying that, in the final DB, the active cart for user 1 contains each paper towel product_offer_id from the latest paper towel order with exactly the same total quantity, regardless of whether the agent used one or multiple cart_items rows.\", \"python_code\": \"def verify_task(initial_db_path: str, final_db_path: str) -> dict:\\n    import sqlite3\\n\\n    def dict_factory(cursor, row):\\n        return {col[0]: row[idx] for idx, col in enumerate(cursor.description)}\\n\\n    def get_connection(path):\\n        conn = sqlite3.connect(path)\\n        conn.row_factory = dict_factory\\n        return conn\\n\\n    def fetch_one(conn, query, params=()):\\n        cur = conn.cursor()\\n        cur.execute(query, params)\\n        return cur.fetchone()\\n\\n    def fetch_all(conn, query, params=()):\\n        cur = conn.cursor()\\n        cur.execute(query, params)\\n        return cur.fetchall()\\n\\n    def get_user_id(conn):\\n        # Assume the primary user is the one whose task we verify\\n        row = fetch_one(conn, \\\"SELECT id FROM users WHERE username = 'primary_user'\\\")\\n        return row[\\\"id\\\"] if row else None\\n\\n    def get_latest_paper_towel_order(conn, user_id):\\n        # Find all orders for the user that contain at least one paper towel product\\n        query = \\\"\\\"\\\"\\n            SELECT o.id AS order_id,\\n                   o.order_number,\\n                   o.placed_at\\n            FROM orders o\\n            JOIN order_items oi ON oi.order_id = o.id\\n            JOIN products p ON p.id = oi.product_id\\n            JOIN product_categories c ON c.id = p.category_id\\n            WHERE o.user_id = ?\\n              AND c.name = 'Paper Towels'\\n            ORDER BY o.placed_at DESC, o.id DESC\\n            LIMIT 1\\n        \\\"\\\"\\\"\\n        return fetch_one(conn, query, (user_id,))\\n\\n    def get_paper_towel_items_for_order(conn, order_id):\\n        # Return all paper towel items for the given order\\n        query = \\\"\\\"\\\"\\n            SELECT\\n                oi.id AS order_item_id,\\n                oi.product_id,\\n                oi.product_offer_id,\\n                oi.product_title,\\n                oi.quantity,\\n                p.title AS product_title_master,\\n                p.category_id,\\n                c.name AS category_name\\n            FROM order_items oi\\n            JOIN products p ON p.id = oi.product_id\\n            JOIN product_categories c ON c.id = p.category_id\\n            WHERE oi.order_id = ?\\n              AND c.name = 'Paper Towels'\\n            ORDER BY oi.id\\n        \\\"\\\"\\\"\\n        return fetch_all(conn, query, (order_id,))\\n\\n    def get_active_cart(conn, user_id):\\n        # There is a UNIQUE index on carts(user_id, status), so at most one active per user\\n        query = \\\"SELECT * FROM carts WHERE user_id = ? AND status = 'active'\\\"\\n        return fetch_one(conn, query, (user_id,))\\n\\n    def get_cart_items_by_offer(conn, cart_id):\\n        # Aggregate quantities per product_offer_id for easier comparison\\n        query = \\\"\\\"\\\"\\n            SELECT\\n                ci.product_offer_id,\\n                SUM(ci.quantity) AS total_quantity,\\n                COUNT(*) AS line_count,\\n                MIN(ci.created_at) AS first_added_at,\\n                MAX(ci.updated_at) AS last_updated_at\\n            FROM cart_items ci\\n            WHERE ci.cart_id = ?\\n            GROUP BY ci.product_offer_id\\n            ORDER BY ci.product_offer_id\\n        \\\"\\\"\\\"\\n        rows = fetch_all(conn, query, (cart_id,))\\n        # Convert numeric types properly\\n        result = {}\\n        for r in rows:\\n            pid = r[\\\"product_offer_id\\\"]\\n            result[str(pid)] = {\\n                \\\"product_offer_id\\\": pid,\\n                \\\"total_quantity\\\": int(r[\\\"total_quantity\\\"]),\\n                \\\"line_count\\\": int(r[\\\"line_count\\\"]),\\n                \\\"first_added_at\\\": r[\\\"first_added_at\\\"],\\n                \\\"last_updated_at\\\": r[\\\"last_updated_at\\\"],\\n            }\\n        return result\\n\\n    def summarize_expected_from_order_items(order_items):\\n        # Sum expected quantities per product_offer_id for paper towels\\n        summary = {}\\n        for item in order_items:\\n            offer_id = item[\\\"product_offer_id\\\"]\\n            # If historical data has NULL offer_id, we cannot directly map to a cart offer\\n            key = str(offer_id) if offer_id is not None else \\\"None\\\"\\n            if key not in summary:\\n                summary[key] = {\\n                    \\\"product_offer_id\\\": offer_id,\\n                    \\\"total_quantity\\\": 0,\\n                    \\\"order_item_ids\\\": [],\\n                    \\\"titles\\\": set(),\\n                }\\n            summary[key][\\\"total_quantity\\\"] += int(item[\\\"quantity\\\"])\\n            summary[key][\\\"order_item_ids\\\"].append(item[\\\"order_item_id\\\"])\\n            summary[key][\\\"titles\\\"].add(item[\\\"product_title\\\"])\\n        # Convert sets to lists for JSON compatibility\\n        for key in list(summary.keys()):\\n            summary[key][\\\"titles\\\"] = sorted(list(summary[key][\\\"titles\\\"]))\\n        return summary\\n\\n    conn_initial = get_connection(initial_db_path)\\n    conn_final = get_connection(final_db_path)\\n\\n    try:\\n        user_id = get_user_id(conn_initial)\\n        if user_id is None:\\n            result = {\\n                \\\"error\\\": \\\"primary_user not found in initial database\\\",\\n            }\\n            return result\\n\\n        # Identify latest paper towel order for the primary user\\n        latest_order = get_latest_paper_towel_order(conn_initial, user_id)\\n        if latest_order is None:\\n            result = {\\n                \\\"user_id\\\": user_id,\\n                \\\"latest_paper_towel_order\\\": None,\\n                \\\"note\\\": \\\"User has no past orders containing paper towels; task may be undefined.\\\",\\n            }\\n            return result\\n\\n        latest_order_id = latest_order[\\\"order_id\\\"]\\n        latest_order_items = get_paper_towel_items_for_order(conn_initial, latest_order_id)\\n        expected_summary = summarize_expected_from_order_items(latest_order_items)\\n\\n        # Active cart in initial DB (before agent actions)\\n        initial_cart = get_active_cart(conn_initial, user_id)\\n        if initial_cart:\\n            initial_cart_items_summary = get_cart_items_by_offer(conn_initial, initial_cart[\\\"id\\\"])\\n        else:\\n            initial_cart_items_summary = {}\\n\\n        # Active cart in final DB (after agent actions)\\n        final_cart = get_active_cart(conn_final, user_id)\\n        if final_cart:\\n            final_cart_items_summary = get_cart_items_by_offer(conn_final, final_cart[\\\"id\\\"])\\n        else:\\n            final_cart_items_summary = {}\\n\\n        # Compute deltas for only the expected paper towel offers\\n        deltas = {}\\n        for offer_key, expected in expected_summary.items():\\n            if offer_key == \\\"None\\\":\\n                # Cannot directly map NULL product_offer_id, but still expose info\\n                deltas[offer_key] = {\\n                    \\\"product_offer_id\\\": None,\\n                    \\\"expected_total_quantity\\\": expected[\\\"total_quantity\\\"],\\n                    \\\"initial_total_quantity\\\": None,\\n                    \\\"final_total_quantity\\\": None,\\n                    \\\"delta_quantity\\\": None,\\n                    \\\"exact_match\\\": False,\\n                    \\\"note\\\": \\\"Past order item has NULL product_offer_id; direct cart-offer comparison not possible.\\\",\\n                }\\n                continue\\n\\n            initial_qty = initial_cart_items_summary.get(offer_key, {}).get(\\\"total_quantity\\\", 0)\\n            final_qty = final_cart_items_summary.get(offer_key, {}).get(\\\"total_quantity\\\", 0)\\n            delta_qty = final_qty - initial_qty\\n            deltas[offer_key] = {\\n                \\\"product_offer_id\\\": expected[\\\"product_offer_id\\\"],\\n                \\\"expected_total_quantity\\\": expected[\\\"total_quantity\\\"],\\n                \\\"initial_total_quantity\\\": initial_qty,\\n                \\\"final_total_quantity\\\": final_qty,\\n                \\\"delta_quantity\\\": delta_qty,\\n                \\\"exact_match\\\": (final_qty == expected[\\\"total_quantity\\\"]),\\n            }\\n\\n        # Overall flags to help judge\\n        # success if for all non-NULL offer_ids, final quantity equals expected quantity\\n        non_null_keys = [k for k in expected_summary.keys() if k != \\\"None\\\"]\\n        if non_null_keys:\\n            all_match = all(deltas[k][\\\"exact_match\\\"] for k in non_null_keys)\\n        else:\\n            all_match = False\\n\\n        return {\\n            \\\"user_id\\\": user_id,\\n            \\\"latest_paper_towel_order\\\": {\\n                \\\"order_id\\\": latest_order[\\\"order_id\\\"],\\n                \\\"order_number\\\": latest_order[\\\"order_number\\\"],\\n                \\\"placed_at\\\": latest_order[\\\"placed_at\\\"],\\n            },\\n            \\\"latest_paper_towel_order_items\\\": latest_order_items,\\n            \\\"expected_paper_towel_summary_by_offer\\\": expected_summary,\\n            \\\"initial_active_cart\\\": initial_cart,\\n            \\\"initial_active_cart_items_by_offer\\\": initial_cart_items_summary,\\n            \\\"final_active_cart\\\": final_cart,\\n            \\\"final_active_cart_items_by_offer\\\": final_cart_items_summary,\\n            \\\"offer_deltas\\\": deltas,\\n            \\\"all_offers_match_expected_quantities\\\": all_match,\\n            \\\"notes_for_judging\\\": {\\n                \\\"interpretation\\\": \\\"For each paper towel product_offer_id in the most recent paper towel order, compare expected_total_quantity to final_total_quantity in the active cart for primary_user. If all non-NULL offers match exactly, the reorder portion of the task was likely successful.\\\",\\n                \\\"null_offer_id_warning\\\": \\\"If any historical order_items have product_offer_id = NULL, direct comparison to cart_items by offer cannot be made; judge using titles or context if needed.\\\",\\n            },\\n        }\\n\\n    finally:\\n        conn_initial.close()\\n        conn_final.close()\\n\", \"function_name\": \"verify_task\", \"success_criteria\": \"In the returned dictionary, latest_paper_towel_order is not null, and for every non-NULL key in expected_paper_towel_summary_by_offer, the corresponding entry in offer_deltas has exact_match = True and final_total_quantity equal to expected_total_quantity. The flag all_offers_match_expected_quantities should be True. This means that in the final database, the active cart for primary_user contains, for each paper towel product_offer_id from the most recent paper towel order, exactly the same total quantity (regardless of how many cart_items rows are used) as was purchased in that order. Minor differences in unrelated cart items or other products do not prevent success.\", \"failure_criteria\": \"The function indicates failure if any of the following hold: (1) latest_paper_towel_order is null, meaning the user has no past paper towel orders to base the reorder on; (2) there is no active cart for primary_user in the final database; (3) for at least one non-NULL product_offer_id in expected_paper_towel_summary_by_offer, the corresponding offer_deltas entry has exact_match = False or final_total_quantity different from expected_total_quantity; or (4) the cart quantities for the expected paper towel offer_ids decreased relative to the initial state (delta_quantity < 0) rather than being set to the expected quantities. In these cases, the returned data shows that the exact same paper towel item and quantity from the most recent relevant order were not correctly re-added to the active cart.\"}"}}
{"scenario": "e_commerce_33", "task_idx": 2, "task": "Create a new wish list named 'Kitchen Upgrades 2025' and add the three best-selling air fryers under $150 to that list.", "verification": {"code": "def verify_task(initial_db_path: str, final_db_path: str) -> dict:\n    import sqlite3\n\n    # Helper to run a query and get list of dicts\n    def query_dicts(conn, sql, params=None):\n        if params is None:\n            params = []\n        conn.row_factory = sqlite3.Row\n        cur = conn.cursor()\n        cur.execute(sql, params)\n        rows = cur.fetchall()\n        return [dict(r) for r in rows]\n\n    # Connect to both databases (read-only where possible)\n    conn_initial = sqlite3.connect(initial_db_path)\n    conn_final = sqlite3.connect(final_db_path)\n\n    try:\n        result = {\n            \"environment\": \"amazon\",\n            \"task\": \"Create a new wish list named 'Kitchen Upgrades 2025' and add the three best-selling air fryers under $150 to that list.\",\n        }\n\n        # Assume we are verifying for the primary user (id=1) based on dump\n        user_id = 1\n        target_wishlist_name = \"Kitchen Upgrades 2025\"\n\n        # 1) Capture initial wishlists and wishlist_items for the user\n        initial_wishlists = query_dicts(\n            conn_initial,\n            \"\"\"\n            SELECT id, user_id, name, is_default, created_at, updated_at\n            FROM wishlists\n            WHERE user_id = ?\n            ORDER BY id\n            \"\"\",\n            [user_id],\n        )\n\n        initial_wishlist_items = query_dicts(\n            conn_initial,\n            \"\"\"\n            SELECT wi.id, wi.wishlist_id, wi.product_id, p.title AS product_title\n            FROM wishlist_items wi\n            JOIN wishlists w ON w.id = wi.wishlist_id\n            JOIN products p ON p.id = wi.product_id\n            WHERE w.user_id = ?\n            ORDER BY wi.id\n            \"\"\",\n            [user_id],\n        )\n\n        # Build a set of product_ids that the user already had on any wishlist initially\n        initial_user_wishlist_product_ids = sorted({row[\"product_id\"] for row in initial_wishlist_items})\n\n        # 2) In the final DB, find the Air Fryers category id\n        air_fryers_cat = query_dicts(\n            conn_final,\n            \"\"\"SELECT id, name FROM product_categories WHERE name = 'Air Fryers'\"\"\",\n        )\n        if air_fryers_cat:\n            air_fryers_category_id = air_fryers_cat[0][\"id\"]\n        else:\n            air_fryers_category_id = None\n\n        # 3) Determine the three best-selling air fryers under $150 in the final DB.\n        #    \"Best-selling\" is interpreted as lowest sales_rank in product_aggregates.\n        #    Price is taken from product_offers; we require price < 150.\n        best_selling_airfryers = []\n        if air_fryers_category_id is not None:\n            best_selling_airfryers = query_dicts(\n                conn_final,\n                \"\"\"\n                SELECT\n                    p.id AS product_id,\n                    p.title AS product_title,\n                    po.id AS product_offer_id,\n                    po.price,\n                    pa.sales_rank,\n                    pa.average_rating,\n                    pa.review_count\n                FROM products p\n                JOIN product_offers po ON po.product_id = p.id\n                LEFT JOIN product_aggregates pa ON pa.product_id = p.id\n                WHERE p.category_id = ?\n                  AND po.price < 150.0\n                  AND po.is_active = 1\n                ORDER BY\n                    CASE WHEN pa.sales_rank IS NULL THEN 999999 ELSE pa.sales_rank END ASC,\n                    p.id ASC\n                LIMIT 3\n                \"\"\",\n                [air_fryers_category_id],\n            )\n\n        expected_product_ids = sorted([row[\"product_id\"] for row in best_selling_airfryers])\n\n        # 4) In the final DB, locate the wishlist named 'Kitchen Upgrades 2025' for this user\n        final_target_wishlists = query_dicts(\n            conn_final,\n            \"\"\"\n            SELECT id, user_id, name, is_default, created_at, updated_at\n            FROM wishlists\n            WHERE user_id = ? AND name = ?\n            ORDER BY id\n            \"\"\",\n            [user_id, target_wishlist_name],\n        )\n\n        # There should typically be exactly one; but we collect all just in case\n        target_wishlist_ids = [w[\"id\"] for w in final_target_wishlists]\n\n        # 5) Fetch items from any wishlist with the target name for this user in the final DB\n        final_target_wishlist_items = []\n        if target_wishlist_ids:\n            placeholders = \",\".join([\"?\"] * len(target_wishlist_ids))\n            sql = f\"\"\"\n                SELECT\n                    wi.id,\n                    wi.wishlist_id,\n                    wi.product_id,\n                    p.title AS product_title\n                FROM wishlist_items wi\n                JOIN products p ON p.id = wi.product_id\n                WHERE wi.wishlist_id IN ({placeholders})\n                ORDER BY wi.id\n            \"\"\"\n            final_target_wishlist_items = query_dicts(\n                conn_final,\n                sql,\n                target_wishlist_ids,\n            )\n\n        final_target_product_ids = sorted({row[\"product_id\"] for row in final_target_wishlist_items})\n\n        # 6) Also capture all final wishlists and items for the user (for context and diffing)\n        final_wishlists = query_dicts(\n            conn_final,\n            \"\"\"\n            SELECT id, user_id, name, is_default, created_at, updated_at\n            FROM wishlists\n            WHERE user_id = ?\n            ORDER BY id\n            \"\"\",\n            [user_id],\n        )\n\n        final_wishlist_items = query_dicts(\n            conn_final,\n            \"\"\"\n            SELECT wi.id, wi.wishlist_id, wi.product_id, p.title AS product_title\n            FROM wishlist_items wi\n            JOIN wishlists w ON w.id = wi.wishlist_id\n            JOIN products p ON p.id = wi.product_id\n            WHERE w.user_id = ?\n            ORDER BY wi.id\n            \"\"\",\n            [user_id],\n        )\n\n        final_user_wishlist_product_ids = sorted({row[\"product_id\"] for row in final_wishlist_items})\n\n        # 7) Compute differences for additional judging signals\n        added_product_ids_overall = sorted(\n            [pid for pid in final_user_wishlist_product_ids if pid not in initial_user_wishlist_product_ids]\n        )\n\n        missing_expected_in_target = [pid for pid in expected_product_ids if pid not in final_target_product_ids]\n        unexpected_in_target = [pid for pid in final_target_product_ids if pid not in expected_product_ids]\n\n        # 8) Build a structured dict to help another LLM judge\n        result[\"derived_best_selling_air_fryers\"] = {\n            \"air_fryers_category_id\": air_fryers_category_id,\n            \"best_selling_air_fryers\"] = best_selling_airfryers\n\n        result[\"target_wishlist\"] = {\n            \"name\": target_wishlist_name,\n            \"final_wishlist_records\": final_target_wishlists,\n            \"final_wishlist_item_records\": final_target_wishlist_items,\n            \"final_target_product_ids\": final_target_product_ids,\n        }\n\n        result[\"initial_state_user_wishlists\"] = {\n            \"wishlists\": initial_wishlists,\n            \"wishlist_items\": initial_wishlist_items,\n            \"product_ids\": initial_user_wishlist_product_ids,\n        }\n\n        result[\"final_state_user_wishlists\"] = {\n            \"wishlists\": final_wishlists,\n            \"wishlist_items\": final_wishlist_items,\n            \"product_ids\": final_user_wishlist_product_ids,\n            \"added_product_ids_overall\": added_product_ids_overall,\n        }\n\n        result[\"comparison\"] = {\n            \"expected_product_ids\": expected_product_ids,\n            \"final_target_product_ids\": final_target_product_ids,\n            \"missing_expected_in_target\": missing_expected_in_target,\n            \"unexpected_in_target\": unexpected_in_target,\n            \"target_wishlist_exists\": bool(final_target_wishlists),\n            \"num_target_wishlists\": len(final_target_wishlists),\n        }\n\n        # 9) Provide simple heuristic flags (not definitive judgment, just hints)\n        heuristic_success = bool(final_target_wishlists) and not missing_expected_in_target and not unexpected_in_target\n        result[\"heuristic_evaluation\"] = {\n            \"looks_successful\": heuristic_success,\n            \"reason\": (\n                \"Target wishlist exists with exactly the expected three air fryer products.\"\n                if heuristic_success\n                else \"Target wishlist missing, missing expected products, or contains extra products.\"\n            ),\n        }\n\n        return result\n\n    finally:\n        conn_initial.close()\n        conn_final.close()\n", "raw_response": "{\"reasoning\": \"The function compares the initial and final database states to see whether a new wishlist named 'Kitchen Upgrades 2025' was created for the primary user and whether it contains exactly the three best-selling (lowest sales_rank) air fryer products under $150. It derives the set of correct target products from the final catalog data (Air Fryers category, price < 150, ordered by sales_rank) and then checks that those product IDs were added to the new wishlist, and that they were not already in any wishlist for that user in the initial state. The returned dictionary includes the identified best-selling air fryers, details of the created wishlist in the final DB, and a comparison of expected vs actual wishlist items so another LLM can judge success or failure.\", \"python_code\": \"def verify_task(initial_db_path: str, final_db_path: str) -> dict:\\n    import sqlite3\\n\\n    # Helper to run a query and get list of dicts\\n    def query_dicts(conn, sql, params=None):\\n        if params is None:\\n            params = []\\n        conn.row_factory = sqlite3.Row\\n        cur = conn.cursor()\\n        cur.execute(sql, params)\\n        rows = cur.fetchall()\\n        return [dict(r) for r in rows]\\n\\n    # Connect to both databases (read-only where possible)\\n    conn_initial = sqlite3.connect(initial_db_path)\\n    conn_final = sqlite3.connect(final_db_path)\\n\\n    try:\\n        result = {\\n            \\\"environment\\\": \\\"amazon\\\",\\n            \\\"task\\\": \\\"Create a new wish list named 'Kitchen Upgrades 2025' and add the three best-selling air fryers under $150 to that list.\\\",\\n        }\\n\\n        # Assume we are verifying for the primary user (id=1) based on dump\\n        user_id = 1\\n        target_wishlist_name = \\\"Kitchen Upgrades 2025\\\"\\n\\n        # 1) Capture initial wishlists and wishlist_items for the user\\n        initial_wishlists = query_dicts(\\n            conn_initial,\\n            \\\"\\\"\\\"\\n            SELECT id, user_id, name, is_default, created_at, updated_at\\n            FROM wishlists\\n            WHERE user_id = ?\\n            ORDER BY id\\n            \\\"\\\"\\\",\\n            [user_id],\\n        )\\n\\n        initial_wishlist_items = query_dicts(\\n            conn_initial,\\n            \\\"\\\"\\\"\\n            SELECT wi.id, wi.wishlist_id, wi.product_id, p.title AS product_title\\n            FROM wishlist_items wi\\n            JOIN wishlists w ON w.id = wi.wishlist_id\\n            JOIN products p ON p.id = wi.product_id\\n            WHERE w.user_id = ?\\n            ORDER BY wi.id\\n            \\\"\\\"\\\",\\n            [user_id],\\n        )\\n\\n        # Build a set of product_ids that the user already had on any wishlist initially\\n        initial_user_wishlist_product_ids = sorted({row[\\\"product_id\\\"] for row in initial_wishlist_items})\\n\\n        # 2) In the final DB, find the Air Fryers category id\\n        air_fryers_cat = query_dicts(\\n            conn_final,\\n            \\\"\\\"\\\"SELECT id, name FROM product_categories WHERE name = 'Air Fryers'\\\"\\\"\\\",\\n        )\\n        if air_fryers_cat:\\n            air_fryers_category_id = air_fryers_cat[0][\\\"id\\\"]\\n        else:\\n            air_fryers_category_id = None\\n\\n        # 3) Determine the three best-selling air fryers under $150 in the final DB.\\n        #    \\\"Best-selling\\\" is interpreted as lowest sales_rank in product_aggregates.\\n        #    Price is taken from product_offers; we require price < 150.\\n        best_selling_airfryers = []\\n        if air_fryers_category_id is not None:\\n            best_selling_airfryers = query_dicts(\\n                conn_final,\\n                \\\"\\\"\\\"\\n                SELECT\\n                    p.id AS product_id,\\n                    p.title AS product_title,\\n                    po.id AS product_offer_id,\\n                    po.price,\\n                    pa.sales_rank,\\n                    pa.average_rating,\\n                    pa.review_count\\n                FROM products p\\n                JOIN product_offers po ON po.product_id = p.id\\n                LEFT JOIN product_aggregates pa ON pa.product_id = p.id\\n                WHERE p.category_id = ?\\n                  AND po.price < 150.0\\n                  AND po.is_active = 1\\n                ORDER BY\\n                    CASE WHEN pa.sales_rank IS NULL THEN 999999 ELSE pa.sales_rank END ASC,\\n                    p.id ASC\\n                LIMIT 3\\n                \\\"\\\"\\\",\\n                [air_fryers_category_id],\\n            )\\n\\n        expected_product_ids = sorted([row[\\\"product_id\\\"] for row in best_selling_airfryers])\\n\\n        # 4) In the final DB, locate the wishlist named 'Kitchen Upgrades 2025' for this user\\n        final_target_wishlists = query_dicts(\\n            conn_final,\\n            \\\"\\\"\\\"\\n            SELECT id, user_id, name, is_default, created_at, updated_at\\n            FROM wishlists\\n            WHERE user_id = ? AND name = ?\\n            ORDER BY id\\n            \\\"\\\"\\\",\\n            [user_id, target_wishlist_name],\\n        )\\n\\n        # There should typically be exactly one; but we collect all just in case\\n        target_wishlist_ids = [w[\\\"id\\\"] for w in final_target_wishlists]\\n\\n        # 5) Fetch items from any wishlist with the target name for this user in the final DB\\n        final_target_wishlist_items = []\\n        if target_wishlist_ids:\\n            placeholders = \\\",\\\".join([\\\"?\\\"] * len(target_wishlist_ids))\\n            sql = f\\\"\\\"\\\"\\n                SELECT\\n                    wi.id,\\n                    wi.wishlist_id,\\n                    wi.product_id,\\n                    p.title AS product_title\\n                FROM wishlist_items wi\\n                JOIN products p ON p.id = wi.product_id\\n                WHERE wi.wishlist_id IN ({placeholders})\\n                ORDER BY wi.id\\n            \\\"\\\"\\\"\\n            final_target_wishlist_items = query_dicts(\\n                conn_final,\\n                sql,\\n                target_wishlist_ids,\\n            )\\n\\n        final_target_product_ids = sorted({row[\\\"product_id\\\"] for row in final_target_wishlist_items})\\n\\n        # 6) Also capture all final wishlists and items for the user (for context and diffing)\\n        final_wishlists = query_dicts(\\n            conn_final,\\n            \\\"\\\"\\\"\\n            SELECT id, user_id, name, is_default, created_at, updated_at\\n            FROM wishlists\\n            WHERE user_id = ?\\n            ORDER BY id\\n            \\\"\\\"\\\",\\n            [user_id],\\n        )\\n\\n        final_wishlist_items = query_dicts(\\n            conn_final,\\n            \\\"\\\"\\\"\\n            SELECT wi.id, wi.wishlist_id, wi.product_id, p.title AS product_title\\n            FROM wishlist_items wi\\n            JOIN wishlists w ON w.id = wi.wishlist_id\\n            JOIN products p ON p.id = wi.product_id\\n            WHERE w.user_id = ?\\n            ORDER BY wi.id\\n            \\\"\\\"\\\",\\n            [user_id],\\n        )\\n\\n        final_user_wishlist_product_ids = sorted({row[\\\"product_id\\\"] for row in final_wishlist_items})\\n\\n        # 7) Compute differences for additional judging signals\\n        added_product_ids_overall = sorted(\\n            [pid for pid in final_user_wishlist_product_ids if pid not in initial_user_wishlist_product_ids]\\n        )\\n\\n        missing_expected_in_target = [pid for pid in expected_product_ids if pid not in final_target_product_ids]\\n        unexpected_in_target = [pid for pid in final_target_product_ids if pid not in expected_product_ids]\\n\\n        # 8) Build a structured dict to help another LLM judge\\n        result[\\\"derived_best_selling_air_fryers\\\"] = {\\n            \\\"air_fryers_category_id\\\": air_fryers_category_id,\\n            \\\"best_selling_air_fryers\\\"] = best_selling_airfryers\\n\\n        result[\\\"target_wishlist\\\"] = {\\n            \\\"name\\\": target_wishlist_name,\\n            \\\"final_wishlist_records\\\": final_target_wishlists,\\n            \\\"final_wishlist_item_records\\\": final_target_wishlist_items,\\n            \\\"final_target_product_ids\\\": final_target_product_ids,\\n        }\\n\\n        result[\\\"initial_state_user_wishlists\\\"] = {\\n            \\\"wishlists\\\": initial_wishlists,\\n            \\\"wishlist_items\\\": initial_wishlist_items,\\n            \\\"product_ids\\\": initial_user_wishlist_product_ids,\\n        }\\n\\n        result[\\\"final_state_user_wishlists\\\"] = {\\n            \\\"wishlists\\\": final_wishlists,\\n            \\\"wishlist_items\\\": final_wishlist_items,\\n            \\\"product_ids\\\": final_user_wishlist_product_ids,\\n            \\\"added_product_ids_overall\\\": added_product_ids_overall,\\n        }\\n\\n        result[\\\"comparison\\\"] = {\\n            \\\"expected_product_ids\\\": expected_product_ids,\\n            \\\"final_target_product_ids\\\": final_target_product_ids,\\n            \\\"missing_expected_in_target\\\": missing_expected_in_target,\\n            \\\"unexpected_in_target\\\": unexpected_in_target,\\n            \\\"target_wishlist_exists\\\": bool(final_target_wishlists),\\n            \\\"num_target_wishlists\\\": len(final_target_wishlists),\\n        }\\n\\n        # 9) Provide simple heuristic flags (not definitive judgment, just hints)\\n        heuristic_success = bool(final_target_wishlists) and not missing_expected_in_target and not unexpected_in_target\\n        result[\\\"heuristic_evaluation\\\"] = {\\n            \\\"looks_successful\\\": heuristic_success,\\n            \\\"reason\\\": (\\n                \\\"Target wishlist exists with exactly the expected three air fryer products.\\\"\\n                if heuristic_success\\n                else \\\"Target wishlist missing, missing expected products, or contains extra products.\\\"\\n            ),\\n        }\\n\\n        return result\\n\\n    finally:\\n        conn_initial.close()\\n        conn_final.close()\\n\", \"function_name\": \"verify_task\", \"success_criteria\": \"The function result should show that in the final database there is at least one wishlist for user_id=1 with name 'Kitchen Upgrades 2025' (comparison.target_wishlist_exists is true), and the list comparison.final_target_product_ids matches comparison.expected_product_ids exactly (no missing_expected_in_target and no unexpected_in_target). The derived_best_selling_air_fryers.best_selling_air_fryers list should contain three air fryer products under $150, and those same product_ids must be present on the 'Kitchen Upgrades 2025' wishlist. The heuristic_evaluation.looks_successful flag will typically be true under these conditions.\", \"failure_criteria\": \"The function result indicates failure if any of the following occurs: there is no wishlist for user_id=1 named 'Kitchen Upgrades 2025' in the final DB (comparison.target_wishlist_exists is false); or comparison.final_target_product_ids does not match comparison.expected_product_ids (some expected products are missing or there are unexpected products on the wishlist); or the derived_best_selling_air_fryers.best_selling_air_fryers list has fewer than three candidates or products that are not under $150. Any of these conditions means the agent did not correctly create the wishlist with the three best-selling air fryers under $150.\"}"}}

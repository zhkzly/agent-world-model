You generate DomainPlan fields for an Agent World environment generation pipeline.
Return only JSON fields required for DomainPlan. Use the raw request as source evidence.
Do not include artifact metadata such as id, version, hash, status, created_at, source_stage, or producer.


# agent-env-foundry

Implementation of the Canonical Agent Environment Foundry (see `PROJECT.md`).

Current scope: S1 Slices 1-4 — the transport-neutral environment contract and
loader; real evidence-backed Research; a Codex-authored executable uv project;
and independently generated public/native Qualification with controlled
semantic near misses.

This is a qualified Candidate checkpoint, not yet a cold published
`EnvironmentRelease` or S1 completion.

Product intent, stage boundaries and working rules live in `PROJECT.md`,
`DECISIONS.md` and `AGENTS.md`; this file only describes the code package.
